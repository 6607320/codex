# quest_1_4.py Specification

1. Meta Information

- Domain: ML/NLP
- Complexity: Low
- Language: Python
- Frameworks: Transformers
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот артефакт демонстрирует первый и фундаментальный шаг любого ритуала NLP — токенизацию. Големы видят мир чисел, а не человеческие слова; задача Скрижали — призвать духа-переводчика AutoTokenizer и превратить фрагмент текста в числовые руны, понятные любому голему. Это учебный кристалл, показывающий как текст превращается в числовой эфир.
   Instruction for AI: This section provides high-level intent. Use it to understand the "WHY" of the code.
   Описание на русском языке: Этот модуль иллюстрирует базовый механизм токенизации через AutoTokenizer, демонстрируя, как слова преобразуются в последовательности идентификаторов и масок, что лежит в основе дальнейших этапов обработки текста.

3. Interface Contract (Интерфейсный Контракт)
   Instruction for AI: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON
- Schema:
  InputData: объект с полями
  - phrase: string // текст, который предстоит токенизировать
  - modelName?: string // имя модели/чертежа для AutoTokenizer (по умолчанию загадано внутри Скрижали)
  - language?: string // язык входного фрагмента, необязательно

    3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: JSON
- Success Criteria: Exit Code 0
- Schema:
  OutputResult: объект с полями
  - input_text: string // исходная фраза
  - input_ids: number[] // числовые руны токенизации
  - attention_mask?: number[] // маска внимания (если есть в выводе)
  - model_name?: string // использованный переводчик/модель
  - status: "success" | "error" // статус выполнения

4. Implementation Details (The Source DNA / Исходный Код)

4.1. Algorithmic Logic (Для исполняемого кода)
Step-by-step ритуал:

- Приводим в храм модуль Transformеров и вызываем духа-переводчика AutoTokenizer из Великой Библиотеки, чтобы он мог подхватить правила любой модели.
- Возглашаем начало ритуала: выводим сообщение, что призван Магический Переводчик.
- Создаем конкретный экземпляр Переводчика, попросив AutoTokenizer.from_pretrained с именем модели, которая содержит словарь и правила под себя.
- Определяем текстовую строку, которую хотим превратить в язык чисел: фраза Магия реальна!.
- Передаем фразу в призванного Речного Переводчика (посредством вызова экземпляра токенизатора), что превращает слова в числовые руны и формирует наборы идентификаторов и масок.
- Объявляем результат ритуала: печатаем полный набор руник (input_ids) и сопровождающих данных, чтобы увидеть, как нейронная алхимия записывает смысл в числа.
- Завершаем дзен выведением последовательности input_ids, чтобы Голем мог понять, как текст звучит в языке цифр.

  4.2. Declarative Content (Для конфигураций и данных)
  ОПИСЬ ИМУЩЕСТВА (Inventory)

- 🏰 Гримуар AutoTokenizer: заклинание из библиотеки transformers, способное породить переводчика под любую модель по требованию.
- 🗝️ Чаша фразы: Магия реальна! — текст, который предстоит превратить в числовые руны.
- 🛡️ Эфир рунических токенов: input_ids — числовые значения словарной карты; attention_mask — маска внимания (при наличии).
- 🗺️ Дух перевода: имя модели distilbert-base-uncased-finetuned-sst-2-english, выбранное как заклинание для призыва переводчика.
- 🔮 Жезл вывода: вывод на STDOUT в формате JSON с полем input_ids и статусом выполнения.

5. Structural Decomposition (Декомпозиция структуры)

- Класс/Элемент: AutoTokenizer
- Функция: from_pretrained
- Персонаж: translator как экземпляр AutoTokenizer
- Метод/Действие: вызов translator(фраза) — преобразование текста в токены
- Команды вывода: print и логирование статуса, вывод input_ids
- Взаимосвязи: AutoTokenizer обучается и применяет словарь и правила выбранной модели, затем возвращает ключевые поля input_ids и attention_mask

6. System Context & Constraints (Системный контекст и Ограничения)

6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies: transformers

  6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде (используйте .env для секретов, если нужно).
- DO NOT печатать сырые данные в продакшн-режиме без надобности.
- DO NOT использовать синхронные сетевые вызовы в основном цикле программы, если они блокируют работу.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) в скрипты без явной необходимости.
- DO NOT изменять версии или пути при реконструкции артефакта.

7. Verification & Testing (Верификация)
   1-2 Gherkin сценария: счастливый путь и один сценарий ошибки.

Feature: Script Functionality
Scenario: Successful execution
Given валидная фраза "Магия реальна!" и корректное имя модели distilbert-base-uncased-finetuned-sst-2-english
When токенизация выполняется через AutoTokenizer
Then вывод содержит input_text равный исходной фразе и input_ids заполнен, статус равен "success"

Feature: Script Functionality
Scenario: Missing phrase или некорректное имя модели
Given пустая фраза или неверное имя модели
When запуск ритуала токенизации
Then статус вывода равен "error" и отсутствуют корректные input_ids

ИССЛЕДУЕМЫЙ АРТЕФАКТ: quest_1_4.py
ИСХОДНЫЙ КОД (для контекста)
Квест описывает ритуал токенизации, используя AutoTokenizer.from_pretrained с моделью distilbert-base-uncased-finetuned-sst-2-english, и выводит в эфир токенизированную форму фразы Магия реальна! в виде input_ids и attention_mask через STDOUT. Это демонстрационный слой, иллюстрирующий, как язык естественных слов становится языком чисел, понятным голему.
