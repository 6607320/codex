# quest_7_3.py Specification

## 1. Meta Information

- Domain: Scripting | ML/NLP
- Complexity: Medium
- Language: Python
- Frameworks: PyTorch, diffusers, PEFT
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот модуль демонстрирует применение LoRA-печати к генеративной печати через конвейер Stable Diffusion. Он демонстрирует, как «просветленный» Демиург может творить образ, наложив на базовую модель обученную магическую броню, и как сохранить полученный шедевр в файл.  
Instruction for AI: This section provides high-level intent. Use it to understand the "WHY" of the code.

Этот файл служит иллюстративным квестом по модульной природе LoRA: загружает базовую модель, накладывает обученную печать, переводит ритуал в исполнение на GPU, формирует новый образ по заданному промпту и сохраняет результат для коммерческих сервисов стилизованной генерации.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON
- Schema:
  interface InputData {
  prompt: string;
  num_inference_steps?: number;
  }

### 3.2. Outputs (Выходы)

- Destination: File
- Format: JSON
- Success Criteria: Exit Code 0
- Schema:
  interface OutputResult {
  filePath: string;
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Подготовка ресурсов и заклинаний: инициализируем магическую мантию среды через импорт соответствующих модулей PyTorch, DiffusionPipeline и PEFT. Это основа для последующих действий.
2. Призыв Демиурга: формируем конвейер творения из предтренированной сущности model_id, устанавливаем формат данных в float16 и снимаем ограничения цензуры, чтобы позволить свободное выражение стиля.
3. Наложение Стилевой Печати: через seal_path загружаем обученную печать и «одеваем» её на сердце Демиурга, заменяя его внутреннюю нейронную скульптору на боевой костюм LoRA.
4. Трансляция Ритуала на Кристалл Маны: конвейер перемещаем на CUDA для ускорения вычислений и готовности к творению.
5. Ритуал Творения: задаём текстовое заклинание промпта и приводим его в движение, используя заданное число шагов очистки, чтобы породить новый образ по заданному стилю.
6. Завершение Творения: полученный образ извлекаем и подготавливаем к материализации.
7. Материализация Шедевра: сохраняем получившийся образ в файл astronout_in_my_style.png и объявляем о завершении ритуала.

### 4.2. Declarative Content (Для конфигураций и данных)

Ключевые данные конфигурации и артефакты, применяемые во время ритуала:

- базовая модель: модель_id = "CompVis/stable-diffusion-v1-4"
- режим вычислений: torch_dtype = float16
- безопасность: safety_checker = None
- путь к артефакту стилизации: seal_path = "../Quest_2/artist_seal"
- путь к файлу финального образа: save_path = "astronaut_in_my_style.png"
- промпт творения: prompt = "a portrait of an astronaut, in sks style"
- количество шагов инференса: num_inference_steps = 50
- окружение исполнения: pipe загружен и отправлен на устройство "cuda"

## 5. Structural Decomposition (Декомпозиция структуры)

- Основной артефакт: quest_7_3.py как последовательный ритуал из актов.
- Компоненты/классы:
  - DiffusionPipeline из модуля diffusers — конвейер творения.
  - PeftModel из PEFT — способ наложения обученной печати на сердце Демиурга.
- Основные этапы:
  - Инициализация конвейера через from_pretrained.
  - Замена pipe.unet на обернутый LoRA-смысл через PeftModel.from_pretrained.
  - Перемещение конвейера на CUDA.
  - Формирование промпта и запуск творения через вызов pipe(prompt, num_inference_steps=50).
  - Извлечение образа и сохранение в файл.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартная конфигурация GPU (CUDA-enabled) для ускорения inference.
- Concurrency: синхронный режим исполнения.
- Dependencies: PyTorch, diffusers, PEFT.
- Memory/Compute: рекомендуется VRAM достаточный для 16-битной загрузки и конвейера Diffusion.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ храните секреты в открытом виде (используйте .env для конфиденциалов).
- НЕ выводите сырые данные в консоль в продакшн-режиме.
- НЕ используйте синхронные сетевые вызовы в ядре цикла творения.
- НЕ оборачивайте конфигурационные файлы (.yaml, .json) в исполняемые скрипты.
- НЕ изменяйте версии или пути во время реконструкции артефактов.

## 7. Verification & Testing (Верификация)

Геркин-истории, описывающие нормальный ход и ошибку

Feature: Quest 7.3 Script Functionality
Scenario: Successful execution
Given рабочая среда с установленными PyTorch, diffusers и PEFT
When выполняется скрипт quest_7_3.py
Then создаётся файл изображения по пути astronaut_in_my_style.png

Feature: Quest 7.3 Script Functionality
Scenario: Missing LoRA seal path
Given отсутствует указанный путь seal_path
When выполняется скрипт quest_7_3.py
Then процесс завершается с ошибкой, Haос сообщает о невозможности наложить печать

ИССЛЕДУЕМЫЙ АРТЕФАКТ: quest_7_3.py

ИСТОКИ СВОЙСТВ (Inventory) в артефакте

- 🏰 Базовый артефакт ритуала: DiffusionPipeline рабочий механизм творения изображения с заданной моделью.
- 🛡️ Щит Печати: PeftModel.from_pretrained — наложение обученной печати на сердце Демиурга (UNet).
- 🏰 Сердце Демиурга: pipe.unet — ядро творца, заменяемое на стилизованную печать.
- 🏰 Каменная Табличка Ввода: prompt = a portrait of an astronaut in sks style — заклинание, задающее стиль и содержание.
- 🛡️ Камень Сохранения: save_path = astronaut_in_my_style.png — место, куда материализуется шедевр.
- 🏰 Кристалл Маны: CUDA-устройство, на котором исполняется конвейер и ускоряет расчет.
- 🏰 Эфир_Печати: seal_path = ../Quest_2/artist_seal — место, где хранится обученная печать.
- 🛡️ Скрижаль Контекста: model_id = "CompVis/stable-diffusion-v1-4" — источник духа, чья личность оживает подour стиль.
- 🏰 Точка доступа к эфирной ткани: num_inference_steps = 50 — порог чистки и формирования образа.

Придерживаясь стиля Прикладной Техномагии, этот артефакт демонстрирует, как модульности LoRA открывает новые горизонты коммерческих генеративных сервисов, позволяя наложить стиль на базовую модель и сохранить результат в виде уникального изображения.
