# quest_3_1.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: datasets, transformers
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот Ритуал демонстрирует практику призыва и использования артефактного зрителя для первой классификации изображений. Через призыв Всевидящего Ока (MobileNetV2) мы проводим анализ случайной иллюстрации из архива CIFAR-10, чтобы освоить концепцию переноса домена и уточнить цель ритуала: понять, как модель, обученная на одном наборе данных, может давать корректные выводы на другом.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
- Format: JSON | Text | Binary | Stream
- Schema:
  interface InputData {
  source: 'CLI Args' | 'STDIN' | 'API Request' | 'Kafka Topic' | 'Smart Contract Call';
  payload?: any;
  [key: string]: any;
  }

### 3.2. Outputs (Выходы)

- Destination: STDOUT | File | Database | API Response | Event Log
- Format: JSON | CSV | Text
- Success Criteria: Exit Code 0 | 200 OK | File Created
- Schema:
  interface OutputResult {
  destination: 'STDOUT' | 'File' | 'Database' | 'API Response' | 'Event Log';
  format: 'JSON' | 'CSV' | 'Text';
  success?: boolean;
  data?: any;
  [key: string]: any;
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Подготовка ритуалов: подключение к двум артефактам — библиотеке Архивов (datasets) и олтарю Прозрения (transformers).
2. Призыв Архива CIFAR-10: открыть архив потоковым заклинанием, выбрать раздел трапезы “train” и активировать режим стриминга, чтобы не призывать весь эфир сразу.
3. Извлечение образца: из потока взять первый образец; добыть изображение из ключа img и зафиксировать истинную сущность образа через перевод числового класса в словесное имя (int2str).
4. Призыв Духа Прозрения: создать амулет классификации изображений с именем задачи image-classification и именем призываемого духа model google/mobilenet_v2_1.0_224.
5. Анализ изображения: подать образец на вход амулету, который подгоняет размер, нормализует цвета и возвращает предсказания вместе с уверенностью.
6. Демонстрация результата: вывести вердикт Всевидящего Ока — список предсказаний с именами классов и их уверенность; при желании сравнить с истинной сутью образа, полученной на втором акте.

### 4.2. Declarative Content (Для конфигураций и данных)

- Конфигурации и данные не встроены в этот акт как параметры скрипта; данные берутся из внешних источников: CIFAR-10 через load_dataset с streaming, и модель через pipeline с заданной моделью MobileNetV2.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции/классы:
  - load_dataset (Архивариус архивов) — загрузчик и потоковый призыв данных
  - pipeline (Амулет Прозрения) — универсальный инструмент преобразования данных в предсказания
  - dataset — поток данных
  - sample — образец из потока
  - image_to_analyze — изображение к анализу
  - true_label — истинная сущность образа (для сравнения)
  - vision_spirit — инстанс амулета классификации
  - result — итоговые предсказания и уверенности

- Для конфигураций и данных: ключевые логические блоки отсутствуют как явные конфиги внутри артефакта; данные формируются из внешних источников CIFAR-10 и результативного вывода.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартный CPU; ориентирован на легковесного духа (MobileNetV2) для экономии ресурсов.
- Concurrency: синхронное выполнение без явной параллельности; поток данных реализуется через streaming, но шаги выполняются по очереди.
- Dependencies: datasets, transformers (и сопряжённые подсистемы для работы с моделями и данными).

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде; использовать окружение (.env) при работе с чувствительными данными.
- НЕ печатать чувствительную сырую информацию в консоль в продакшн-режиме.
- НЕ выполнять синхронные сетевые вызовы в главном цикле без надобности.
- НЕ упаковывать конфигурационные файлы (.yaml, .json) внутрь скриптов.
- НЕ менять версии библиотек или пути к артефактам во время реконструкции.

## 7. Verification & Testing (Верификация)

Геркин сценарии:

Feature: Script Functionality
Scenario: Successful execution
Given CIFAR-10 поток доступен и MobileNetV2 загружен
When образ подаётся на вход классификационному амулету
Then выводится вердикт Всевидящего Ока с именем класса и уверенностью

Scenario: Error due to dataset streaming failure
Given поток CIFAR-10 недоступен
When скрипт пытается призвать образец
Then процесс сообщает об ошибке призыва архива и завершается с ошибкой

## 8. Inventory (Инвентарь) — Описание ИМУЩЕСТВА

- 🏰 Архиватор Архивов: load_dataset из библиотеки datasets
- 🛡️ Амулет Прозрения: pipeline из библиотеки transformers
- 🏰 Архив CIFAR-10: источник данных, используемый в потоковом режиме
- 🏰 Образ Эфира: первый образец из потока, поле img
- 🏰 Истинная Сущность: int2str перевод числового класса в строковый ярлык
- 🛡️ Дух Всевидящий: модель-пассажер для классификации изображений через image-classification
- 🛡️ Вердикт Эфира: результат анализа — список предсказаний и их уверенность

Готовность: этот архивный ритуал quest_3_1.py готов для занесения в Великий Говорящий Свиток Кодекса, чтобы будущие мастера техномагии могли повторить и изучить путь переноса домена через искусство компьютерного зрения.
