# setup_video_palette.py Specification

1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: cv2 (OpenCV), numpy
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот скрипт — подготовительный ритуал для Квеста 8.2, цель которого программно сотворить учебник видео-датасета для обучения классификатора действий. Он создает две короткие видеорассказы, демонстрирующие разные действия, и размещает их в строгой скрижали папок для чистого, контролируемого набора данных.
   Instruction for AI: Это высокая цель. Зачем нужен код — чтобы автоматически генерировать визуальные примеры движений, пригодные для обучения модели распознавания действий в рамках Квеста 8.2.

Описание: Скрипт инициирует ритуал с помощью OpenCV, создает магический сад video_palette с двумя грядками action_A и action_B, берет артефакт magiçal_cat.png как актера и записывает две кинопленки: одну сдвига движений вправо, другую с эффектом увеличения. В финале появляется сообщение о завершении ритуала.

3. Interface Contract (Интерфейсный Контракт)

3.1. Inputs (Входы)

- Source: None (никаких внешних входов не требуется; скрипт запускается как автономный процесс)
- Format: N/A
- Schema:
  interface InputData {
  // Нет полей ввода; скрипт не принимает параметров
  }

  3.2. Outputs (Выходы)

- Destination: File
- Format: N/A (результаты — файлы видеопроектов и текстовый итог)
- Success Criteria: Файлы созданы и доступны
- Schema:
  interface OutputResult {
  status: 'success' | 'failure';
  createdFiles: string[]; // пути к сгенерированным видео
  message?: string;
  error?: string;
  }

4. Implementation Details (The Source DNA / Исходный Код)

4.1. Algorithmic Logic (Для исполняемого кода)

- Ритуал начинается с призыва Архивариуса os, взывающего к миру файлов и папок, и с призыва к Великану cv2, известному как Набор Кинематографиста.
- Прозрачный голос скрипта объявляет о начале ритуала и создаёт магический сад video_palette, внутри которого вырастают две грядки: action_A и action_B.
- Акторский дух активируется: загружаем артефакт magical_cat.png и измеряем его высоту и ширину, чтобы картина соответствовала размеру сцены.
- Секвенцией А создается первая кинопленка video1.mp4 в грядке action_A. В кадре каждого шага применяется матрица трансформации, которая смещает изображение вправо на i умноженное на 5 пикселей, и записывает 30 кадров с частотой 10 кадров в секунду.
- Секвенцией Б создается вторая кинопленка video2.mp4 в грядке action_B. Каждый кадр масштабируется на фактор 1 + i\*0.02, затем изображение центрируется и обрезается до исходного размера, после чего кадр записывается в файл. Также делается 30 кадров при той же частоте.
- По завершении процесса оба файла закрываются, и скрипт объявляет об успешном создании видео-палитры.

  4.2. Declarative Content (Для конфигураций и данных)
  Inventory (Имущество) в формате RPG-предметов:

- 🏰 video_palette directory (магический сад) — корневой контейнер артефактов ритуала.
- 🏰 action_A — первая грядка, в ней video1.mp4 — кинопленка для Движения вправо.
- 🏰 action_B — вторая грядка, в ней video2.mp4 — кинопленка для Увеличения.
- 🛡️ magical_cat.png — артефакт-актер, источник изображения для обоих фильмов.
- 🛡️ OpenCV cv2 — чародейский набор инструментов для обработки кадров.
- 🛡️ numpy — амулет матричных преобразований, необходим для сдвигов и изменений масштаба.
- 🏰 setup_video_palette.py — полный свиток ритуального кода, который вызывает эти чары.

5. Structural Decomposition (Декомпозиция структуры)

- Ритуал запуска: основная точка входа, инициализация окружения.
- Хранитель храмов: создание директорий video_palette и подгадание путей к файлам.
- Актёрская сущность: загрузка magical_cat.png и извлечение размеров кадра.
- Действие А: формирование аудиовизуального потока движения вправо через последовательность 30 трансформаций с горизонтальным сдвигом.
- Действие Б: формирование потока увеличения через 30 кадров масштабирования и центрированного обрезания.
- Завершение: закрытие файловых потоков и вывод завершающего сообщения.

6. System Context & Constraints (Системный контекст и Ограничения)

6.1 Technical Constraints

- Performance: Обычный процессор общего назначения; код рассчитан на стандартную нагрузку без специализированной графики.
- Concurrency: Синхронный, последовательный ритуал без параллелизма.
- Dependencies: OpenCV (cv2), NumPy; Python 3.x.

  6.2 Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде (использовать .env для секретов при необходимости).
- НЕ печатать сырые данные в консоль в продакшн-режиме.
- НЕ использовать синхронные сетевые вызовы в критическом цикле ритуала.
- НЕ оборачивать конфигурационные файлы (.yaml, .json) в скрипты.
- НЕ менять версии библиотек или путей во время реконструкции артефакта.

7. Verification & Testing (Верификация)

1. Gherkin сценарии (1–2 happy-path и 1 кейс ошибки)

Feature: Setup Video Palette Script

Scenario: Successful execution
Given magical_cat.png exists in the working directory
And video_palette/action_A and video_palette/action_B directories do not exist
When the script setup_video_palette.py is executed
Then video_palette/action_A/video1.mp4 and video_palette/action_B/video2.mp4 are created
And the console prints "Видео-палитра с двумя 'действиями' создана."

Scenario: Missing actor image
Given magical_cat.png is missing
When the script setup_video_palette.py is executed
Then the script terminates with a non-zero exit code and an error indicates missing image file

Scenario: Existing targets
Given video_palette/action_A/video1.mp4 and video_palette/action_B/video2.mp4 already exist
When the script runs
Then the script should either overwrite or skip with a clear status message and no crash

Итоговый артефакт: setup_video_palette.py
Эпическая задача выполнена — из хаоса файлов рождается упорядоченная палитра кадров, где каждый кадр — светящийся камень в стене знаний о действиях.
