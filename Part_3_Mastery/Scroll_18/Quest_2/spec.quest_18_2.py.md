# quest_18_2.py Specification

1. Meta Information

- Domain: Скриптинг
- Complexity: Medium
- Language: Python
- Frameworks: PyTorch, OpenCV (cv2), tqdm, Ultralytics Yolov5
- Context: Independent Artifact

2. Goal & Purpose (Легенда)
   Этот артефакт внедряет крохотную память в ритуал анализа видео: не просто видеть кадры, а считать число объектов заданного типа на каждом кадре. Мастерство позволяет не терять обнаружения при мимолетной пропаже объекта, повторяя предыдущий удачный отчет, чтобы получить более устойчивые данные в динамике видео.

3. Interface Contract (Интерфейсный Контракт)

3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema:
  interface InputData {
  videoPath: string;
  }

  3.2. Outputs (Выходы)

- Destination: File, STDOUT
- Format: JSON
- Success Criteria: Exit без критических ошибок, кадры сохранены в папке counting_results, итог подсчета выведен в консоль
- Schema:
  interface OutputResult {
  framesProcessed: number;
  outputFolder: string;
  finalCount: number;
  summary: string;
  }

4. Implementation Details (The Source DNA / Исходный Код)

4.1. Algorithmic Logic (Для исполняемого кода)

1. Приветствием инициализируем ритуал: подключаем помощников-артефактов PyTorch, OpenCV и tqdm, и объявляем, что Химера пробуждается для подсчета объектов.
2. Пробуждаем и настраиваем Химеру YOLOv5n: загружаем модель через torch.hub, снижаем порог уверенности до 0.10, и, если доступна Кристальная Пана манны (CUDA), переносим вычисления на устройство.
3. Подготавливаем место охоты: открываем видеопоток test_video.mp4, создаём папку counting_results, вычисляем общее число кадров и инициализируем память last_known_results как пустую.
4. Запускаем основной ритуал охоты с памятью: для каждого кадра читаем его, прогоняем через модель, затем применяем логику памяти:
   - если текущие результаты пусты и есть сохраненная память, рисуем по памяти;
   - иначе рисуем по свежему отчету и обновляем память, если есть что-то новое.
5. Подсчет и рисование: обнуляем локальный счетчик объектов для кадра, цель — остановки (stop sign). Перебираем обнаружения, увеличиваем счетчик, если найденный класс совпадает с целью, рисуем рамку и бирку над объектом.
6. Наносим итоговый счет на кадр красным текстом и сохраняем кадр в counting*results с именем count_frame*<n>.jpg. Увеличиваем счетчик сохранённых кадров.
7. Финализация: освобождаем видеопоток и выводим финальный итог: сколько кадров обработано и где они сохранены.

4.2. Declarative Content (Для конфигураций и данных)
Inventory превращено в образы RPG-предметов:

- 🏰 Чаша Памяти last_known_results: место хранения последнего успешного_detected набора.
- 🛡️ Дух YOLOv5n: модель детекции, с порогом уверенности 0.10.
- 🏰 Видеокнига test_video.mp4: исходный источник кадров.
- 🛡️ Свиток counting_results: папка, куда записываются обработанные кадры.
- 🏰 Руна Кадров frame_count: счетчик сохранённых кадров.
- 🛡️ Эфир Верификации config: порог детекции 0.10.
- 🏰 Кристалл CUDA (если доступен): ускорение вычислений.
- 🛡️ Свиток cap: источник видеопотока (cv2.VideoCapture).
- 🏰 Руна Маркировки glyphs: рисование рамок и подписей на кадре.
- 🛡️ Лор Names: сопоставление классов с именами, полученными из модели.

5. Structural Decomposition (Декомпозиция структуры)

- Функции и модули, как бы они существовали в коде:
  - initialize_model(): загрузка Yolov5n, настройка порога, перенос на CUDA при наличии.
  - open_video_source(): открытие test_video.mp4 и создание counting_results.
  - compute_total_frames(): вычисление общего числа кадров.
  - memory_pool(): управление last_known_results и логикой возвращения памяти.
  - process_frame(frame): выполнение детекции для кадра и выбор между свежими и памятованными результатами.
  - annotate_frame(frame, detections): прорисовка рамок и подписей на кадре.
  - save_frame(frame, index): сохранение обработанного кадра в файловую систему.
  - finalize(): освобождение ресурсов и вывод финального сообщения.

- Классы: В текущем виде артефакт не содержит классов; основная логика реализована через функции и последовательность актов.

6. System Context & Constraints (Системный контекст и Ограничения)

6.1. Technical Constraints

- Performance: Стандартный CPU, при наличии CUDA возможна ускорение.
- Concurrency: Синхронная обработка кадров (один поток).
- Dependencies: PyTorch, OpenCV (cv2), tqdm, Ultralytics Yolov5.

  6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (используйте .env для ключей).
- DO NOT печатать реальные данные в консоль в продакшн-режиме.
- DO NOT выполнять синхронные сетевые вызовы в главном цикле.
- DO NOT оборачивать конфиги (.yaml, .json) в скрипты.
- DO NOT менять версии библиотек или пути во время реконструкции.

7. Verification & Testing (Верификация)

Геркин-сценарии:

Feature: Script Functionality
Scenario: Successful execution
Given valid video file test_video.mp4 is present and readable
When the script processes frames, detects stop signs with threshold 0.10, draws bounding boxes, and updates memory
Then counting_results directory contains processed frames and the console outputs a summary like "Stop signs found: N"

Scenario: Video file missing
Given test_video.mp4 is missing or unreadable
When the script attempts to read frames
Then it terminates gracefully with zero frames processed and a final summary indicating zero detections

Context
Independent Artifact

Примечание по Atmosphere (Lore): в каждом разделе铭 слаготворения и названий подчеркиваются темы ритуала, памяти и магии детекции, чтобы связать технические шаги с волшебной тканью квеста.
