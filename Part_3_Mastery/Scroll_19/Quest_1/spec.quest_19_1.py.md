# quest_19_1.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: High
- Language: Python
- Frameworks: Librosa, NumPy, PyTorch (torch), datasets (Hugging Face), SciPy (wav write), transformers (GPT2, Whisper, pipeline)
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот файл реализует _Ритуал_ по созданию конвейера големов для обработки аудио и генерации текста. В его сердце стоят три голема: Писец, Толкователь и Оракул, которые последовательно превращают звук в текст, определяют намерение и формируют ответ. Сквозной замысел — показать создание сложных AI-пайплайнов и управлять жизненным циклом моделей на едином GPU, демонстрируя работу с несколькими моделями и освобождение ресурсов после каждого шага.  
Instruction for AI: Этот модуль объясняет, зачем он нужен и как он достигает цели — преобразовать аудиоввод в завершённый текстовый ответ через цепочку взаимосвязанных этапов.

Кратко: собирается аудио из Hugging Face, транскрибируется Гolem-Writer, определяется намерение, генерируется ответ, при этом управляется память GPU и жизненный цикл моделей.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

```typescript
export interface InputData {
  source: "CLI Args" | "STDIN" | "API Request" | "Kafka Topic" | "Smart Contract Call";
  format: "JSON" | "Text" | "Binary" | "Stream";
  payload?: any;
  filename?: string;
  sample_rate?: number;
  metadata?: {
    requested_by?: string;
    timestamp?: string;
  };
}
```

### 3.2. Outputs (Выходы)

```typescript
export interface OutputResult {
  destination: "STDOUT" | "File" | "Database" | "API Response" | "Event Log";
  format: "JSON" | "CSV" | "Text";
  successCode?: number;
  data?: {
    final_response?: string;
    transcription?: string;
    intent?: string;
  };
  logs?: string[];
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Шаг подготовки: определяем устройство выполнения как CUDA-устройство, если доступно, иначе — CPU. Сообщаем о выбранном устройстве.

- Ритуал 1 — Извлечение Эфирного Свитка (Fetch Audio):
  - Обращаемся к Великой Библиотеке Hugging Face через load_dataset к набору данных "superb", "ks" в разделе "validation" с доверительными правами на удалённый код.
  - Набор становится доступен локально; выбираем один образец по индексу (10-й элемент) и извлекаем аудио-материю и частоту дискретизации.
  - Если родная частота не равна 16000 Гц, выполняем гармонизацию (ресэмплинг) до 16000 Гц через соответствующее преобразование.
  - Эфир преобразуется в формат int16 и сохраняется на диск как WAV-файл. Возвращается имя файла.

- Ритуал 2 — Призыв Голема-Писца (Whisper):
  - Загружаем процессор Whisper и модель WhisperTiny.
  - Загружаем локальный аудиофайл и приводим его к частоте 16000 Гц.
  - Преобразуем аудио в тензоры и подаем на Голема для генерации рунического вывода (токенов).
  - Декодируем токены в текстовую транскрипцию.
  - Удаляем модель и процессор, очищаем VRAM и сообщаем об изгнании.

- Ритуал 3 — Призыв Голема-Толкователя (Zero-shot Classifier):
  - Создаём конвейер нулевого схода классификации и запускаем его на нужном устройстве.
  - Задаём кандидатские метки намерений: «узнать погоду», «включить музыку», «рассказать шутку», «неизвестная команда».
  - Получаем наилучшее намерение и публикуем результат; очищаем память.

- Ритуал 4 — Призыв Голема-Оракула (GPT-2):
  - Загружаем DistilGPT-2 и его токенизатор, настраиваем pad_token.
  - Формируем затравку (prompt) в зависимости от определённого намерения:
    - узнать погоду → затравка о погоде
    - включить музыку → затравка о воспроизведении музыки
    - рассказать шутку → затравка для шутки
    - прочие случаи → общая затравка-пустышка
  - Генерируем продолжение, ограничиваем длину и избегаем повторов.
  - Декодируем в финальный текст, удаляем модель и токенизатор, очищаем VRAM.

- Главная точка входа: если файл запускается напрямую, выполняются шаги выше по цепочке: извлечение аудио, транскрибация, определение намерения, генерация ответа и финальный вывод.

Весь процесс реализован как понятная цепочка из четырех последовательных ритуалов, которые в совокупности образуют живую Эфир‑цепочку, где каждый элемент влияет на следующий и обеспечивает устойчивую работу модели на одном GPU, контролируя ресурсы и освобождая их по завершении.

### 4.2. Declarative Content (Для конфигураций и данных)

- Идея цикла жизненного Ритуала: подключаемые гримуары и данные, которые управляют стадиями обработки.
- Источник Эфира: Hugging Face Hub dataset "superb", "ks" (audio samples), с элементом trust_remote_code.
- Вещи-артефакты:
  - WhisperTiny для Писца (STT)
  - Zero-shot classifier модель (valhalla/distilbart-mnli-12-3)
  - Distilgpt2 для Оракула
- Контроль маны: вызов torch.cuda.empty_cache() после каждого голема.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции:
  - fetch_audio_from_hub(filename: string = "command_from_hub.wav", sample_rate: number = 16000): string
  - transcribe_audio(audio_path: string): string
  - classify_intent(text: string): string
  - generate_response(intent: string): string
- Главная стезя: if **name** == "**main**": запускает цепочку ритуалов и печатает итоговый ответ.
- Взаимодействие с данными: аудио → текст → намерение → ответ.
- Логика управления ресурсами: удаление моделей, очистка VRAM.

Inventory (Declarative Content) — Инвентарь РПГ-артефактов:

- 🏰 Голем-Писец (Whisper) — транскрибация аудио в текст через WhisperTiny и WhsisperProcessor.
- 🏰 Голем-Толкователь (Classifier) — нулевой сход классификации (zero-shot) через DistilBART-MNLI.
- 🏰 Голем-Оракул (GPT-2) — генерация текста через DistilGPT-2.
- 🛡️ Кристалл Маны — управление VRAM, очистка после каждого цикла.
- 🗺️ Сводки Великой Библиотеки — Hugging Face Hub и доступ к набору данных.
- ⚔️ Бестиарий зависимостей — librosa, numpy, torch, datasets, scipy, transformers.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Определение устройства выполняется в начале; при наличии CUDA используется GPU ('cuda'), иначе CPU.
- Concurrency: Выполнение синхронное и последовательное (нет параллельного запуска этапов в одном потоке).
- Dependencies: Необходимы библиотеки librosa, numpy, torch, datasets, scipy, transformers; модели WhisperTiny, distilgpt2 и distilbart-мnli доступны через HuggingFace.

### 6.2. Prohibited Actions (Negative Constraints)

- Не хранить секреты в открытом виде (использовать .env при необходимости).
- Не выводить в продакшн необработанные данные на консоль.
- Не использовать синхронные сетевые вызовы в главном цикле (если бы были вызовы сети, держать их асинхронно).
- Не оборачивать конфигурационные файлы (.yaml, .json) в скрипты.
- Не изменять версии или пути во время реконструкции артефакта.

## 7. Verification & Testing (Верификация)

Геркин-сценарии:
Feature: Quest 19_1 Script Functionality
Scenario: Successful execution
Given устройство CUDA доступно и Hugging Face набор доступен
When полный ритуал выполняется от начала до конца
Then финальный ответ выводится на STDOUT

Scenario: Empty transcription
Given транскрипция возвращает пустой текст
When главный цикл обрабатывает отсутствие аудио‑информации
Then намерение по умолчанию становится "неизвестная команда" и формируется ответ

Итог: артефакт quest*19_1.py демонстрирует целостную, управляемую цепочку — от сборки *Эфира\_ до вывода финального текста, с аккуратной очисткой ресурсов и понятной логикой обработки, сопровождаемой тестами для счастья пути и обработки ошибок.
