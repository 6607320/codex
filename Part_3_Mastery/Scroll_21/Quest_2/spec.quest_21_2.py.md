# quest_21_2.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: Medium
- Language: Python
- Frameworks: Pandas, PyTorch (torch, torch.nn), SciPy (cosine), NumPy
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот файл демонстрирует эпохальное применение эмбеддингов для решений двух царств домашней рекламы артефактов: поиск похожих предметов и персональные предсказания для конкретного искателя. Схема ритуала иллюстрирует, как бедность данных и холодный старт преследуют реальный мир, и как искусственные ауры можно гибко обучать и использовать для примера рекомендаций.

Why: показать последовательность действий от подготовки данных, через обучение голема-картографа, до извлечения полезных предсказаний и поисков артефактной близости.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

Source: STDIN
Format: JSON
Schema:
interface InputData {
user: string;
item: string;
rating: number;
}

### 3.2. Outputs (Выходы)

Destination: STDOUT
Format: Text
Success Criteria: Exit Code 0
Schema:
interface OutputResult {
success: boolean;
message?: string;
topItems?: string[];
recommendations?: string[];
}

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Инициализация среды магии: выбрать устройство вычислений DEVICE так, чтобы CUDA использовался, если доступна поддержкаgraphics-мануала, иначе CPU.
- Чертеж Голема-Картографа: определить нейронную сеть RecommendationNet с двумя embeddings таблицами: user_embeddings и item_embeddings; размер эмбеддингов задаётся параметром embedding_dim (по умолчанию 5).
- Подготовка данных: создать таблицу данных с полями user, item и rating; построить словари маппинга имен в индексы (user_to_idx, item_to_idx) и обратно (idx_to_user, idx_to_item); добавить столбцы user_id и item_id.
- Обучение: использовать MSELoss и оптимизатор Adam; подать на вход пары (user_ids, item_ids) и их рейтинги; обучать 100 эпох, накапливая градиенты и обновляя веса.
- Извлечение эм aura: после обучения взять веса embeddings и превратить в массивы numpy для последующего анализа.
- Поиск похожих артефактов: для заданного имени артефакта искать соседей по косинусной близости между эмедиентами; вывести топ-N соседей.
- Персональные рекомендации: для заданного пользователя просмотреть все артефакты, которых пользователь ещё не видел, предсказать рейтинг через модель и отсортировать по убыванию; вывести топ-N рекомендаций.
- Завершение: вывести сообщение о готовности голема и наличии карты сокровищ; положить модель в режим eval для фиксации весов.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory of the ritual and its data substances:

- 🏰 df: магическая таблица данных с колонками user, item, rating, user_id, item_id, созданная из базового набора искателей и артефактов.
- 🛡️ user_to_idx, idx_to_user: маппинги имен искателей к числовым идентификаторам и обратно.
- 🛡️ item_to_idx, idx_to_item: маппинги названий артефактов к числовым идентификаторам и обратно.
- 🧭 DEVICE: устройство вычислений, выбранное как "cuda" если доступно, иначе "cpu".
- 🗺️ RecommendationNet: головной чертог голема-картографа с двумя Embedding слоями.
- 🧭 user_embeddings, item_embeddings: обученные ауры искателей и артефактов после тренировки.
- 🗒️ df[ user_id, item_id, rating ]: числовые столбцы для обучения, полученные из ментального свитка данных.
- 🔮 find_similar_items: ритуал для поиска похожих артефактов по косинусной схожести между эмбеддингами предметов.
- 🧭 recommend_for_user: ритуал персональных рекомендаций для конкретного искателя.
- ⚔️ loss_function (MSELoss) и optimizer (Adam): наставник, который регулирует ауры во время Великих Этапов обучения.
- ⚗️ Тензоры и массивы: user_ids, item_ids, ratings, embeddings — артефакты вычислений, перенесённые на устройство и затем конвертированные в CPU для анализа.

Важно: Имена переменных и библиотек сохранены как в оригинальном артефакте — pd, torch, nn, cosine, np, DEVICE, RecommendationNet, df, user_to_idx, item_to_idx, а также функции find_similar_items и recommend_for_user.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции/Классы:
  - Class RecommendationNet: нейронная сеть с embedding-таблицами для пользователей и предметов.
  - Function find_similar_items(item_name, item_embeddings, item_to_idx, idx_to_item, top_n=3): поиск соседей по косинусной близости.
  - Function recommend_for_user(user_name, user_to_idx, model, df, idx_to_item, top_n=3): генерация топ-N рекомендаций для пользователя.
  - Main блок if **name** == "**main**": настройка данных, обучение модели, извлечение эм aura и вызовы ритуалов.
- Конфигурации/Данные:
  - data: словарь с user, item, rating; превращается в DataFrame df.
  - user_to_idx, idx_to_user, item_to_idx, idx_to_item: словари индексов.
  - device, embedding_dim, loss_function, optimizer: параметры и средства тренировки.
- Взаимодействия между компонентами:
  - df -> mappings -> model -> embeddings -> выводы find_similar_items и recommend_for_user.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартное CPU/плавающее CUDA, если доступно; обучающий цикл настроен на 100 эпох.
- Concurrency: одиночный поток, синхронная арифметика вычислений, без параллельных задач.
- Dependencies: pandas, torch (и torch.nn), scipy (cosine), numpy.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде; используйте .env для конфиденциальной информации.
- DO NOT выводить сырые данные в консоль в продуктивной среде.
- DO NOT использовать синхронные сетевые вызовы в основном цикле.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) в скрипты.
- DO NOT менять версии библиотек или пути реконструкции.

## 7. Verification & Testing (Верификация)

Геркин-сценарии:
Feature: Script Functionality
Scenario: Successful execution
Given все зависимости установлены и данные доступны в кодовой форме
When скрипт quest_21_2.py выполняется
Then выводится сообщение "Голем-Картограф обучен и готов к работе."
And выводятся топ-N похожих артефактов для "Свиток Огненного Шара"
And выводятся топ-N персональных рекомендаций для пользователя "Ария"

Scenario: Error due to пустой набор данных
Given набор данных пуст (нет записей user/item/rating)
When скрипт quest_21_2.py запускается
Then происходит ошибка обучения (например, пустой датасет) и сценарий завершается с неуспехом

ИНСТРУКЦИИ ПО РАЗДЕЛАМ выполнены.
