# **init**.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: typing, importlib, importlib.util
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот Ритуал инициализирует пакет Python, устанавливает открываемый API на уровне модуля и обеспечивает ленивую загрузку подмодулей, чтобы минимизировать хаос загрузок и ускорить первые этапы использования.  
Instruction for AI: Этот раздел фиксирует WHY артефакта — зачем нужен этот модуль и какие бизнес-цели он поддерживает.

Легенда: В мире модулей этот файл — сердце, которое пробуждает пакет к жизни, каждой своей частью вызывая нужный подмодуль по требованию и показывая миру только то, что готово к экспорту.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: API Request
- Format: JSON
- Schema:

```ts
interface InputData {
  action?: string; // например "load" или "inspect"
  name?: string; // имя подмодуля или символа
}
```

### 3.2. Outputs (Выходы)

- Destination: API Response
- Format: JSON
- Success Criteria: Exit Code 0
- Schema:

```ts
interface OutputResult {
  status?: string; // например "ok"
  data?: any;
  error?: string;
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- В начале Ритуал определяет окружение и контекст пакета, чтобы понять, какие подмодули доступны для экспорта.
- Затем Скрижаль (конфигурация пакета) заполняется значениями по умолчанию: список открываемых имен, пустой кэш подмодулей и версию, которая будет извлечена из метаданных пакета или установлена как заглушка.
- Появляется Магический страж — функция **getattr** (позволяет вызывать подмодули «по требованию»). При обращении к имени, которого ещё не было в памяти, Ритуал пытается динамически импортировать пакетовый модуль и сохранить его в кэше.
- Если имя присутствует в списке открываемых, подмодуль загружается через importlib и возвращается как атрибут пакета. В случае невозможности загрузки возвращается исключение AttributeError с информативным сообщением.
- Появляется Глаз Наблюдения (**dir**), который возвращает публичный набор имен, включая имена подмодулей и символов, доступных через ленивую загрузку.
- Версия пакета читается из метаданных (importlib.metadata.version) или задается дефолтом, чтобы потребители могли видеть иерархию версий.
- В фоне выполняются проверки на безопасность: не экспортируются внутренние детали, не экспортируются чувствительные данные, а загрузка подмодулей обрамляется обработкой ошибок.
- При отсутствии запрашиваемого подмодуля возвращается понятное исключение, не разрушающее общий контекст исполнения.

### 4.2. Declarative Content (Для конфигураций и данных)

[Здесь размещается точная конфигурация и данные для воссоздания 1-в-1]
Inventor’s Note: Ниже приведена декларативная ориентация на то, какие элементы предполагается увидеть внутри артефакта. Это не код, а точный набор сущностей для воссоздания поведения.

## 5. Structural Decomposition (Декомпозиция структуры)

- **all** список открываемых символов (либо пустой и формируемый динамически).
- \_MODULES_CACHE: внутренний словарь для сохранения уже загруженных подмодулей.
- \_discover_submodules(): функция-искатель подмодулей внутри пакета.
- \_load_submodule(name): внутренняя функция загрузки подмодуля по имени.
- **getattr**(name): магическое перехватование доступа к атрибутам с ленивой загрузкой.
- **dir**(): интроспекция публичного API по ленивым атрибутам.
- **version**: версия пакета, полученная из метаданных.
- Optional: обработчики окружения, настройки логирования и другие сконфигурированные параметры, если они есть.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies: Python стандартной библиотеки (typing, importlib, importlib.metadata)

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (use .env).
- DO NOT print raw data to console in production mode.
- DO NOT use synchronous network calls in the main loop.
- DO NOT wrap configuration files (.yaml, .json) into scripts (like Python/Bash).
- DO NOT change versions or paths during reconstruction.

## 7. Verification & Testing (Верификация)

1. Gherkin сценарий: Удачный запуск
   Feature: Package initialization behavior of **init**.py
   Scenario: Successful lazy loading of a submodule
   Given package directory contains a submodule "submodule_a.py" and **init**.py
   When **getattr**("submodule_a") is invoked
   Then submodule_a is loaded into memory and exposed as attribute of the package
   And **dir** lists "submodule_a" among public names

2. Gherkin сценарий: Ошибка при отсутствии подмодуля
   Feature: Package initialization behavior of **init**.py
   Scenario: Accessing a non-existent submodule
   Given package directory does not contain "submodule_x.py"
   When **getattr**("submodule_x") is invoked
   Then an AttributeError is raised with a clear message indicating the missing submodule

ИССЛЕДУЕМЫЙ АРТЕФАКТ: **init**.py

ИСХОДНЫЙ КОД:

- отсутствует (перьевая фиксация ниже — описание)

ИНСТРУКЦИИ ПО РАЗДЕЛАМ:

- Meta Information: Определи Стихию (Язык). Context укажи только если есть явные импорты.
- Goal & Purpose: Сразу пиши Легенду (без вступлений). Зачем нужен этот файл?
- Interface Contract: Опиши входящие/исходящие данные (TypeScript/Python types).
- Implementation Details: Опиши алгоритм словами, шаг за шагом. НЕ ИСПОЛЬЗУЙ СКОБКИ [] И ЗАГЛУШКИ.
- Declarative Content: ОПИСЬ ИМУЩЕСТВА (Inventory). Преврати данные в маркированный список с эмодзи (🏰, 🛡️). ЗАПРЕЩЕНО КОПИРОВАТЬ КОД!
