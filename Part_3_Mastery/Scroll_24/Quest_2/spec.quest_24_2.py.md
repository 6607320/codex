# quest_24_2.py Specification

## 1. Meta Information

- **Domain:** Scripting
- **Complexity:** Low
- **Language:** Python
- **Frameworks:** PyTorch, PyInstaller
- **Context:** ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

- **Context for Creator:** Этот модуль демонстрирует бизнес-задачу переноса простого Python-ритуала в автономный артефакт. Он иллюстрирует, как за счёт одного ритуала PyInstaller можно сплавить код, зависимости и духа интерпретатора в единое исполняемое создание, пригодное к жизни на любой машине.
- **Instruction for AI:** This section provides high-level intent. Use it to understand the "WHY" of the code.
- Легенда: это ковалка, где скрипт превращается в самодостаточного голема-исполняемца, а дух PyTorch шепчет своё имя внутри него.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args
- **Format:** Text
- **Schema:**
  - InputData: пустой набор полей (нет обязательных входов; ритуал запускается как обычный Python-скрипт)

### 3.2. Outputs (Выходы)

- **Destination:** STDOUT
- **Format:** Text
- **Success Criteria:** Нормальное завершение после нажатия Enter
- **Schema:**
  - OutputResult: объекты/поля
    - messages: массив строк, выводимых в процессе ritual
    - exitCode?: целочисленный код завершения (если применимо)

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Ритуал начинается, когда этот файл запущен как главная сущность.
- Затем спускается приветствие, формулирующее образ голема-артефакта: он говорит о своей самодостаточности и жизни благодаря мастерству кузнеца.
- Дух PyTorch объявляет себя внутри артефакта и сообщает версию установленной библиотеки PyTorch.
- Магический клик по стихии подтверждается просьбой пользователя нажать Enter, чтобы завершить ритуал — это своеобразное плавление кристаллов воедино.
- Вся логика упаковывается в единое исполняемое создание и готовится к жизни на любой машине посредством внешнего ритуала PyInstaller.

### 4.2. Declarative Content (Для конфигураций и данных)

- Инвентарь артефакта (Эфир и Скрижали):
  - 🏰 Эфир PyTorch: версия pyTorch внутри артефакта (torch.**version**) и сам дух библиотеки
  - 🛡️ Скрижаль PyInstaller: командный ритуал --onefile, который сплавляет скрипт, зависимости и интерпретатор в единое исполняемое существо
  - 🗺️ Карта файла: quest_24_2.py — источник и «сердце» артефакта
  - ✨ Дух интерпретатора: Python, как основа магии, в которую «погружен» артефакт
  - 🔮 Всплеск взаимодействия: приветствие и запрос на Enter, чтобы освободить духы и завершить ритуал

> Визуализированная сущность артефакта: простая текстовая демонстрация, резонирующая с заявленной целью — показать, как переносимый исполняемый файл может существовать независимо от окружения.

## 5. Structural Decomposition (Декомпозиция структуры)

- Нет пользовательских функций или классов в коде. Единственный структурный элемент — главный блок, который исполняется при условии if **name** == "**main**".
- Основной фрагмент выполняет следующие роли:
  - Инициация ритуала и вывод приветствия
  - Объявление присутствия духа PyTorch и его версии
  - Ожидание активации пользователя (нажатие Enter) для завершения
- Вся логика сконцентрирована в одном модульном сегменте: файл quest_24_2.py — это источник и хранитель ритуала.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Стандартный CPU; артефакт не требует ускорения графикой или профильной памяти.
- **Concurrency:** Синхронный режим выполнения; ритуал выполняется последовательно.
- **Dependencies:** Python, PyTorch (torch), PyInstaller для упаковки в единый исполняемый файл.
- Контекст подразумевает, что артефакт должен жить на машинах без установленного Python, благодаря упаковке.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде; используйте окружение (.env) для секретов, если они нужны.
- DO NOT выводить сырые данные в консоль в продакшн-режиме без необходимости.
- DO NOT выполнять синхронные сетевые вызовы в основной петле без надобности.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) прямо в скрипты (как часть кода).
- DO NOT менять версии или пути во время реконструкции артефакта.

## 7. Verification & Testing (Верификация)

Геркин-сценарии для описания удачного сценария и одного ошибочного случая.

```gherkin
Feature: Quest 24.2 Script Functionality
  Scenario: Successful execution
    Given quest_24_2.py is run as the main program
    When the script prints the greeting and the PyTorch version
    And the user presses Enter
    Then the program terminates gracefully

  Scenario: Missing PyTorch dependency (error case)
    Given quest_24_2.py is executed in an environment without PyTorch available
    When importing torch fails
    Then the program exits with an error message and a non-zero exit code
```

ИССЛЕДУЕМЫЙ АРТЕФАКТ: quest_24_2.py

ИСХОДНЫЙ КОД (для контекста, без копирования кода):
"""
Квест 24.2: Ковка "Самодостаточного Артефакта".

Этот пергамент — кульминация "Свитка Печати Мастера". Его главная цель
(МАКРО-контекст) — создать "Кристаллического Голема": артефакт, который
несет всю свою магию внутри себя.

Сам скрипт (`quest_24_2.py`) предельно прост. Он служит "душой" для
могущественного "духа-кузнеца" `PyInstaller`. Ритуал заключается в
исполнении команды `pyinstaller --onefile`, которая "сплавляет" этот
скрипт, все его гримуары-зависимости и самого духа-интерпретатора Python
в единый, монолитный, исполняемый файл. Этот квест доказывает мастерство
в создании полностью переносимых приложений, которые могут "ожить" на
любой машине, даже там, где магия Python никогда не была установлена.
"""

# Призываем 'torch' - наш Источник Маны.

import torch

# Главное заклинание нашего самодостаточного артефакта.

if **name** == "**main**": # Приветствие от голема.
print("🔮 Я - Самодостаточный Артефакт! Мой Мастер вдохнул в меня жизнь.") # Доказательство того, что внутри меня живет дух PyTorch.
print(f"Дух PyTorch внутри меня имеет версию: {torch.**version**}") # Просим пользователя нажать Enter, чтобы ритуал не завершился мгновенно.
input("\nНажмите Enter, чтобы отпустить меня на покой...")
"""
