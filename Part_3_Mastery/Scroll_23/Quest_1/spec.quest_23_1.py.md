# quest_23_1.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: Medium
- Language: Python
- Frameworks: Gradio, PyTorch, Transformers
- Context: ../AGENTS.md

## 2. Goal & Purpose

**Context for Creator:** Этот модуль — магический свиток, который оборачивает дообученного голема-казателя в простой веб-интерфейс. Он демонстрирует быструю витринизацию AI-моделей: дать текстовую затравку и получить продолжение, не требуя веб‑разработки.  
**Instruction for AI:** Это существо-обертка подчеркивает WHY: показать, как можно быстро и наглядно презентовать модель генерации текста через интерактивный интерфейс, сохранив простоту развёртывания и повторного использования.

Описание: Этот файл создаёт магический зеркальный интерфейс с помощью Gradio, который связывает загрузку дообученного голема-казателя из заданного пути, оборачивает логику генерации в единую функцию и запускает веб‑интерфейс для интерактивной демонстрации.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: API Request
- Format: Text
- Schema:
  - interface InputData {
    prompt_text: string;
    max_new_tokens?: number;
    }

### 3.2. Outputs (Выходы)

- Destination: API Response
- Format: Text
- Success Criteria: 200 OK
- Schema:
  - interface OutputResult {
    generated_text: string;
    }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Определяется устройство выполнения: CUDA доступно — выбрать CUDA, иначе — CPU.
- Задаётся путь к телу голема: MODEL_PATH = "./results/checkpoint-250".
- Загружаются сосуды языка: токенизатор и сам голем (GPT2LMHeadModel) из MODEL_PATH и отправляются на выбранное устройство. Голем переводят в режим генерации (evaluation).
- Описывается единое заклинание conjure_story, которое принимает затравку prompt_text и максимальное число новых токенов max_new_tokens. При вызове:
  - Затравка печатается в хроникерах порта.
  - Текст преобразуется в токены и отправляется на освящённый алтарь (устройство).
  - Голем генерирует продолжение с ограничением max_new_tokens и без повторения фрагментов (no_repeat_ngram_size=2).
  - Рунопись декодируется обратно в человекочитаемый текст и возвращается в интерфейс.
  - Результат печатается в хрониках и возвращается пользователю через Gradio.
- В точке входа (**main**) создаётся Gradio-интерфейс, задаются поля ввода и вывода:
  - inputs: текстовое поле (Multiline) с подписью и подсказкой.
  - outputs: текстовое поле для сгенерированной истории.
  - title и description описывают суть зеркала и упоминание источника модели.
  - интерфейс запускается локальным веб-сервером для доступа через браузер.

### 4.2. Declarative Content (Для конфигураций и данных)

- Глобальные константы:
  - DEVICE: "cuda" если доступна CUDA, иначе "cpu"
  - MODEL_PATH: "./results/checkpoint-250"
- Ресурсы:
  - tokenizer: GPT2Tokenizer.from_pretrained(MODEL_PATH)
  - model: GPT2LMHeadModel.from_pretrained(MODEL_PATH).to(DEVICE)
- Основная функция:
  - conjure_story(prompt_text, max_new_tokens)
- Главная точка входа:
  - if **name** == "**main**": запуск Gradio интерфейса demo

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции
  - conjure_story: инкапсулирует всю логику конструирования и генерации истории.
- Глобальные объекты/ресурсы
  - DEVICE, MODEL_PATH
  - tokenizer, model
  - demo: Gradio Interface, связывающий conjure_story с UI
- Основной поток
  - Блок инициализации, затем запуск интерфейса при входе в точку main.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Демонстрационный уровень производительности; ускорение лучше при наличии GPU (CUDA). Модель загружается из локального пути и выполняется в режиме генерации.
- Concurrency: Интерфейс Gradio обрабатывает вызовы асинхронно с точки зрения UI, но вызов конъюрации истории сам по себе синхронный в рамках одного запроса.
- Dependencies: gradio, torch, transformers; путь к модели ./results/checkpoint-250.
- Memory/Runtime: Потребуются память GPU/CPU в зависимости от устройства и размера загруженной модели.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде в коде; используйте .env для секретов.
- НЕ выводить в консоль сырые данные в продакшн-режиме без фильтрации.
- НЕ выполнять синхронные сетевые вызовы в основном цикле интерфейса без контроля конкуренции.
- НЕ оборачивать файлы конфигураций (.yaml, .json) прямо в скрипты как часть логики.
- НЕ менять версии библиотек или пути к моделям во время реконструкции Artefact.

## 7. Verification & Testing (Верификация)

Геркин сценарии

Feature: Script Functionality
Scenario: Successful execution
Given инфраструктура запущена с доступной моделью и Gradio интерфейсом
When пользователь вводит допустимую затравку и запускает генерацию
Then возвращается корректное продолжение истории и UI отображает Generated_text

Scenario: Missing prompt input
Given интерфейс открыт
When пользователь отправляет пустую затравку
Then система должна обработать запрос gracefully и отобразить соответствующее сообщение или пустой вывод без срыва работы

Инвентарь проекта (Inventory)

- 🏰 DEVICE: Кристалл Маны переходит в режим CUDA, если доступен, иначе CPU
- 🏰 MODEL_PATH: "./results/checkpoint-250"
- 🏰 tokenizer: Гримуар Толмача, загруженный из MODEL_PATH
- 🏰 model: Голем-Сказитель, загруженный из MODEL_PATH
- 🛡️ conjure_story: Единственный маяк заклинания генерации
- 🛡️ demo: Магический интерфейс Gradio, позволяющий ввести затравку и увидеть историю
- 🛡️ prompt_text: Затравка, вводимая пользователем
- 🛡️ generated_text: Сгенерированное продолжение истории
- 🏰 MAIN: точка входа и священный триггер запуска интерфейса

Примечание: Этот артефакт сохраняет наборы имен и путей как есть в исходном коде, чтобы сохранить технологическую идентичность и репродуцируемость. Вся магия оформлена так, чтобы легко перенести концепцию на другие модели и интерфейсы, сохраняя дух быстрого прототипирования и наглядности взаимодействия человека с искусственным разумом.
