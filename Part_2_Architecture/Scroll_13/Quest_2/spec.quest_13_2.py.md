# quest_13_2.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: High
- Language: Python
- Frameworks: PyTorch, torchvision, tqdm
- Context: PyTorch stack: torch, torch.nn, torch.nn.functional as F, torch.optim, torchvision.datasets, torchvision.transforms, tqdm

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот Ритуал перегонки знаний предназначен для сжатия мудрости большого Голема-Магистра в более быстрого Подмастерья. Задача — обучить крупного учителя на MNIST, а затем использовать его “мудрый шепот” как дополнительный учебный материал для маленького ученика, чтобы тот учился эффективнее и быстрее обретал интуицию учителя.

Instruction for AI: Этот раздел передаёт WHY кода — зачем нужна перегонка знаний и как она структурно выстроена: подготовка учителя, затем настройка ученика через сочетание потерь дистилляции и обычной кросс-энтропии.

Суть: реализовать двухактный Ритуал перегонки знаний, где учитель обучается один проход эпохой, а затем ученик обучается через сочетание смягчённых предсказаний учителя и реальных ответов.

Описание на русском языке: Данный артефакт реализует конвейер дистилляции: подготавливаются данные и модели, обучается большой учитель, затем ученик учится по суставу сигналов учителя и фактических ответов, чтобы впитать интуицию мастера и повысить эффективность обучения на MNIST.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: None (скрипт запускается как автономный модуль без явного CLI/REST ввода)
- Format: None (нет внешних данных на вход в явном виде)
- Schema:
  interface InputData {
  // Входные поля отсутствуют
  }

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit Code 0
- Schema:
  interface OutputResult {
  status: string; // например "success" или "error"
  message?: string; // сообщение о ходе выполнения
  finalLoss?: number; // финальное значение потери Подмастерья, если доступно
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Акт 1 — Подготовка Гримуаров: создаётся трансформационный конвеер для изображений, загружается учебник MNIST, формируется поднос данных, а также конструируются две ментальные формы: Магистр и Подмастерье.
- Акт 2 — Черчение Магистра: объявляется архитектура учителя в виде сверточной сети с двумя сверточными блоками и двумя полносвязными слоями; он готовится к обучению на GPU.
- Акт 3 — Обучение Магистра: Магистр инициализируется на CUDA, настраивается оптимизатор Adam и функция потерь CrossEntropyLoss; проводится одна эпоха обучения на загрузчике данных с последовательной подачей батчей, вычислением потерь и шагами оптимизации.
- Акт 4 — Ритуал Перегонки Знаний: создаётся Подмастерье, настраиваются его параметры, фиксируются параметры для перетирания знаний: температура Distillation и коэффициент alpha. Магистр переводится в режим оценки, Подмастерье обучается через комбинированную потерю, которая сочетает distillation_loss и student_loss. Distillation_loss вычисляется через KLDivLoss между логарифмами предсказаний ученика и смещёнными предсказаниями учителя, масштабированными температурой; student_loss — CrossEntropy между выходами ученика и реальными целевыми метками. Финальная потеря — взвешенная сумма distillation_loss и student_loss, скорректированная по температуре. Происходит обратное распространение и шаг оптимизации ученика. В конце выводится финальная ошибка и сообщение об освоении мудрости Магистра.

### 4.2. Declarative Content (Для конфигураций и данных)

- 🏰 TeacherModel — Большой архитектурный проект учителя: два сверточных слоя, два полносвязных слоя, реLU активации и макс-пулинг.
- 🛡️ StudentModel — Малая экономичная архитектура: один сверточный слой, один полносвязный слой, реLU активации.
- 🗺️ Transform pipeline — Преобразования: конвертация изображений в тензоры и нормализация по MNIST-стандарту.
- 💾 Dataset и DataLoader — MNIST, путь ./data, загрузчик batch_size 64, перемешивание включено.
- 🪄 Оптимизаторы — Adam для учителя и ученика с lr 0.01.
- 🔮 Distillation temperature — 5.0.
- 🧭 Потери — CrossEntropyLoss для реальных ответов и KLDivLoss для дистилляции.
- 🗡️ Распределители вычислений — CUDA устройство для ускорения вычислений.
- ⚗️ Ритуал — поэтапное обучение учителя и последующая перегонка знаний ученика.

## 5. Structural Decomposition (Декомпозиция структуры)

- Классы:
  - TeacherModel: архитектура учителя с двумя сверточными слоями и двумя полносвязными слоями.
  - StudentModel: меньшая сверточная архитектура с одним сверточным слоем и одним полносвязным слоем.
- Конвейеры и блоки:
  - transform: последовательность преобразований MNIST.
  - train_dataset: MNIST набор данных с заданным преобразованием.
  - train_loader: DataLoader с размером батча 64.
- Учебные циклы:
  - Обучение учителя: один проход по обучающим данным.
  - Обучение ученика через перегонку знаний: обучение с совмещённой потерей distillation и student loss.
- Вспомогательные элементы:
  - criterion: CrossEntropyLoss
  - optimizer_teacher: Adam для учителя
  - optimizer_student: Adam для ученика
  - distillation_loss: KLDivLoss с лог-вероятностями учителя и ученика
  - temperature и alpha: параметры регулировки жесткости и вклада Loss

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Оптимизирован для GPU; обучение через CUDA.
- Concurrency: Синхронные циклы обучения в рамках одного потока Python.
- Dependencies: PyTorch, torchvision, tqdm; MNIST датасет онлайн-д 다운로드, CUDA доступность.

### 6.2. Prohibited Actions (Negative Constraints)

- ЗАПРЕЩЕНО хранить секреты в виде простого текста; используйте переменные окружения (.env) при необходимости.
- ЗАПРЕЩЕНО выводить сырые данные в консоль в продакшн-режиме.
- ЗАПРЕЩЕНО использовать синхронные сетевые вызовы в критичных циклах обучения.
- ЗАПРЕЩЕНО оборачивать конфигурационные файлы в скрипты без явной необходимости.
- ЗАПРЕщено менять версии библиотек или пути к данным в процессе реконструкции.

## 7. Verification & Testing (Верификация)

### Гgherkin Scenarios

Feature: Knowledge Distillation Script Functionality
Scenario: Successful execution
Given Python environment with PyTorch installed and CUDA available
And MNIST dataset can be downloaded or loaded
When the script runs through Teacher training and Student distillation
Then a final loss is produced and the process ends with exit code 0
Scenario: CUDA unavailable
Given CUDA device is unavailable
When the script attempts to move models to CUDA
Then an error occurs with a clear message about missing CUDA
