# quest_14_2.py Specification

## 1. Meta Information

- Domain: Scripting | ML/NLP
- Complexity: Medium
- Language: Python
- Frameworks: PyTorch, ONNX, onnxruntime, NumPy
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот файл описывает миграцию простой нейронной сети на универсальный язык обмена данными ONNX и проверку работоспособности через универсального Чтеца Свитков (onnxruntime). Он демонстрирует готовность к развёртыванию модели в продакшн-среде без привязки к конкретной фреймворк-библиотеке PyTorch во время исполнения.

Instruction for AI: Данный модуль задаёт понятный путь от разработки модели до её переноса в ONNX и последующей верификации через ONNX Runtime. Цель — показать ключевые шаги развёртывания и подтвердить совместимость формата.

Легенда: Этот артефакт экспонирует ритуал экспорта нейронной сети в универсальный формат и экзамен «Чтеца Свитков», чтобы продемонстрировать, что творение Голема может жить за пределами мира PyTorch и успешно дать предсказание даже из чуждого эфира.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON
- Schema:
  interface InputData {
  onnxModelPath?: string; // путь к сохраняемому Свитку
  exportParams?: boolean; // сохранить параметры весов
  opsetVersion?: number; // версия ONNX-протокола
  inputNames?: string[]; // имена входных порталов
  outputNames?: string[]; // имена выходных порталов
  dummyInputShape?: number[]; // форма входного болвана: например [1, 1, 28, 28]
  modelName?: string; // имя модели (для контекста)
  }

### 3.2. Outputs (Выходы)

- Destination: File, STDOUT
- Format: JSON
- Success Criteria: Exit code 0
- Schema:
  interface OutputResult {
  onnxModelPath?: string; // путь сохранённого Свитка
  prediction?: number; // итоговая цифра, полученная Чтецом
  status?: 'success' | 'failure';
  log?: string[]; // системные сообщения и уведомления
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Сначала конструируется простая сверточная сеть MiniCNN с двумя слоями свертки и одной полносвязной головкой.
- Ритуал подготовки: устанавливается режим eval() для предотвращения обновления весов.
- Акт 2 — Ритуал перевода на Латынь: создаётся болванка ввода и формируется путь сохранения, затем вызывается торжественный обряд torch.onnx.export с параметрами:
  - exportParams = True
  - opsetVersion = 10
  - inputNames = ["input"]
  - outputNames = ["output"]
    и сохраняется файл mnist_cnn.onnx.
- Акт 3 — Экзамен с помощью Чтеца: создаётся ONNX Runtime InferenceSession, формируется входной массив NumPy случайных данных с формой [1, 1, 28, 28] и типом float32, затем выполняется прогон Свитка и вычисляется предсказание как индекс максимального значения в выходном тензоре.
- Акт 4 — Вердикт: выводится сообщение об успешности ритуала и итоговая цифра-предсказание, демонстрируя работоспособность артефакта.

### 4.2. Declarative Content (Для конфигураций и данных)

- Конфигурация экспортируемого Свитка включает:
  - имя входного портала: input
  - имя выходного портала: output
  - размер_OPS: 10
  - сохраняемые весовые параметры: да
- Входные данные для экзамена представляют собой случайный набор чисел, соответствующий ожидаемой форме входа модели MNIST.

## 5. Structural Decomposition (Декомпозиция структуры)

- Class MiniCNN (nn.Module)
  - **init**: определение слоёв свертки и линейного слоя
  - forward: прохождение данных через conv, ReLU, max-pool, развёртывание, линейный слой и log_softmax
- Переменная model: экземпляр MiniCNN, переведённый в режим eval
- Акты:
  - Акт 2: экспорт ONNX через torch.onnx.export с заданными именами входов/выходов и параметрами
  - Акт 3: экзамен через ONNX Runtime InferenceSession
- Вспомогательные данные:
  - dummy_input: болванка ввода формы [1, 1, 28, 28]
  - onnx_model_path: mnist_cnn.onnx
  - input_data_np: случайные данные типа float32 размера [1, 1, 28, 28]
  - ort_session: сеанс ONNX Runtime
  - prediction: итоговое число класса

Inventory (Инвентарь) в духе RPG-артефактов:

- 🏰 Class MiniCNN — архитектурный чертёж мини-Ц neural-Голема, две свёртки и одно полносвязное чародейство
- 🛡️ mnist_cnn.onnx — Свиток универсального языка, хранитель весов и логики
- 🔮 Выносной болванок ввода — dummy_input и input_data_np, источники эфира форм входа
- 🗺️ Врата экспорта ONNX — параметры экспорта: exportParams, opsetVersion, inputNames, outputNames
- ⚔️ Чтец Свитков (onnxruntime) — Инферен-сессия, которая читает и выполняет Свиток
- 🧭 Эфир входа — массив случайных данных для прогона и проверки вывода
- 📜 Жезл логов — log массив сообщений о ходе ритуала и итогах

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies: torch, onnxruntime, numpy
- Обязательная совместимость: экспорт в ONNX протокол версии 10, совместимость входа/выхода с именами input и output

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (использовать .env для секретов, если они появляются в контексте сборки)
- DO NOT print raw data (массивы/байты) в консоль в продакшн-режиме
- DO NOT использовать синхронные сетевые вызовы в главном цикле
- DO NOT обернуть конфигурационные файлы (.yaml, .json) внутрь скриптов
- DO NOT менять версии или пути во время реконструкции артефакта

## 7. Verification & Testing (Верификация)

```gherkin
Feature: Quest 14.2 Script Functionality
  Scenario: Successful export and inference
    Given окружение имеет установленный PyTorch и onnxruntime
    When скрипт экспортирует модель в mnist_cnn.onnx и выполнит инференс через ONNX Runtime
    Then процесс завершается успешно и выводится корректное предсказание цифры

  Scenario: Export failure due to write permissions
    Given путь записи mnist_cnn.onnx недоступен
    When скрипт пытается экспортировать
    Then появляется ошибка и лог фиксирует сообщение об ошибке
```
