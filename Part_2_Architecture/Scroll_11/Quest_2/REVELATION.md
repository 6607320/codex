# **Архитектура Многоголового Внимания: Механика, Реализация и Кросс-модальная Адаптация**

## **1\. Введение: Смена Парадигмы в Обработке Последовательностей**

Эволюция нейросетевого моделирования последовательностей претерпела фундаментальный сдвиг с появлением архитектуры Transformer. До 2017 года доминирующая парадигма опиралась на рекуррентные нейронные сети (RNN) и сети с долгой краткосрочной памятью (LSTM). Эти архитектуры обрабатывали информацию последовательно, поддерживая скрытое состояние, которое обновлялось на каждом временном шаге. Хотя этот подход был интуитивно понятен для временных рядов и языка, он накладывал жесткие ограничения: градиентная информация и контекст от ранних токенов имели тенденцию к затуханию на больших расстояниях, что делало моделирование долгосрочных зависимостей вычислительно сложным и нестабильным.1 Более того, итеративная природа RNN исключала возможность распараллеливания обучения, поскольку вычисления для шага $t$ явно зависели от состояния на шаге $t-1$.  
Внедрение механизма **Multi-Head Attention (MHA)**, описанного в основополагающей работе "Attention Is All You Need" 3, ознаменовало отказ от рекуррентности в пользу глобального, параллельного механизма маршрутизации информации. Создание Multi-Head Attention с нуля, как предложено в рамках текущей исследовательской задачи (Квест 11.2), — это не просто упражнение в кодировании, а воспроизведение ключевого компонента, лежащего в основе современных больших языковых моделей (LLM), таких как GPT-4, Claude и Llama.4  
Данный отчет представляет собой исчерпывающий анализ архитектуры многоголового внимания. Мы деконструируем его математический фундамент, детально рассмотрим динамику тензорных преобразований при реализации, исследуем интерпретируемость через визуализацию "карт внимания" и, наконец, проанализируем адаптацию этого механизма для компьютерного зрения в архитектуре Vision Transformer (ViT), отвечая на запрос о "Башне Прозрения".

## ---

**2\. Математический Фундамент Внимания**

Для глубокого понимания "Совета Мудрецов" (метафора для Multi-Head Attention) необходимо сначала разобрать работу отдельного "мудреца" — механизма Scaled Dot-Product Attention. Этот механизм можно концептуализировать как дифференцируемую систему поиска информации, оперирующую в непрерывном векторном пространстве.

### **2.1. Парадигма Query-Key-Value**

В традиционных системах баз данных запрос (**Query**) сопоставляется с набором ключей (**Keys**) для извлечения конкретного значения (**Value**). Transformer обобщает этот принцип, делая процесс сопоставления вероятностным, а не бинарным. Каждый входной токен проецируется в три различных вектора:

- **Query ($Q$):** Вектор, представляющий информационную потребность текущего токена. Это "вопрос", который токен задает контексту.
- **Key ($K$):** Вектор, представляющий содержание токена, используемое для сопоставления. Это "метка", по которой другие токены могут найти данный токен.
- **Value ($V$):** Вектор, содержащий фактическую информацию, которая будет агрегирована. Это "ответ" или "смысловая нагрузка", передаваемая при успешном сопоставлении.5

Математически, для входной матрицы $X$ (например, эмбеддинги слов размером $L \\times d\_{model}$), эти векторы создаются через обучаемые линейные проекции:

$$Q \= XW^Q, \\quad K \= XW^K, \\quad V \= XW^V$$  
Где $W^Q, W^K, W^V$ — весовые матрицы, параметры которых оптимизируются в процессе обучения.4 Важно отметить, что в реализации, предоставленной в задаче, используются слои nn.Linear, которые выполняют именно эту операцию проекции, добавляя также вектор смещения (bias), если это не отключено явно.

### **2.2. Масштабированное Скалярное Произведение (Scaled Dot-Product)**

Ядром механизма является вычисление меры сходства между запросами и ключами. Формула внимания определяется как:

$$\\text{Attention}(Q, K, V) \= \\text{softmax}\\left(\\frac{QK^T}{\\sqrt{d\_k}}\\right)V$$  
Здесь скалярное произведение $QK^T$ вычисляет оценку сходства (score) между каждым запросом и каждым ключом. Геометрически, если два вектора в многомерном пространстве направлены в одну сторону, их скалярное произведение велико. Эта оценка определяет, сколько "внимания" токен-запрос должен уделить токену-ключу.7

#### **2.2.1. Статистическая Необходимость Масштабирования ($\\sqrt{d\_k}$)**

Критически важной, но часто упускаемой деталью является коэффициент масштабирования $\\frac{1}{\\sqrt{d\_k}}$, где $d\_k$ — размерность векторов ключей. Почему эта нормализация необходима для стабильности обучения?  
Рассмотрим статистические свойства скалярного произведения. Предположим, что компоненты векторов $q$ и $k$ являются независимыми случайными величинами с математическим ожиданием 0 и дисперсией 1\. Их скалярное произведение $q \\cdot k \= \\sum\_{i=1}^{d\_k} q\_i k\_i$ будет иметь математическое ожидание 0, но его дисперсия будет равна сумме дисперсий компонентов, то есть $d\_k$.3  
Следовательно, стандартное отклонение скалярного произведения растет пропорционально $\\sqrt{d\_k}$. Для больших размерностей (например, $d\_k \= 64$ или $128$) значения скалярных произведений могут становиться очень большими по модулю. Когда эти значения подаются на вход функции softmax, она переходит в области насыщения, где градиенты экспоненциально малы (проблема исчезающего градиента).  
Например, для входа $$, softmax выдаст значения, близкие к $\[0.000045, 0.999955\]$. Малое изменение входных данных практически не изменит выход, что остановит процесс обучения. Деление на $\\sqrt{d\_k}$ нормализует дисперсию результата до 1, удерживая значения в чувствительной зоне функции softmax и обеспечивая надежный поток градиентов при обратном распространении ошибки.3 В коде квеста это реализовано строкой attn_scores \= torch.matmul(Q, K.transpose(-2, \-1)) / math.sqrt(self.d_k).

### **2.3. От Оценок к Вероятностям**

Функция softmax преобразует сырые оценки сходства (логиты) в распределение вероятностей. Для заданного запроса сумма весов внимания по всем ключам равна 1\. Это создает механизм "мягкого" выбора. Финальный выход представляет собой взвешенную сумму векторов значений ($V$), где весами выступают рассчитанные вероятности. Если токен отдает 90% внимания слову "Король" и 10% слову "Корону", результирующий вектор будет линейной комбинацией, в которой доминирует семантика "Короля".5

## ---

**3\. Архитектура Multi-Head Attention: Совет Мудрецов**

Хотя одноголовое внимание является мощным инструментом, оно ограничено способностью фокусироваться только на одном типе взаимосвязей одновременно. Одно вероятностное распределение может успешно отслеживать синтаксическое согласование (например, подлежащее-сказуемое), но при этом подавлять информацию о семантических связях (например, сущность-действие). "Многогранность", упомянутая в легенде квеста, требует параллельного анализа.

### **3.1. Параллельные Подпространства Репрезентации**

Multi-Head Attention решает эту проблему, запуская $h$ механизмов внимания (голов) параллельно. Каждая голова имеет свой собственный набор обучаемых матриц проекции ($W\_i^Q, W\_i^K, W\_i^V$), что позволяет им проецировать входные эмбеддинги в различные "подпространства репрезентации".3  
Это аналогично рассмотрению предложения через различные лингвистические фильтры одновременно:

- **Голова 1 ("Грамматик"):** Может фокусироваться на морфологии и окончаниях слов.
- **Голова 2 ("Синтаксис"):** Может отслеживать зависимости между глаголами и их дополнениями.
- **Голова 3 ("Семантик"):** Может искать синонимы или антонимы в контексте.
- **Голова 4 ("Позиционер"):** Может просто смотреть на предыдущий токен, имитируя работу n-граммной модели.

Математическая формулировка для Multi-Head Attention выглядит следующим образом:

$$\\text{MultiHead}(Q, K, V) \= \\text{Concat}(\\text{head}\_1, \\dots, \\text{head}\_h)W^O$$  
где каждая голова вычисляется как:

$$\\text{head}\_i \= \\text{Attention}(QW\_i^Q, KW\_i^K, VW\_i^V)$$  
В финале конкатенированные выходы всех голов проходят через итоговую линейную проекцию $W^O$, которая "смешивает" информацию из различных подпространств и восстанавливает исходную размерность модели.3 В коде квеста это отражено в методе combine_heads и последующем применении self.W_o.

### **3.2. Размерный Анализ и Эффективность**

Для модели с размерностью эмбеддинга $d\_{\\text{model}} \= 128$ и количеством голов $h=8$ (как в условиях задачи), каждая голова оперирует в пространстве размерностью $d\_k \= d\_v \= d\_{\\text{model}} / h \= 16$.  
Критически важно понимать, что общая вычислительная стоимость мульти-голового внимания примерно равна стоимости одноголового внимания с полной размерностью. Это достигается за счет разделения размерности модели, а не ее дублирования.

| Параметр                         | Значение в Квесте | Комментарий                                         |
| :------------------------------- | :---------------- | :-------------------------------------------------- |
| d_model                          | 128               | Общая "глубина смысла" (размер вектора токена).     |
| num_heads                        | 8                 | Количество "мудрецов" (независимых механизмов).     |
| d_k                              | 16                | Размерность проекции для одной головы ($128 // 8$). |
| Размер матриц $W\_Q, W\_K, W\_V$ | $128 \\times 128$ | Проекции осуществляются для всех голов сразу.       |
| Размер матрицы $W\_O$            | $128 \\times 128$ | Финальное смешивание результатов.                   |

Такое разбиение гарантирует, что емкость модели распределяется между различными типами извлечения признаков, вместо того чтобы углублять лишь один тип.10

## ---

**4\. Динамика Реализации и Тензорные Преобразования**

Реализация MHA "с нуля", требуемая в квесте, предполагает точную манипуляцию многомерными тензорами. Эффективность этой реализации напрямую влияет на скорость обучения и инференса. Рассмотрим ключевые этапы, реализованные в коде quest_11_2.py.

### **4.1. От Наивного к Эффективному Подходу**

Наивная реализация могла бы итерировать через каждую голову с помощью цикла for, вычисляя внимание последовательно. Это катастрофически неэффективно для GPU, так как не использует возможности массивного параллелизма. Стандартная "эффективная" реализация, представленная в квесте, использует тот факт, что линейные проекции для всех голов можно объединить в единые большие матрицы весов.4  
Вместо 8 отдельных матриц размером $128 \\times 16$, мы используем одну матрицу $128 \\times 128$ для $Q$, одну для $K$ и одну для $V$. Разделение на головы происходит исключительно через операции изменения формы (view) и перестановки осей (transpose).

### **4.2. Критические Операции: View и Transpose**

В методе split_heads кода квеста происходит ключевая трансформация:  
x.view(batch_size, seq_length, self.num_heads, self.d_k).transpose(1, 2\)  
Разберем, что происходит с формой тензора:

1. **Вход:** $(B, L, D)$, где $D \= H \\times d\_k$.
2. **View:** $(B, L, H, d\_k)$. Тензор интерпретируется как имеющий 4 измерения. Данные в памяти не перемещаются.
3. **Transpose:** $(B, H, L, d\_k)$. Мы меняем местами размерности длины последовательности и количества голов.

**Почему это важно?** Операция матричного умножения torch.matmul в PyTorch поддерживает "бродкастинг" (broadcasting). Если ей передать тензоры размерности $(B, H, L, d\_k)$ и $(B, H, d\_k, L)$, она будет выполнять умножение матриц для последних двух измерений ($L \\times d\_k$ и $d\_k \\times L$) независимо для каждой комбинации $(B, H)$. Таким образом, мы вычисляем внимание для всех 8 голов и всех примеров в батче **одной операцией**.10

### **4.3. Ловушка Памяти: Contiguous vs. Permute**

Распространенной ошибкой при реализации является непонимание разницы между логическим представлением тензора и его физическим расположением в памяти.

- **view** требует, чтобы тензор был непрерывным (contiguous) в памяти.
- **transpose (или permute)** меняет "шаги" (strides) тензора, но не перемещает данные физически. Результат становится _не непрерывным_.

В методе combine_heads кода квеста мы видим цепочку:  
x.transpose(1, 2).contiguous().view(...)  
Вызов .contiguous() здесь обязателен. После обратной перестановки осей (возвращение к $(B, L, H, d\_k)$), память все еще "перепутана" с точки зрения линейного доступа. Попытка вызвать .view без .contiguous() вызвала бы ошибку RuntimeError. Метод .contiguous() форсирует копирование данных в новый буфер памяти в правильном порядке, позволяя затем безопасно "сплющить" последние две размерности обратно в $D\_{model}$.12

## ---

**5\. Оптимизация Инференса: KV-Кэширование**

Хотя представленная в квесте реализация корректна для обучения и демонстрации, при реальном использовании в LLM для генерации текста (авторегрессионный режим) возникает проблема производительности.  
При генерации каждого нового токена модель должна "смотреть" на все предыдущие токены. Если использовать стандартный метод forward, то для генерации токена $t$ мы будем заново пересчитывать ключи ($K$) и значения ($V$) для всех токенов от $0$ до $t-1$. Это приводит к квадратичной сложности $O(N^2)$ при генерации.  
Решение: KV-Кэширование (KV Cache).  
Поскольку веса модели не меняются во время инференса, проекции $K$ и $V$ для прошлых токенов остаются неизменными. Мы можем сохранить их в видеопамяти (VRAM) и переиспользовать.

$$\\text{Cache}\_{t} \= \[\\text{Cache}\_{t-1}; K\_t\]$$  
На шаге $t$ вычисляется только $Q\_t$, $K\_t$ и $V\_t$ для _текущего_ токена. Затем $K\_t$ и $V\_t$ добавляются в кэш, и внимание вычисляется относительно всего кэша. Это снижает сложность вычислений проекций до $O(N)$.14  
Бизнес-ценность понимания KV-Cache:  
Основным узким местом при работе больших моделей (LLM) часто являются не вычисления (FLOPs), а пропускная способность памяти (Memory Bandwidth). Загрузка огромного кэша KV из памяти в вычислительные ядра занимает время. Это привело к появлению таких оптимизаций, как Multi-Query Attention (MQA), где все головы делят одну пару матриц $K$ и $V$, значительно уменьшая объем памяти, необходимый для кэша, ценой небольшого снижения качества.14

## ---

**6\. Интерпретируемость: Чтение Карт Внимания**

В рамках квеста создается артефакт multi_head_attention_map.png. Визуализация карт внимания — это мощный инструмент диагностики и интерпретации. Визуализируя веса внимания (выход softmax) как тепловые карты, где ось Y — это Query, а ось X — Key, мы можем декодировать, что именно "выучила" каждая голова.

### **6.1. Типология Паттернов Внимания**

Анализируя тепловые карты, можно выделить устойчивые визуальные паттерны 16:

1. **Диагональ (Diagonal):** Яркая линия вдоль главной диагонали ($i=j$). Это **Self-Attention** в буквальном смысле — токен смотрит сам на себя. Это часто наблюдается в ранних слоях для сохранения локальной информации.
2. **Сдвинутая Диагональ (Offset Diagonal):** Линия, сдвинутая на \+1 или \-1. Это означает, что токен смотрит на своего непосредственного соседа. В этом режиме голова внимания имитирует свертку (Convolution) или n-грамму, собирая локальный контекст.
3. **Вертикаль (Vertical):** Сплошная вертикальная полоса. Это означает, что _все_ токены смотрят на один конкретный токен (часто на знак препинания или специальный токен \`\`). Это механизм "глобального сентинела" — когда голове не на что смотреть, она "паркует" внимание на нейтральном токене, или же этот токен аккумулирует глобальное представление всего предложения.
4. **Блоки (Block):** Квадраты высокой интенсивности. Это указывает на внимание внутри определенной фразы или семантической группы, изолированной от остального текста.

### **6.2. Лингвистическая Специализация Голов**

Исследования, такие как "What Does BERT Look At?" (Clark et al., 2019), подтверждают гипотезу "Совета Мудрецов". Головы специализируются на лингвистических функциях без явного учителя 18:

- **Синтаксические головы:** Определенные головы с высокой точностью отслеживают связи типа "прямое дополнение". Если Query — глагол, такая голова будет иметь максимальный вес на существительном-объекте.
- **Кореферентные головы:** Другие головы связывают местоимения (например, "она") с их антецедентами ("Мария"), даже если они находятся далеко друг от друга в предложении.

Понимание этого позволяет проводить **эффективную отладку**, как указано в целях квеста. Если модель ошибается в связывании местоимений, можно проверить активации конкретных "кореферентных" голов.

## ---

**7\. Vision Transformer (ViT): Башня Прозрения**

В финале квеста "Техномант" спрашивает о применении созданного "Рунного Камня" к изображениям. Ответ Мастера отсылает к архитектуре **Vision Transformer (ViT)**. Это применение демонстрирует универсальность механизма MHA.

### **7.1. От Пикселей к Патчам: Визуальные Слова**

Стандартный Transformer требует на входе последовательность векторов. Изображение же представляет собой двумерную сетку пикселей. Чтобы преодолеть этот разрыв, ViT использует технику **Patch Embeddings** (Эмбеддинги Патчей).

1. **Нарезка (Patching):** Изображение (например, $224 \\times 224$) разбивается на сетку фиксированных квадратов — патчей (например, $16 \\times 16$).
2. **Выпрямление (Flattening):** Каждый патч преобразуется в 1D вектор. Патч $16 \\times 16 \\times 3$ (RGB) становится вектором размерности $768$ ($16 \\cdot 16 \\cdot 3$).
3. **Проекция (Projection):** Этот вектор линейно проецируется в размерность модели $D$.

В PyTorch это часто реализуется не через циклы нарезки, а через **Сверточный слой (Conv2d)** с размером ядра и шагом (stride), равными размеру патча: nn.Conv2d(in_channels=3, out_channels=768, kernel_size=16, stride=16). Эта операция мгновенно превращает картинку в последовательность "визуальных слов".20

### **7.2. Проблема Позиции в 2D и Интерполяция**

MHA инвариантен к перестановкам. Если перемешать слова в предложении, вычисление $QK^T$ даст те же значения (хотя и в других местах). Для изображения знание того, что патч находится в левом верхнем углу, а не в правом нижнем, критически важно.  
ViT добавляет **Позиционные Эмбеддинги** к эмбеддингам патчей. Интересно, что стандартный ViT использует **1D обучаемые эмбеддинги** (от 1 до 256), а не 2D координаты ($X, Y$). Модель сама выучивает топологию 2D пространства из этих 1D индексов в процессе обучения. Карты внимания ранних слоев ViT часто реконструируют эту сетку, показывая, что патч смотрит в основном на своих пространственных соседей.22  
Инженерный вызов: Что делать, если разрешение изображения меняется? Если модель обучена на $224 \\times 224$ (196 патчей), а на вход подается $384 \\times 384$ (576 патчей), предобученные позиционные эмбеддинги не подойдут.  
Решение — 2D Интерполяция. Предобученные 1D эмбеддинги "раскладываются" в 2D сетку и интерполируются (растягиваются) до нового размера. Это позволяет ViT работать с любым разрешением, что является существенным преимуществом.23

### **7.3. CNN против ViT: Битва Индуктивных Смещений**

Переход от CNN к ViT — это обмен **Индуктивного Смещения (Inductive Bias)** на **Эффективность Данных**.

- **CNN (ResNet):** Обладают сильным индуктивным смещением (локальность, инвариантность к сдвигу). Они "знают" заранее, что пиксели рядом связаны. Поэтому они отлично учатся на малых данных.24
- **ViT:** Лишены этих предположений. Механизм MHA с первого слоя связывает _каждый патч с каждым_. Модель должна _выучить_ с нуля, что локальные пиксели важны. Это делает ViT "голодными до данных" (Data Hungry). Они проигрывают CNN на малых датасетах (ImageNet-1k), но при обучении на гигантских массивах (JFT-300M) начинают превосходить CNN, так как их не ограничивает жесткое окно свертки. Они способны улавливать глобальные связи, недоступные сверткам.25

## ---

**8\. Заключение**

Механизм Multi-Head Attention — это не просто статистический инструмент, а обобщенная архитектура для контекстуального рассуждения. Проецируя данные в множественные подпространства, разделяя их между параллельными "мудрецами" и позволяя глобальное взаимодействие через масштабированное скалярное произведение, MHA позволяет моделям синтезировать сложные, многогранные представления данных.  
Выполнив Квест 11.2, вы не просто написали код. Вы воссоздали двигатель, который позволил ИИ перейти от простого предсказания следующего слова к пониманию нюансов языка, а затем — к зрению и генерации мультимедиа. Визуализация, которую вы создали, является доказательством того, что внутри "черного ящика" действительно существует структура: специализация, разделение труда и многоуровневый анализ контекста.

### **Таблица 1: Сравнительный анализ CNN и Vision Transformer (ViT)**

| Характеристика                 | CNN (ResNet, EfficientNet)                          | Vision Transformer (ViT)                                      |
| :----------------------------- | :-------------------------------------------------- | :------------------------------------------------------------ |
| **Индуктивное Смещение**       | **Высокое** (Локальность, Инвариантность к сдвигу). | **Низкое** (Должен выучить пространственные связи).           |
| **Рецептивное Поле**           | **Локальное** (растет с глубиной).                  | **Глобальное** (с самого первого слоя).                       |
| **Эффективность Данных**       | **Высокая** (Хорошо работает на малых датасетах).   | **Низкая** (Требует ImageNet-21k/JFT-300M для превосходства). |
| **Обработка Входа**            | Сетка пикселей через скользящее окно.               | Последовательность патчей через Self-Attention.               |
| **Позиционное Кодирование**    | Не требуется (неявно в свертке).                    | **Критично** (Обучаемые 1D или 2D эмбеддинги).                |
| **Масштабирование Разрешения** | Архитектурно обусловлено (Global Pooling).          | **Гибкое** через 2D интерполяцию позиций.                     |

### **Таблица 2: Словарь Тензорных Операций MHA**

| Символ/Операция | Размерность (Typ.)               | Роль в Архитектуре                               |
| :-------------- | :------------------------------- | :----------------------------------------------- |
| $X$ (Input)     | $(B, L, D)$                      | Входные данные (батч, длина, размерность).       |
| $W^Q, W^K, W^V$ | $(D, D)$                         | Матрицы проекции (линейные слои).                |
| split_heads     | $(B, L, D) \\to (B, H, L, d\_k)$ | Разделение на головы через view и transpose.     |
| $QK^T$ (MatMul) | $(B, H, L, L)$                   | Матрица "сырых" оценок внимания.                 |
| softmax         | $(B, H, L, L)$                   | Преобразование в вероятности (сумма строк \= 1). |
| combine_heads   | $(B, H, L, d\_k) \\to (B, L, D)$ | Слияние голов через transpose, contiguous, view. |
| $W^O$ (Output)  | $(D, D)$                         | Финальное смешивание информации от всех голов.   |

#### **Источники**

1. “Attention is all you need” in plain English | by Ujwal Tickoo | Nov, 2025, дата последнего обращения: декабря 20, 2025, [https://medium.com/@ujwaltickoo/attention-is-all-you-need-in-plain-english-b176d1ad4ada](https://medium.com/@ujwaltickoo/attention-is-all-you-need-in-plain-english-b176d1ad4ada)
2. Attention Mechanisms Demystified. What is Attention ? | by ritesh ratti, Ph.D | Dec, 2025, дата последнего обращения: декабря 20, 2025, [https://medium.com/@ritesh.ratti/attention-mechanisms-demystified-0382d36109da](https://medium.com/@ritesh.ratti/attention-mechanisms-demystified-0382d36109da)
3. Attention is All you Need \- NIPS papers, дата последнего обращения: декабря 20, 2025, [https://papers.neurips.cc/paper/7181-attention-is-all-you-need.pdf](https://papers.neurips.cc/paper/7181-attention-is-all-you-need.pdf)
4. Multi-Head Attention From Scratch \- Sanjaya's Blog, дата последнего обращения: декабря 20, 2025, [https://sanjayasubedi.com.np/deeplearning/multihead-attention-from-scratch/](https://sanjayasubedi.com.np/deeplearning/multihead-attention-from-scratch/)
5. Self-Attention & Multi-Head Attention Made Simple | by Hugo Le Picard, PhD | Medium, дата последнего обращения: декабря 20, 2025, [https://medium.com/@lepicardhugo/attention-from-first-principles-to-production-the-fundamentals-def39bee8f46](https://medium.com/@lepicardhugo/attention-from-first-principles-to-production-the-fundamentals-def39bee8f46)
6. Tutorial 6: Transformers and Multi-Head Attention — UvA DL Notebooks v1.2 documentation, дата последнего обращения: декабря 20, 2025, [https://uvadlc-notebooks.readthedocs.io/en/latest/tutorial_notebooks/tutorial6/Transformers_and_MHAttention.html](https://uvadlc-notebooks.readthedocs.io/en/latest/tutorial_notebooks/tutorial6/Transformers_and_MHAttention.html)
7. In Depth Understanding of Attention Mechanism (Part II) \- Scaled Dot-Product Attention and Example | by FunCry | Medium, дата последнего обращения: декабря 20, 2025, [https://medium.com/@funcry/in-depth-understanding-of-attention-mechanism-part-ii-scaled-dot-product-attention-and-its-7743804e610e](https://medium.com/@funcry/in-depth-understanding-of-attention-mechanism-part-ii-scaled-dot-product-attention-and-its-7743804e610e)
8. Attention Is All You Need \- Wikipedia, дата последнего обращения: декабря 20, 2025, [https://en.wikipedia.org/wiki/Attention_Is_All_You_Need](https://en.wikipedia.org/wiki/Attention_Is_All_You_Need)
9. MultiheadAttention — PyTorch 2.9 documentation, дата последнего обращения: декабря 20, 2025, [https://docs.pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html](https://docs.pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html)
10. Understanding Transformers and Multi-Head Attention with PyTorch | by Vamsikd \- Medium, дата последнего обращения: декабря 20, 2025, [https://medium.com/@vamsikd219/understanding-transformers-and-multi-head-attention-with-pytorch-008715e6cf88](https://medium.com/@vamsikd219/understanding-transformers-and-multi-head-attention-with-pytorch-008715e6cf88)
11. Multi-Head Attention \- pytorch \- D2L Discussion, дата последнего обращения: декабря 20, 2025, [https://discuss.d2l.ai/t/multi-head-attention/1635](https://discuss.d2l.ai/t/multi-head-attention/1635)
12. Different between permute, transpose, view? Which should I use? \- PyTorch Forums, дата последнего обращения: декабря 20, 2025, [https://discuss.pytorch.org/t/different-between-permute-transpose-view-which-should-i-use/32916](https://discuss.pytorch.org/t/different-between-permute-transpose-view-which-should-i-use/32916)
13. Difference between view, reshape and permute \- autograd \- PyTorch Forums, дата последнего обращения: декабря 20, 2025, [https://discuss.pytorch.org/t/difference-between-view-reshape-and-permute/54157](https://discuss.pytorch.org/t/difference-between-view-reshape-and-permute/54157)
14. 📌 Day 11: 21 Days of Building a Small Language Model: Multi Query Attention📌, дата последнего обращения: декабря 20, 2025, [https://devopslearning.medium.com/day-11-21-days-of-building-a-small-language-model-multi-query-attention-5103652dd7c6](https://devopslearning.medium.com/day-11-21-days-of-building-a-small-language-model-multi-query-attention-5103652dd7c6)
15. Taming LLMs: Inference Optimization Techniques | by İlyurek Kılıç | Nov, 2025, дата последнего обращения: декабря 20, 2025, [https://medium.com/@ilyurek/taming-llms-inference-optimization-techniques-3700d5512e08](https://medium.com/@ilyurek/taming-llms-inference-optimization-techniques-3700d5512e08)
16. How Does Attention Work in Vision Transformers? A Visual Analytics Attempt \- PMC \- NIH, дата последнего обращения: декабря 20, 2025, [https://pmc.ncbi.nlm.nih.gov/articles/PMC10290521/](https://pmc.ncbi.nlm.nih.gov/articles/PMC10290521/)
17. Understanding Transformer Attention Mechanisms: Visual Insights | by Micheal Bee, дата последнего обращения: декабря 20, 2025, [https://medium.com/@mbonsign/understanding-transformer-attention-mechanisms-visual-insights-af164a5a9e39](https://medium.com/@mbonsign/understanding-transformer-attention-mechanisms-visual-insights-af164a5a9e39)
18. Do Attention Heads in BERT Track Syntactic Dependencies? \- OpenReview, дата последнего обращения: декабря 20, 2025, [https://openreview.net/pdf?id=rJgoYekkgB](https://openreview.net/pdf?id=rJgoYekkgB)
19. What Does BERT Look at? An Analysis of BERT's Attention \- ACL Anthology, дата последнего обращения: декабря 20, 2025, [https://aclanthology.org/W19-4828.pdf](https://aclanthology.org/W19-4828.pdf)
20. How do Vision Transformers Work? Architecture Explained \- Codecademy, дата последнего обращения: декабря 20, 2025, [https://www.codecademy.com/article/vision-transformers-working-architecture-explained](https://www.codecademy.com/article/vision-transformers-working-architecture-explained)
21. 11.8. Transformers for Vision — Dive into Deep Learning 1.0.3 documentation, дата последнего обращения: декабря 20, 2025, [https://d2l.ai/chapter_attention-mechanisms-and-transformers/vision-transformer.html](https://d2l.ai/chapter_attention-mechanisms-and-transformers/vision-transformer.html)
22. Understanding Patch Embeddings for Vision Transformers (ViT) | by Frederik vom Lehn | Advanced Deep Learning | Medium, дата последнего обращения: декабря 20, 2025, [https://medium.com/advanced-deep-learning/understanding-vision-transformers-vit-70ca8d817ff3](https://medium.com/advanced-deep-learning/understanding-vision-transformers-vit-70ca8d817ff3)
23. Vision Transformers (VIT)- A detailed walkthrough(part1) | by ..., дата последнего обращения: декабря 20, 2025, [https://medium.com/@sinhamaitry6/vision-transformer-model-vit-a-detailed-walkthrough-5713d795016b](https://medium.com/@sinhamaitry6/vision-transformer-model-vit-a-detailed-walkthrough-5713d795016b)
24. Vision Transformer vs. CNN for Object Detection \- Roboflow Blog, дата последнего обращения: декабря 20, 2025, [https://blog.roboflow.com/vision-transformer-vs-cnn-for-detection/](https://blog.roboflow.com/vision-transformer-vs-cnn-for-detection/)
25. A comparative study between vision transformers and CNNs in digital pathology \- arXiv, дата последнего обращения: декабря 20, 2025, [https://arxiv.org/abs/2206.00389](https://arxiv.org/abs/2206.00389)
26. A Comparative Study of Vision Transformers and CNNs for Few-Shot Rigid Transformation and Fundamental Matrix Estimation \- arXiv, дата последнего обращения: декабря 20, 2025, [https://arxiv.org/html/2510.04794v1](https://arxiv.org/html/2510.04794v1)
