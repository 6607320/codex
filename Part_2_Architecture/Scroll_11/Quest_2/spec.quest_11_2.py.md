# quest_11_2.py Specification

## 1. Meta Information

- **Domain:** Scripting
- **Complexity:** Medium
- **Language:** Python
- **Frameworks:** PyTorch, Matplotlib, NumPy
- **Context:** ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот модуль — ритуал визуализации работы многогранного внимания в трансформерах. Он строит самодостаточный Рунный Камень под названием MultiHeadAttention, проводит через него условный эфир Q, K и V, а затем выводит восемь карт внимания — по одной для каждых мудрецов головы — чтобы продемонстрировать различие восприятия одного факта разными точками зрения.

**Instruction for AI:** This section provides high-level intent. Use it to understand the "WHY" of the code.

Этот артефакт призван показать, что многогранность внимания не сводится к единому взгляду, а порождает восемь параллельных трактовок одного предложения. Визуальные карты внимания служат доказательством того, как каждый голова выбирает свои значимые связи.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args
- **Format:** JSON
- **Schema:**
  interface InputData {
  Q: number[][][];
  K: number[][][];
  V: number[][][];
  }

### 3.2. Outputs (Выходы)

- **Destination:** File | STDOUT
- **Format:** JSON
- **Success Criteria:** Output produced; image сохранена как multi_head_attention_map.png; консоль выдала легенды
- **Schema:**
  interface OutputResult {
  outputShape: number[]; // например [1, 5, 128]
  imagePath?: string; // например "multi_head_attention_map.png"
  lastAttnWeightsShape?: number[]; // например [8, 5, 5]
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Ритуал начинается с подготовки чар и камней: подключаются нужные библиотеки и создаются чертежи артефакта MultiHeadAttention. Далее выполняется проверка баланса смыслов: глубина модели должна быть кратна числу мудрецов, чтобы каждый мудрец получил ровную долю смысла. Затем строятся рунические камни для Q, K и V — линейные преобразования, которые превращают входную ауру в три набора признаков. Эти элементы раскладываются по головам, чтобы каждый мудрец мог работать автономно.

Следующий шаг — главный ритуал внимания: для каждого мудреца вычисляется схожесть между Q и K, нормализуется через мягкое максимальное преобразование и умножается на V, создавая новую ауру для каждого мудреца. Выводы мудрецов собираются обратно в единое целое и проходят через финальный камень W_o, чтобы получить итоговую ауру вывода.

Параллельно хранится карта весов внимания last_attn_weights для каждого мудреца. Затем начинается акт визуализации: создаются восемь холстов на карте, на каждом из которых рисуется тепловая карта внимания соответствующего мудреца. Подписи осей дают имя каждому слову в тестовом наборе, а заголовок всей картины сообщает о роли Совета Мудрецов. Картина сохраняется в файл multi_head_attention_map.png, и ритуал завершается сообщением в консоль.

Наконец, производится расшифровка результата: карта внимания толкуется как множество разночтений восприятия и демонстрируются примеры того, какие связи могли уловить разные мудрецы.

### 4.2. Declarative Content (Для конфигураций и данных)

Это артефакт конфигураций и данных, превращённый в рейдовые предметы:

- 🏰 Гримуар MultiHeadAttention: d_model 128, num_heads 8, d_k 16
- 🛡️ Рунный камень W_q: линейный преобразователь из 128 в 128
- 🛡️ Рунный камень W_k: линейный преобразователь из 128 в 128
- 🛡️ Рунный камень W_v: линейный преобразователь из 128 в 128
- 🛡️ Рунный камень W_o: линейный преобразователь из 128 в 128
- 🗺️ Эфир входа: тензор x формы 1x5x128 (примерные значения — случайные для демонстрации)
- 🗺️ Карты внимания: last_attn_weights формы 8x5x5 (после выполнения)
- 🗺️ Визуализация: файл multi_head_attention_map.png
- 🗺️ Итог вывода: форма вывода 1x5x128
- 🔮 Сообщения консоли: тексты о ходе ритуала и итогах

## 5. Structural Decomposition (Декомпозиция структуры)

- Класс MultiHeadAttention
  - Метод **init**(self, d_model, num_heads)
  - Метод scaled_dot_product_attention(self, Q, K, V)
  - Метод split_heads(self, x)
  - Метод combine_heads(self, x)
  - Метод forward(self, Q, K, V)
- Вспомогательные ритуалы установки параметров, вызов forward и генерация визуализации
- Главная секция тестирования, где задаются параметры и запускается ритуал для демонстрации

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Стандартный CPU с опциональной поддержкой CUDA; рассчитан на небольшие тензорные наборы для демонстрации
- **Concurrency:** Синхронный режим выполнения
- **Dependencies:** PyTorch, Matplotlib, NumPy

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде; используйте переменные окружения (.env) для чувствительных данных
- DO NOT выводить сырые данные в консоль в продакшн-режиме
- DO NOT выполнять синхронные сетевые вызовы в главном цикле без необходимости
- DO NOT оборачивать конфигурационные файлы YAML/JSON в скрипты (как часть кода)
- DO NOT менять версии библиотек или пути во время реконструкции артефакта

## 7. Verification & Testing (Верификация)

### Gherkin: Happy path

Feature: Quest 11_2 Script Functionality
Scenario: Successful execution
Given valid input tensors Q, K, V shaped как 1x5x128
When the MultiHeadAttention forward is executed with Q, K и V равными входной ауре
Then the output tensor имеет форму 1x5x128 и файл multi_head_attention_map.png сохранен

### Gherkin: Error case

Feature: Quest 11_2 Script Functionality
Scenario: Divisibility constraint violation
Given модель создана с d_model равным 128 и num_heads равным 7
When пытаются инициализировать MultiHeadAttention
Then генерируется ошибка assertion с сообщением d_model must be divisible by num_heads

ИСТОЧНИК АРТЕФАКТА: quest_11_2.py
ИСХОДНЫЙ КОД: запрет копировать код, но лор артефакта описан выше и сопровождается-step wise объяснениями и визуализацией.
