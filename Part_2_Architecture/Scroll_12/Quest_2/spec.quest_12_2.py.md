# quest_12_2.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: High
- Language: Python
- Frameworks: PyTorch, torchvision, tqdm
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот модуль реализует упрощенную версию диффузионной модели, демонстрируя ритуал обучения простого UNet для предсказания шума и обратного процесса вычищения шума, завершающего созданием нового образа. Привязка к бизнес-задаче — иллюстрировать базовый цикл генеративной диффузии на примере MNIST и показать путь от «хаоса» к чистой статуе изображения. Instruction for AI: В этом разделе изложен WHY — зачем нужен файл и что он демонстрирует на концептуальном уровне.

Легенда: Это учебный квест по созданию Туманного Образа с помощью упрощенного диффузионного процесса: от прямого процесса добавления шума к step-by-step обратному процессу, приводящему к новым изображениям.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
Format: JSON | Text | Binary | Stream
Schema:

```typescript
// Define input interfaces here
interface InputData {
  // Нет внешних входов для этого скрипта
}
```

### 3.2. Outputs (Выходы)

Destination: <STDOUT | File | Database | API Response | Event Log>
Format: <JSON | CSV | Text>
Success Criteria: <Exit Code 0 | 200 OK | File Created>
Schema:

```typescript
// Define output interfaces here
interface OutputResult {
  destination: "STDOUT" | "File" | "Database" | "API Response" | "Event Log";
  path?: string;
  fileName?: string;
  success: boolean;
  details?: string;
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Подготовка духовного архива: инициализация окружения магических ресурсов — подключение к ос, torch, nn, F, optim, torchvision и прогресс-магии tqdm.
- Настройка Скрижали (Скрипта): выбор параметров ритуала — размер изображения 32, размер пачки 128, число эпох 20.
- Подготовка Учебника: создание конвейера трансформаций из MNIST — увеличение до 32x32, преобразование в тензор, нормализация к диапазону [-1, 1].
- Чертеж Скульптора Хаоса: построение простого UNet-подобного контура SimpleUNet с путями сжатия и восстановления, механизмом skip-соединения и операциями свертки/пуллинга/апсайлинга.
- Настройка Магии Шума: конфигурация прямой диффузии — 300 шагов, стартовые и финальные бета-коэффициенты, вычисление betas, alphas и алгебраических накоплений; перевод параметров на CUDA.
- Ритуал Наставления Скульптора: обучение модели через цикл эпох и батчей. В каждой итерации выбирается случайный шаг зашумления t для каждой картинки в пачке, чистые изображения помещаются на CUDA, создаются зашумленные варианты и истинный шум, предскаваемый шум сравнивается с реальным шумом через MSELoss, выполняется обратное распространение и шаг оптимизации. По завершении эпохи выводится текущий показатель потери.
- Магия Творения (Обратная Диффузия): после обучения запускается генерация 64 шедевров. Инициализируется случайный тензор шума, затем в обратном порядке шагов t выполняется предсказание шума и обновление изображения по формуле обратной диффузии; при каждом шаге добавляется небольшой шум для стабильности, кроме последнего.
- Финальный этап: нормализация финальных изображений в диапазон [0, 1], сохранение сетки шедевров в chaos_sculptures/new_creation.png и сообщение об удачном завершении.

Эти шаги образуют полный цикл: от прямого процесса добавления шума до обратного процесса очистки, завершающего создание нового образа из хаоса.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory items превратим в RPG-предметы (🏰, 🛡️), описывая активы и структуры проекта как артефакты Латентного Логира.

- 🏰 Архитектура SimpleUNet: базовый песочный скелет с двумя уровнями сжатия и двумя уровнями восстановления, включая skip-соединение для сохранения деталей.
- 🛡️ Оптимизатор Adam: хранитель нитей обучения, задающий скорость обучения 1e-3 и адаптивное обновление параметров.
- 🗡️ Критерий MSELoss: заклятие вычисления разницы между предсказанным шумом и истинным шумом.
- 🏰 Эпохи и шаги: 20 эпох обучения, 128 элементов батча, 300 шагов диффузии, конфигурация betas/alphas/alphas_cumprod.
- 🧭 Учебник MNIST: источник данных, который превращает изображения в тензоры 1x28x28 и подготавливает их к диффузии.
- 🔮 Расписание шума: массив betas, алгебра алф и накопление альф, предоставляющие траекторию шума по времени.
- 🧭 Доступ к CUDA: все вычисления проходят на CUDA-устройстве (при наличии), с запасной поддержкой CPU.
- 📜 Инструменты прогресса: tqdm для отображения хода обучения и прогресса творения.
- 🧊 Ритуал "Скульптора Хаоса": цикл обучения и синхронная оптимизация с сохранением итоговой картинки chaos_sculptures/new_creation.png.
- 📚 Ритуальная логика: get_noisy_image как предикат зашумления и обратная формула для восстановления изображения на финальном этапе.

Важно: этот раздел содержит неперекодированные концептуальные артефакты, а не копии кода.

## 5. Structural Decomposition (Декомпозиция структуры)

- Фрагменты кода и классы:
  - SimpleUNet: класс архитектуры нейронной сети с контурами Down и Up, пуллингом и апсайпингом, skip-соединением.
- Основные блоки логики:
  - Подготовка данных: MNIST загрузка и предобработка.
  - Настройка обучения: параметры ритуала, устройство, оптимизатор, критерий.
  - Фаза наставления: цикл обучения по эпохам и батчам.
  - Фаза творения: обратная диффузия, сборка финального изображения и сохранение.
- Вспомогательные элементы:
  - get_noisy_image: вычисление зашумленного изображения на основе текущего шага t.
  - Генерация итоговых изображений: процесс обратной диффузии с обновлением состояний.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Требуется CUDA-совместимая видеокарта для нормальной скорости; скрипт рассчитан на использование GPU, но имеет запас на CPU.
- Concurrency: Однопроцессная последовательная обработка (нет распределенного обучения).
- Dependencies: PyTorch, torchvision, tqdm; MNIST данные автоматически загружаются/скачиваются.
- Memory: image_size 32, batch_size 128, модель SimpleUNet — умеренно требовательна к памяти; таймстепы 300 и сохранение массива параметров — умеренно потребуют памяти при обучении.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде в коде или конфигурациях (.env рекомендуется для референсной интеграции).
- НЕ выводить сырые данные в консоль в продакшн-режиме.
- НЕ использовать синхронные сетевые вызовы в основном цикле обучения.
- НЕ оборачивать конфигурационные файлы (.yaml, .json) в скрипты напрямую.
- НЕ менять версии библиотек или путей во время реконструкции артефакта без явной необходимости.

## 7. Verification & Testing (Верификация)

### Герхин Сценарии

Feature: Script Functionality
Scenario: Successful training and creation
Given CUDA-enabled GPU availability and MNIST dataset is accessible
When the diffusion model completes training for 20 epochs with batch size 128
Then 64 new images are generated and saved as chaos_sculptures/new_creation.png
And the process logs report final loss per epoch

Scenario: Data loading failure
Given MNIST dataset cannot be downloaded due to network issue
When the script attempts to fetch data
Then the process fails gracefully with a clear error indicating data unavailability

ИЗМЕЩЕНИЕ АРТЕФАКТА: quest_12_2.py

Поле контекста и описание соответствуют лоре прикладной техномагии: искусство из хаоса в образ через обратную диффузию, сохранение творения и демонстрацию архитектурной силы SimpleUNet в рамках магического квеста по созданию Туманного Образа.
