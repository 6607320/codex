# quest_16_2.py Specification

1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: mlflow, torch, torchvision, tqdm
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)
   Легенда: Этот артефакт соединяет ритуал отслеживания экспериментов с летописью научных достижений. Его задача — превратить хаос обучения MiniCNN на MNIST в воспроизводимую, запечатанную в Скрижали процедуру: фиксировать гиперпараметры, шаги обучения и финальный артефакт модели в Хрониках через MLflow. Назначение — показать, как R&D-процесс становится повторимым и проверяемым, превращая текущее исследование в совместимый след в библиотеке экспериментов.

3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema:
  interface InputData {
  learning_rate?: number
  batch_size?: number
  epochs?: number
  device?: "cpu" | "cuda"
  experiment_name?: string
  }

### 3.2. Outputs (Выходы)

- Destination: File
- Format: JSON
- Success Criteria: Exit code 0
- Schema:
  interface OutputResult {
  final_loss?: number
  metrics?: { [step: number]: number }
  model_path?: string
  mlflow_run_id?: string
  }

4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Подготовка хроник: выбрать и активировать скрижаль экспериментов MLflow, импортировать нужные артефакты машинного волшебства (Torch, torchvision и пр.), подготовить конвейер обработки изображений (ToTensor и Normalize).
2. Создание чертежа Голема: определить архитектуру MiniCNN с двумя свертками и полносвязным слоем, задать круги мыслей через forward путь и завернуть выход в лог-софтоммакс для классификации MNIST.
3. Настройка записи в хроники: определить экспериментальную страницу и начать новый экземпляр журнала with mlflow.start_run; зафиксировать гиперпараметры (learning_rate, batch_size, epochs) на скрижали.
4. Запуск ритуала наставления: перевести модель на устройство CUDA (или CPU по доступности); собрать оптимизатор Adam и функцию потерь CrossEntropyLoss; перевести модель в режим обучения.
5. Эпический цикл обучения: на каждом шаге подготавливать партию данных, переносить данные на устройство магии, обнулить градиенты, выполнить прямой проход, вычислить потерю, выполнить обратное распространение и шаг оптимизации.
6. Запись Хаоса в Хроники: каждые 100 шагов держать под контролем текущее значение потери и записывать его в журнал как метрику; сохранять последнюю потерю эпохи в качестве финальной.
7. Финализация артефакта: записать артефакт модели в Хроники через mlflow.pytorch.log_model, завершить запись и вывести сообщение о завершенности.
8. Ритуал завершен: подсказка запустить mlflow ui для просмотра результатов.

### 4.2. Declarative Content (Для конфигураций и данных)

- learning_rate: 0.01
- batch_size: 64
- epochs: 1
- dataset: MNIST
- transform: Compose(ToTensor, Normalize(0.1307, 0.3081))
- model: MiniCNN с контурами conv1 (1→16, ядро 3×3), conv2 (16→32, ядро 3×3), fc1 (32×7×7 → 10)
- device по умолчанию: cuda
- mlflow.experiment: "Первый Свиток Хроник"
- run_name: "Обучение MiniCNN"

5. Structural Decomposition (Декомпозиция структуры)

- Класс MiniCNN: определение архитектуры и метода forward
- transform: обработчик подготовки изображений
- train_loader: загрузчик MNIST с батчем 64
- mlflow интерфейсы: set_experiment, start_run, log_param, log_metric, pytorch.log_model
- переменные гиперпараметров: learning_rate, batch_size, epochs
- цикл обучения: эпохи, шаги, вычисление loss, backward и optimizer.step
- выводы: печать статуса ритуала и финальная ошибка
- артефакт: сохранение модели в подкаталог model внутри журнала

6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard GPU или CPU-fallback; исполнение на CUDA по возможности
- Concurrency: Синхронная обработка (одна эпоха за другой, шаг за шагом)
- Dependencies: mlflow, torch, torchvision, tqdm

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (использовать .env для настроек)
- DO NOT printRawDataToConsoleInProduction (ограничить вывод в продакшн)
- DO NOT use synchronous network calls in the main loop (избегать блокирующих сетевых вызовов внутри цикла)
- DO NOT wrap configuration files (.yaml, .json) into скрипты
- DO NOT менять версии библиотек или пути во время реконструкции

7. Verification & Testing (Верификация)

1) Гхеркин: успешное выполнение
   Feature: Script Functionality
   Scenario: Successful execution
   Given MLflow сервер доступен и GPU/CPU доступны
   When запускается файл quest_16_2.py
   Then создаётся новый Run в MLflow, логируются параметры, логируется loss на шагах и финальная loss, сохранён артефакт модели и путь к нему возвращён

2) Гхеркин: обработка отсутствия CUDA
   Feature: Script Functionality
   Scenario: Fallback к CPU при отсутствии CUDA
   Given CUDA недоступен
   When запускается файл quest_16_2.py
   Then обучение выполняется на CPU, параметры и метрики логируются, артефакт сохраняется и путь доступен

Инвентарь артефакта (Inventory) — предметы Летописного магазина:

- 🏰 mlflow Архивариус Экспериментов: set_experiment, start_run, log_param, log_metric, log_model
- 🛡️ MiniCNN Страж Архитектуры: класс нейронной сети с двумя свёртками и полносвязным слоем
- 🏰 Transform Литейный Гравировщик: transforms.Compose с ToTensor и Normalize
- 🏰 MNIST Учебный Грейд: датасет MNIST и train_loader
- 🏰 tqdm Индикатор Прогресса: прогресс-бар для эпох и шагов
- 🛡️ Adam Рунер: оптимизатор Adam, lr = learning_rate
- 🏰 CrossEntropy Вестник: функция потерь
- 🏰 CUDA/CPU Алтарь: устройство выполнения ("cuda") или CPU fallback
- 🏰 Скрижаль Скриптов: mlflow.log_param и mlflow.log_metric хроники
- 🏰 Модель Артефакт: mlflow.pytorch.log_model модель MiniCNN

Примечание по стилю: термины Script/Config/Data/Errors переведены в метафоры ритуалов: Ритуал, Скрижаль, Эфир, Хаос, чтобы звучать как часть Великой Книги Кодекса. Прямые фрагменты кода не вставлены, а логика описана словами шаг за шагом.
