quest_10_2_manual.py Specification

1. Meta Information

- Domain: ML/NLP
- Complexity: High
- Language: Python
- Frameworks: PyTorch, torchvision, torch.utils.data
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот файл представляет собой вручную сконструированную Башню Прозрения, в которой архитектура CNN собирается из нулевых рун MyLinear и MyConv вместо готовых слоев. Цель — продемонстрировать внутреннюю механику сверточной нейронной сети на примере MNIST, обучить голема без использования готовых модулей и затем провести экзамен на распознавание цифр. Instruction for AI: Понимай цель как объяснение «почему» кода — показать принцип сборки и обучения вручную, а не просто запустить готовый пример.

Легенда: В этом артефакте Архитектор выводит на свет ручной дизайн сверточной башни, чтобы любой ученик увидел, как из базовых рун рождается работающий Голем и как следует задавать этапы обучения и экзамена в царстве PyTorch.

3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
Format: JSON | Text | Binary | Stream
Schema:
interface InputData {
// В данном артефакте внешних входов нет явной конфигурации. Поля могут быть добавлены при интеграции, но текущий код полагается на внутренние defaults.
// Оставлено пустым намеренно.
}

### 3.2. Outputs (Выходы)

Destination: STDOUT
Format: Text
Success Criteria: Exit Code 0
Schema:
interface OutputResult {
summary?: string
finalLoss?: number
predictedClass?: number
actualLabel?: number
status?: string
}

4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Step-by-step ritual:

1. Вызовем Источник Маны — импортируем PyTorch и сопутствующие модули: nn, функциональные заклинания F, torchvision и DataLoader.
2. Воскрешаем рунные камни линейного слоя MyLinear: создаем обучаемую весовую руну и смещение, и задаем их форму через размеры входов/выходов.
3. Воскрешаем рунные камни свертки MyConv: создаем ядро свертки и смещение как обучаемые руны с заданными размерами (in_channels, out_channels, kernel_size).
4. Соединяем их в рукотворную башню MyManualCNN: первый этаж — свертка (conv1) с 1 входным каналом и 16 выходами; второй элемент — линейный модуль (fc1) для 10 вердиктов.
5. Определяем прямой проход башни: конвейер обработки — свертка, ReLU, MaxPooling, разворот в вектор и финальный линейный вердикт.
6. Через главный ритуал запускаем датасет MNIST: загрузчик данных из ./data, трансформации ToTensor, загрузку обучения с батчами 64 и перемешиванием.
7. Наставление имитирует обучение: выбираем CrossEntropyLoss как меру ошибки и Adam как наставника; обучаем модель 3 эпохи, шаги оптимизации повторяются для каждого батча.
8. По завершении эпох выводим метрику/ошибку и завершаем наставление.
9. Экзамен: загружаем тестовый набор MNIST, берем первую рукопись, переводим модель в режим оценки, выполняем предсказание и печатаем реальную цифру и вердикт Голема; если совпало — объявляется успешность, иначе — требуется доработка.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory of assets and exact data for 1-to-1 recreation:

- 🏰 Архитектура ручной башни: MyManualCNN (состоит из conv1 и fc1)
- 🏰 Рунные камни свертки: MyConv(in_channels=1, out_channels=16, kernel_size=3)
- 🏰 Рунные камни линейного слоя: MyLinear(in_features=16 _ 14 _ 14, out_features=10)
- 🏰 Главная оболочка процесса: forward-поток башни (conv1 -> ReLU -> MaxPool2d -> view -> fc1)
- 🏰 Датасет MNIST: root "./data", train=True/False, transform ToTensor, download=True
- 🏰 Дух-переносчик данных: DataLoader(train_dataset, batch_size=64, shuffle=True)
- 🏰 Наставник и карта ошибок: criterion = CrossEntropyLoss(), optimizer = Adam(model.parameters(), lr=0.001)
- 🏰 Эпохи обучения: 3
- 🏰 Экзамен: тестовый набор MNIST, выборка первой рукописи, режим eval, вывод вердикта

5. Structural Decomposition (Декомпозиция структуры)

- Импорт и настройка: импорт torch, nn, F, torchvision, DataLoader
- Чертежи рунных камней: класс MyLinear, класс MyConv
- Чертеж башни: класс MyManualCNN, конструктор строит conv1 и fc1
- Главный ритуал: главный блок if **name** == "**main**": загрузка данных, создание модели, настройка loss и оптимизатора
- Акт обучения: цикл по эпохам, пакетам данных, очистка градиентов, расчет потери, обратное распространение, шаг оптимизации
- Акт экзамена: загрузка тестовых данных, прогон по образцу, вывод вердиктов
- Сообщение о завершении: печать итоговых статусов

6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартный CPU-центр; нет специфических GPU-оптимизаций
- Concurrency: синхронный цикл обучения; DataLoader использует дефолтные настройки (нет явного multi-processing)
- Dependencies: PyTorch, torchvision, torch.utils.data (и их версии должны быть совместимы)

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде (использовать .env при реинтеграции)
- DO NOT выводить сырые данные в консоль в продакшн-режиме
- DO NOT выполнять синхронные сетевые вызовы в главном цикле без необходимости
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) в скрипты
- DO NOT изменять версии библиотек или пути во время реконструкции

7. Verification & Testing (Верификация)
   1-2 Gherkin сценария, описывающие счастливый путь и один путь с ошибкой.

Сценарий 1: Успешное обучение и экзамен
Функционал: Успешная настройка и тестирование Башни Прозрения
Контекст: Данные MNIST доступны в ./data; модель настроена согласно артефакту
Действие: Скрипт выполняется на 3 эпохи; затем проводится экзамен
Ожидаемый результат: Выводит итоговый вердикт, например: Вердикт ВЕРНЫЙ. 'Башня Прозрения' видит!

Сценарий 2: Ошибка загрузки данных
Функционал: Неполная или отсутствующая база данных MNIST
Контекст: Путь ./data недоступен
Действие: Скрипт запускается
Ожидаемый результат: Скрипт завершает работу с ошибкой (не нулевой код выхода) и сообщает об отсутствии данных

Итоговая легенда Артефакта: quest_10_2_manual.py — зримый манускрипт, в котором ручной конструктор башни собирается из базовых камней, чтобы продемонстрировать внутреннюю магию глубинного обучения и принципы работы CNN без готовых чудодейственных слоев.
