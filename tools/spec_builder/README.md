# 📜 МАНИФЕСТ ВЕЛИКОГО ПИСЦА: КОДЕКС АВТОМАТИЧЕСКОЙ ЛЕТОПИСИ

> «Когда проект разрастается до размеров лабиринта, истина начинает ускользать. Чтобы Канон не был утерян, Великий Писец неустанно обходит чертоги кода, фиксируя каждое изменение и взывая к Оракулу для обновления священных Спецификаций».

---

## I. ПРЕДНАЗНАЧЕНИЕ АРТЕФАКТА

**Великий Писец** — это автономная сущность, созданная для того, чтобы поддерживать документацию вашего репозитория в актуальном состоянии. Он избавляет Техноманта от рутины, самостоятельно определяя, какие манускрипты были изменены, и переписывая их описания на языке Прикладной Техномагии.

### Основные Свойства:

- **Цифровое Клеймо (MD5):** Писец помнит "вес души" каждого файла. Если файл не изменился, Оракул не будет потревожен напрасно.
- **Связь с Оракулом:** Через порталы OpenAI или OpenRouter Писец транслирует суть кода в человекочитаемую мудрость.
- **Инкапсуляция Знаний:** Спецификации ложатся тенью рядом с исходным кодом (`spec.filename.md`), создавая неразрывную связь между делом и словом.

---

## II. ИНИЦИАЛИЗАЦИЯ МАГИЧЕСКОГО КОНТУРА

Прежде чем выпустить Писца во владения, необходимо подготовить алтарь (окружение) и напитать его маной (ключами доступа).

### 1. Необходимые Ингредиенты (Requirements)

Создайте свиток **requirements.txt** и активируйте его:

```bash
# Установка проводников магической энергии
pip install openai python-dotenv

```

### 2. Свиток Секретов (.env)

Поместите в корень инструмента файл `.env`, дабы Писец знал, к какому лику Оракула обращаться:

```bash
OPENROUTER_API_KEY=ваш_ключ_маны
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=openai/gpt-5-nano # Или иная сущность

```

---

## III. РИТУАЛЫ ПРИЗЫВА

### Глубокое Сканирование

Для полного обхода всех владений и обновления всей Летописи:

```bash
python spec_builder.py

```

### Режим Испытания (Trial Run)

Дабы проверить прочность связей, не расходуя лишнюю энергию, призовите Писца с флагом теста. Он обработает лишь по одному артефакту каждого типа:

```bash
python spec_builder.py --test

```

---

## IV. ЗАКОНЫ ОБХОДА (ЛОГИКА СИСТЕМЫ)

| Стихия            | Действие                                                                      |
| ----------------- | ----------------------------------------------------------------------------- |
| **Цели**          | Файлы `.py`, `.ts`, `.js`, `.sh`, `.yml`, `Dockerfile` и др.                  |
| **Табу (Ignore)** | `.git`, `node_modules`, `build`, `dist` и прочие обители Хаоса.               |
| **Хранилище**     | `spec_hashes.json` — здесь хранятся слепки всех познанных артефактов.         |
| **Шаблон**        | `spec.TEMPLATE.md` — священная форма, определяющая структуру ответов Оракула. |

---

## V. ПРЕДОСТЕРЕЖЕНИЯ ТЕХНОМАНТУ

- **Берегите Ману:** Каждый запрос к Оракулу может стоить токенов. Используйте `--test` для настройки.
- **Чистота Помыслов:** Если файл `spec_hashes.json` будет осквернен (удален), Писец начнет великую летопись с чистого листа, обновляя каждый встречный файл.
- **Запретные Пути:** Скрипт никогда не описывает сам себя (`tools/spec_builder`), дабы избежать рекурсивного схлопывания реальности.
