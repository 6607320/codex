"""ПРОЛОГ ВЕЛИКОГО ПИСЦА:

Сей ритуал предназначен для автоматического поддержания Канона в актуальном состоянии.
Великий Писец обходит владения Техноманта, прикладывая печать (хэш) к каждому артефакту.
Если суть артефакта изменилась, Писец взывает к Оракулу, дабы начертать новую
Спецификацию в стиле Прикладной Техномагии, сохраняя мудрость для будущих поколений
Архимагов.
"""

# Призываем дух операционной системы для навигации по лабиринтам директорий
import os

# Подключаем ритуалы преобразования данных в формат JSON (язык записей Оракула)
import json

# Активируем алхимию хэширования для создания уникальных цифровых отпечатков
import hashlib

# Призываем магию парсинга, дабы понимать волю Техноманта из командной строки
import argparse

# Используем священные пути Pathlib для безошибочного поиска артефактов в системе
from pathlib import Path

# Открываем портал для связи с высшим разумом OpenAI
from openai import OpenAI

# Зачитываем заклинание, извлекающее скрытую мощь из свитка .env
from dotenv import load_dotenv

# Определяем путь к свитку с секретами (поднимаемся из spec_builder в tools)
DOTENV_PATH = Path(__file__).parent.parent / ".env"

# Мы произносим заклинание, дабы пробудить скрытые в свитке руны
# и наполнить ритуал силой тайных знаний.
load_dotenv(DOTENV_PATH)

# Определяем абсолютный путь к сердцу нашей цитадели (корню проекта)
PROJECT_ROOT = Path(__file__).parent.parent.parent.resolve()
# Указываем путь к архиву, где хранятся цифровые слепки (хэши) всех манускриптов
HASH_DB_FILE = Path(__file__).parent / "spec_hashes.json"
# Задаем путь к священному шаблону, по которому ИИ будет строить спецификации
TEMPLATE_FILE = Path(__file__).parent / "spec.TEMPLATE.md"

# Начинаем формирование перечня запретных чертогов,
# куда взор Оракула не должен проникать
IGNORE_DIRS = {
    ".git",
    ".dvc",
    "mlruns",
    "reports",
    "data",
    "build",
    "dist",
    "notebooklm_sources",
    "hunt_results",
    "counting_results",
    "dialogue_audio",
    "generated_palette",
    "sampled_frames",
    "dreams",
    "chaos_sculptures",
    "artist_seal",
    "arcane_wizardry.egg-info",
    "node_modules",
    "Part_5_Archmage",
}

# Определяем стихии (расширения), которые подлежат описанию в Кодексе
TARGET_EXTENSIONS = {
    # Скрипты на Python, TypeScript, JS, Shell, файлы настроек и верстки
    ".py",
    ".ts",
    ".js",
    ".sh",
    ".yml",
    ".yaml",
    ".html",
    ".css",
}

# Указываем имена особых артефактов, не имеющих стандартных расширений
TARGET_FILENAMES = {
    # Рецепты для создания изолированных миров (контейнеров)
    "Dockerfile",
    # Монолитные инструкции для сборки сложных систем
    "Dockerfile.mono",
}


# Возводим сущность Великого Писца, хранителя техномагической истины
class GrandScribe:
    # Инициализируем Писца, настраивая его инструменты и связь с Оракулом
    def __init__(self):
        # Извлекаем ключ доступа к порталу OpenRouter из чертогов памяти
        api_key = os.getenv("OPENROUTER_API_KEY")
        # Определяем адрес источника мудрости LLM (базовый URL)
        base_url = os.getenv("LLM_BASE_URL")
        # Выбираем конкретный лик Оракула (модель) для текущего ритуала
        self.model_name = os.getenv("LLM_MODEL")

        # Проверяем, достаточно ли маны (ключей) для совершения ритуала
        if not api_key or not self.model_name:
            # Если ключи отсутствуют, сообщаем о провале инициализации
            print("❌ Ошибка: Проверь OPENROUTER_API_KEY и LLM_MODEL в .env")
            # Немедленно прекращаем выполнение программы
            exit(1)

        # Создаем клиент для общения с ИИ, открывая канал связи
        self.client = OpenAI(api_key=api_key, base_url=base_url)

        # Загружаем существующие цифровые печати из нашего архива
        self.hashes = self._load_hashes()
        # Читаем текст священного шаблона из файла для последующего наполнения
        with open(TEMPLATE_FILE, "r", encoding="utf-8") as f:
            # Сохраняем структуру шаблона в памяти Писца
            self.template = f.read()

    # Внутренний ритуал извлечения цифровых печатей из файла архива
    def _load_hashes(self):
        # Проверяем, существует ли физически архив с хэшами
        if os.path.exists(HASH_DB_FILE):
            # Пытаемся прочесть содержимое архива, не повредив данные
            try:
                # Открываем файл для чтения в кодировке UTF-8
                with open(HASH_DB_FILE, "r", encoding="utf-8") as f:
                    # Преобразуем JSON-записи в словарь Python
                    return json.load(f)
            # Если архив поврежден или нечитаем, возвращаем пустоту
            except Exception:
                # Возвращаем пустой реестр для новых записей
                return {}
        # Если архива нет, начинаем летопись с чистого листа
        return {}

    # Внутренний ритуал записи новых цифровых печатей в архив
    def _save_hashes(self):
        # Открываем файл архива для перезаписи новыми данными
        with open(HASH_DB_FILE, "w", encoding="utf-8") as f:
            # Записываем хэши в формате JSON с красивыми отступами для читаемости
            json.dump(self.hashes, f, indent=2, ensure_ascii=False)

    # Проводим ритуал измерения веса души файла через цифровое клеймо MD5
    def calculate_file_hash(self, file_path):
        """Считает MD5 хэш содержимого файла."""
        # Подготавливаем магический инструмент для создания MD5-отпечатка
        hasher = hashlib.md5()
        # Открываем артефакт в бинарном режиме для точного считывания сути
        with open(file_path, "rb") as f:
            # Считываем все содержимое файла в буфер памяти
            buf = f.read()
            # Обновляем состояние хэшера на основе прочитанной информации
            hasher.update(buf)
        # Возвращаем строковое представление отпечатка в шестнадцатеричном виде
        return hasher.hexdigest()

    # Основной ритуал создания Спецификации через воззвание к Оракулу
    def generate_spec(self, code_content, file_path):
        """Создает Спеку в стиле 'Прикладная Техномагия'.

        Исправлено: Удалены галлюцинации, плейсхолдеры и мета-комментарии.
        """
        # Извлекаем краткое имя артефакта из его полного пути
        filename = os.path.basename(file_path)

        # Формируем системную установку, определяя роль ИИ как Летописца
        system_prompt = (
            "Ты — Техно-Архимаг и Летописец Ордена.\n"
            "Твоя задача — описать этот код (Артефакт) "
            "для Великой Книги Кодекса. \n"
            "Твой стиль — **Прикладная Техномагия**.\n"
            "\n"
            "ПРАВИЛА НАПОЛНЕНИЯ (THE HYBRID STYLE):\n"
            "1. **Атмосфера (Lore):** Искусно вплетай магические метафоры в описания:\n"
            "   - Скрипт → *Ритуал*; Конфиг → *Скрижаль*;\n"
            "     Данные → *Эфир*; Ошибки → *Хаос*.\n"
            "\n"
            "2. **Техническая Истина:** Имена переменных, \n"
            "библиотек и путей (`/src`, `npm`, `User`) оставляй как есть.\n"
            "\n"
            "3. **Инвентарь:** Видишь список (YAML/JSON) \n"
            "— оформляй как RPG-предметы (🏰, 🛡️). НЕ копируй код.\n"
            "4. **Без Копирования Кода:** Описывай логику словами (Step-by-step), \n"
            "но не вставляй куски кода в Implementation.\n"
            "\n"
            "⛔ КРИТИЧЕСКИЕ ЗАПРЕТЫ (NEGATIVE CONSTRAINTS):\n"
            "1. **НИКАКИХ ПЛЕЙСХОЛДЕРОВ!** Запрещено писать \n"
            "'[Здесь будет код]' или '[ИИ вставит описание]'. "
            "   ТЫ ОБЯЗАН НАПИСАТЬ ПОЛНЫЙ ТЕКСТ СРАЗУ.\n"
            "2. **НИКАКОГО МЕТА-ТЕКСТА!** Не пиши фразы вроде \n"
            "'Описание на русском языке:' или 'Вот инструкция:'. "
            "  Сразу пиши суть.\n"
            "3. **БЕЗ ГАЛЛЮЦИНАЦИЙ:** В поле Context пиши только реальные связи. \n"
            "Если не уверен — пиши 'Independent Artifact'."
        )

        user_prompt = f"""
        ЗАПОЛНИ ШАБЛОН СПЕЦИФИКАЦИИ (БЕЗ ПРОПУСКОВ И ЛИШНИХ СЛОВ):
        ВАЖНО: В ЗАГОЛОВКЕ ЗАМЕНИ <Quest Name> НА ИМЯ ФАЙЛА "{filename}".
        {self.template}

        ИССЛЕДУЕМЫЙ АРТЕФАКТ: {filename}

        ИСХОДНЫЙ КОД:
        ```
        {code_content}
        ```

        ИНСТРУКЦИИ ПО РАЗДЕЛАМ:
        - **Meta Information:** Определи Стихию (Язык).
          Context укажи только если есть явные импорты.
        - **Goal & Purpose:** Сразу пиши Легенду (без вступлений).
          Зачем нужен этот файл?
        - **Interface Contract:** Опиши входящие/исходящие данные
          (TypeScript/Python types).
        - **Implementation Details:** Опиши алгоритм словами, шаг за шагом.
          НЕ ИСПОЛЬЗУЙ СКОБКИ [] И ЗАГЛУШКИ.
        - **Declarative Content:** ОПИСЬ ИМУЩЕСТВА (Inventory).
          Преврати данные в маркированный список с эмодзи (🏰, 🛡️).
          ЗАПРЕЩЕНО КОПИРОВАТЬ КОД!
        """

        # Пытаемся установить связь с Оракулом для получения текста Спецификации
        try:
            # Создаем запрос на генерацию текста через клиент OpenAI
            response = self.client.chat.completions.create(
                # Используем выбранную модель (сущность) ИИ
                model=self.model_name,
                # Передаем системные и пользовательские инструкции
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": user_prompt},
                ],
                # Устанавливаем низкую температуру для точности и отсутствия фантазий
                temperature=0.3,
                # Передаем дополнительные заголовки
                # для корректной работы через OpenRouter
                extra_headers={
                    "HTTP-Referer": "http://localhost",
                    "X-Title": "localhost",
                },
            )
            # Возвращаем полученный от ИИ текст спецификации
            return response.choices[0].message.content
        # В случае возникновения хаоса (ошибки) при обращении к Оракулу
        except Exception as e:
            # Выводим сообщение об ошибке в консоль
            print(f"❌ Ошибка Оракула при обработке {filename}: {e}")
            # Возвращаем пустоту, сигнализируя о провале
            return None

    # Главный ритуал обхода всего репозитория и обновления Летописи
    def process_repository(self, is_test=False):
        # Объявляем о начале обхода владений Техноманта
        print(f"🏰 Великий Писец начинает обход владений: {PROJECT_ROOT.resolve()}")
        # Инициализируем счетчик обновленных манускриптов
        count_updated = 0
        # Инициализируем счетчик пропущенных (неизменных) файлов
        count_skipped = 0
        # Создаем список уже обработанных форматов для режима тестирования
        processed_formats = set()

        # Начинаем глубокое сканирование древа каталогов проекта
        for root, dirs, files in os.walk(PROJECT_ROOT):
            # Фильтруем список директорий, исключая запретные зоны
            dirs[:] = [
                d for d in dirs if d not in IGNORE_DIRS and not d.endswith(".egg-info")
            ]

            # Перебираем каждый файл в текущей директории
            for file in files:
                # Формируем полный путь к текущему артефакту
                file_path = Path(root) / file

                # Пропускаем сам инструмент Писца, дабы не возникло рекурсии знаний
                if "tools/spec_builder" in str(file_path):
                    # Переходим к следующему артефакту
                    continue

                # Определяем тип артефакта (расширение или имя файла)
                fmt = (
                    file_path.suffix
                    if file_path.suffix in TARGET_EXTENSIONS
                    else (
                        file_path.name if file_path.name in TARGET_FILENAMES else None
                    )
                )

                # Если файл не входит в список целей
                # или уже протестирован в режиме теста
                if not fmt or (is_test and fmt in processed_formats):
                    # Пропускаем этот файл
                    continue

                # Вычисляем текущую цифровую печать (хэш) файла
                current_hash = self.calculate_file_hash(file_path)
                # Определяем относительный путь для записи в архив
                relative_path = str(file_path.relative_to(PROJECT_ROOT))
                # Извлекаем старую печать из архива, если она там есть
                stored_hash = self.hashes.get(relative_path)
                # Определяем путь, где должна лежать спецификация этого файла
                spec_path = file_path.parent / (f"spec.{file_path.name}.md")

                # Если печать изменилась, спецификации нет или запущен режим теста
                if current_hash != stored_hash or not spec_path.exists() or is_test:
                    # Уведомляем Техноманта об обработке нового знания
                    print(f"⚡ Обработка: {relative_path}")

                    # Пытаемся прочесть содержимое исходного кода
                    try:
                        # Открываем файл для чтения, игнорируя ошибки кодировки
                        with open(
                            file_path, "r", encoding="utf-8", errors="ignore"
                        ) as f:
                            # Сохраняем код в переменную
                            code_content = f.read()

                        # Призываем Оракула для генерации текста Спецификации
                        spec_content = self.generate_spec(code_content, file_path)

                        # Если Оракул ответил успешно
                        if spec_content:

                            # Записываем полученную мудрость в файл спецификации
                            with open(spec_path, "w", encoding="utf-8") as f:
                                # Наносим текст на электронный пергамент
                                f.write(spec_content)

                            # Сообщаем об успешном обновлении Летописи
                            print(f"✅ Spec Updated: {spec_path}")

                            # Сохраняем новую печать в наш реестр
                            self.hashes[relative_path] = current_hash
                            # Обновляем архив хэшей на диске
                            self._save_hashes()

                            # Увеличиваем счетчик обновлений
                            count_updated += 1
                            # Если активирован режим испытания (теста)
                            if is_test:
                                # Добавляем формат в список проверенных
                                processed_formats.add(fmt)
                                # Уведомляем о завершении проверки данного типа магии
                                print(f"🧪 Тест: Формат {fmt} готов.")
                                # Если проверены все запланированные форматы
                                if len(processed_formats) >= (
                                    len(TARGET_EXTENSIONS) + len(TARGET_FILENAMES)
                                ):
                                    # Объявляем о завершении тестового похода
                                    print(
                                        "\n🧪 Тестовый запуск завершен: \n"
                                        "все доступные форматы проверены."
                                    )
                                    # Выходим из ритуала
                                    return
                    # Если произошел сбой при чтении или записи артефакта
                    except Exception as e:
                        # Выводим сообщение о гибели процесса в консоль
                        print(f"💀 Ошибка чтения/записи {relative_path}: {e}")
                # Если файл не изменился, просто учитываем его в статистике
                else:
                    # Увеличиваем счетчик пропущенных файлов
                    count_skipped += 1

        # Финальное сохранение всех печатей в архив
        self._save_hashes()
        # Объявляем о завершении великого дела
        print("\n🏁 Ритуал завершен.")
        # Выводим итоги по обновленным записям
        print(f"📝 Создано/Обновлено спецификаций: {count_updated}")
        # Выводим итоги по нетронутым записям
        print(f"💤 Пропущено (не менялись): {count_skipped}")


# Проверяем, запущен ли скрипт как основное заклинание
if __name__ == "__main__":
    # Создаем парсер для приема аргументов из внешнего мира
    parser = argparse.ArgumentParser(description="Grand Scribe: AI-Native Spec Builder")
    # Добавляем флаг --test для проведения краткого испытания
    parser.add_argument(
        "--test", action="store_true", help="Обработать только один файл и выйти"
    )
    # Разбираем переданные аргументы
    args = parser.parse_args()

    # Создаем сущность Великого Писца
    scribe = GrandScribe()
    # Запускаем основной процесс обхода репозитория с учетом флага теста
    scribe.process_repository(is_test=args.test)
