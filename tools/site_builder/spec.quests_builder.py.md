# quests_builder.py Specification

## 1. Meta Information

- Domain: Скрипты и ритуалы сборки данных (Scripting)
- Complexity: Medium
- Language: Python
- Frameworks: Python-Markdown (для конвертации Markdown в HTML)
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Великий Ритуал Сборки Финальной Страницы объединяет рассеянные источники истины CODEX.md, manifest.md и сценарии scenarios, чтобы выковать единый артефакт на сайте Кодекса. Он применяет инкрементальную сборку по хешам: если отпечаток текущего архива совпадает с записанным в кэше, сборка пропускается, экономя время и ресурсы. Итоговый набор артефактов размещается в static/ quests, сопровождается индексом и кэшем.

**Legend for AI:** Этот модуль превращает фрагменты информации в новые, единые страницы квестов, готовые к отображению на сайте Кодекса, используя последовательную логику проверки изменений и безопасного сохранения артефактов.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON | Text | Binary | Stream
- Schema: В явном виде заданные входные данные не имеют фиксированного набора полей в коде; структура может включать любые ключи, используемые оболочкой для передачи параметров во время исполнения.

Примерная TypeScript-форма интерфейсов (для контрактной интеграции):

- InputData
  - [key: string]: any
- OutputResult
  - [key: string]: any

### 3.2. Outputs (Выходы)

- Destination: STDOUT | File | API Response
- Format: JSON
- Success Criteria: Exit code 0
- Schema: Обобщенная структура вывода может включать произвольные поля, зависящие от результатов сборки.

Примерная TypeScript-форма интерфейсов (для контрактной интеграции):

- OutputResult
  - [key: string]: any

## 4. Implementation Details (The Source DNA / Исходный Код)

Суть алгоритма выражается в последовательности ритуальных шагов, без показа кода, чтобы сохранить мистическую целостность артефакта.

1. Подготовка пространства и путей

- Определяем путь к директории скрипта, затем поднимаемся к корню проекта.
- Задаем директории для артефактов сайта, для полных данных квестов и для сценариев.
- Определяем файлы кэша и индексного сводника. На кристалле консоли выводим корень проекта.

2. Магия вычисления отпечатков

- Есть чертеж calculate_hash, который принимает текстовую совокупность данных и возвращает уникальный отпечаток через MD5.
- Отпечаток служит индикатором изменений для каждого квеста.

3. Кэша и его дух хранителя

- load_cache загружает хранилище прошлой сборки из файла или возвращает пустой словарь, если кэш отсутствует.
- save_cache сохраняет новую версию кэша в файл.

4. Разбор Великих Летописей

- parse_codex читает CODEX.md, ищет разделы, относящиеся к Частям и Свиткам, и извлекает список квестов.
- Для каждого квеста формируются поля идентификатор, золото названия, номер Части, название Части, имя Свитка, легенда в формате Markdown и HTML, статус по умолчанию заблокирован.
- Легенда собирается из скрытых комментариев внутри квеста, конвертация Markdown в HTML выполняется с поддержкой кода и таблиц.

5. Построение карты соответствий и обход Цитадели

- Создаем карту соответствия ID квеста и файловой системы, проходя по дереву проекта, исключая служебные директории.
- Ищем папки с форматом Scroll_X/Quest_Y и наличие MANIFEST.md, чтобы связать квест с конкретной директорией.

6. Генерация артефактов и инкрементальная сборка

- Для каждого квеста извлекаем контент манифеста и сценария. МанIFEST.md превращается в HTML с Markdown-расширениями.
- Сценарий читается из файла JSON в SCENARIOS_DIR. Поддерживаются два формата: новый формат с полем scenario и старый формат в виде массива.
- Объединяем данные квеста и сценария в строковую совокупность и вычисляем текущий отпечаток.
- Если отпечаток изменился по сравнению с сохраненным в кэше или файл артефакта ещё не существует, создаем полный артефакт с полями id, title, legend, manifest, status и сценарий, и сохраняем его в static/ quests/quest\_{id}.json.
- Обновляем счетчик обновленных квестов.

7. Индекс сайта и финальный кэш

- Формируем сводный индекс всех квестов с идентификатором, названием, номером и названием Части, заголовком Свитка и статусом, и сохраняем его в static/index.json.
- Сохраняем обновленный кэш подписи сборки.
- Выводим на консоль итог о количестве обновленных квестов.

8. Призыв к запуску

- Когда модуль запущен как главный скрипт, инициируется главный ритуал сборки.

## 5. Structural Decomposition (Декомпозиция структуры)

- 🏰 SCRIPT_DIR: путь к исполняемому скрипту
- 🏰 PROJECT_ROOT: путь к корню проекта
- 🏰 OUTPUT_DIR: директория артефактов сайта
- 🏰 QUESTS_DIR: директория с данными по каждому квесту
- 🛡️ INDEX_FILE: файл старшего индекса
- 🛡️ CACHE_FILE: файл кэша сборки
- 🗝️ SCENARIOS_DIR: директория со сценариями
- 🗺️ calculate_hash: ритуал вычисления отпечатка данных квеста
- 🗺️ load_cache: страж кэша — загрузка прошлой сборки
- 🗺️ save_cache: страж кэша — сохранение новой сборки
- 🗺️ parse_codex: Великая Летопись парсится из CODEX.md во структуры квестов
- 🗺️ build: главный ритуал сборки, воплощение всего артефакта
- 🎯 quest_fs_map: карта файловой системы для каждого квеста
- 🎯 quests_list: список квестов, полученный из кодекса
- 🎯 manifest_content, scenario_data: содержимое манифеста и сценария
- 🎯 current_hash, new_cache: отпечаток текущей сборки и обновленный кэш
- 🎯 json_path: путь к артефкту на диске
- 🎯 index_data: сводная информация для индекса сайта
- 🎯 final_index: итоговый индекс в виде словаря

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Инкрементальная сборка, пропускающая повторные квесты через кэш
- Concurrency: Однопоточная/синхронная реализация (нет асинхронности, цикл по квестам)
- Dependencies: Python 3.x, пакет Python-Markdown для конвертации Markdown в HTML
- Файловая система: артефакты пишутся в директорию static, манифесты считываются из MANIFEST.md, сценарии из файла quest_X.json
- Путь и константы: фиксированные пути к корню проекта, кэшу, индексам и сценариям, которые не должны произвольно изменяться во время реконструкции

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде — используйте безопасные способы конфигурации (.env или аналог).
- DO NOT выводить на продакшн бесшовные данные или сырые данные в консоль без необходимости.
- DO NOT выполнять блокирующие сетевые вызовы в главном цикле сборки.
- DO NOT оборачивать конфигурационные файлы в скрипты или иным образом нарушать структуру проекта.
- DO NOT менять версии библиотек или фиксированные пути во время реконструкции.
- DO NOT игнорировать отсутствие CODEX.md без надлежащей обработки ошибок — сборка должна завершаться корректно, возвращая пустой набор квестов, если источники недоступны.

## 7. Verification & Testing (Верификация)

Геркин сценарии

Функционал: сборка артефактов квестов

Сценарий 1. Успешная инкрементальная сборка

- Контекст: есть валидный CODEX.md с обновленным квестом или изменениями в манефесте/сценарии
- Действие: выполнить скрипт сборки
- Ожидаемо: созданы или обновлены артефакты для измененных квестов в static/quests, индекс обновлен в static/index.json, кэш обновлен, остальные квесты не изменены

Сценарий 2. Отсутствие исходников

- Контекст: CODEX.md отсутствует
- Действие: выполнить скрипт сборки
- Ожидаемо: не создаются новые артефакты, не изменяются существующие, сборка завершается корректно с пустым набором квестов

Сценарий 3. Невалидный сценарий

- Контекст: файл quest_X.json существующий, но содержит битые данные
- Действие: выполнить скрипт сборки
- Ожидаемо: обработка исключения при чтении сценария, пустой сценарий для квеста, кэш обновляется только для тех квестов, которые успешно обработаны

Итоговый формат горизонтальных проверок соответствия артефактов и индексов подтверждается выводами на консоль и наличием обновленного кэша.
