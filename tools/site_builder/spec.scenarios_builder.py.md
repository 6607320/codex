# scenarios_builder.py Specification

## 1. Meta Information

- **Domain:** <Scripting>
- **Complexity:** <Medium>
- **Language:** <Python>
- **Frameworks:** <OpenAI, python-dotenv>
- **Context:** ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Генератор финальных JSON-артефактов сценариев для квестов, который берет легенды и связанный код квестов из проекта, вычисляет магическую подпись входных данных, обращается к оракулу через набор моделей и сохраняет результат на диск, избегая повторной генерации при неизменности входных данных.

**Instruction for AI:** Этот модуль обеспечивает интерацию с Оракулом для преобразования текстовых легенд и реального кода квестов в структурированный JSON-скелет сценариев, управляет кешированием на основе подписи и записывает артефакт в файловую систему.

Вектор легенд и кода объединяется в единственный запрос к промпту, который затем отправляется в OpenAI через реестровый набор моделей. Результат очищается и сохраняется как итоговый артефакт с метаданными.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** <CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call>
- **Format:** <JSON | Text | Binary | Stream>
- **Schema:**

```ts
interface InputData {
  q_id: string;
  legend: string;
  code: string;
}
```

### 3.2. Outputs (Выходы)

- **Destination:** <STDOUT | File | Database | API Response | Event Log>
- **Format:** <JSON | CSV | Text>
- **Success Criteria:** <Exit Code 0 | 200 OK | File Created>
- **Schema:**

```ts
interface OutputResult {
  targetFile: string;
  artifact?: {
    _meta: { input_hash: string };
    scenario: any[];
  };
  status: "created" | "skipped" | "failed";
  message?: string;
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Определяются магические константы и пути к ритуалам Скрижали: BASE_DIR как путь к текущему скрипту, PROJECT_ROOT как корень проекта, SCENARIOS_DIR как место хранения артефактов, PROMPT_FILE как внешний свиток с инструкциями, CODEX_FILE как Великая Летопись CODEX.md, и DOTENV_PATH как путь к секретам. Загружаются секреты из окружения через dotenv. Затем опрашиваются секреты: OPENROUTER_API_KEY и OPENROUTER_BASE_URL. Если ключ не найден, ритуал прерывается с предупреждением. Из окружения извлекаются MODELS_LIST; если оно задано, формируется список моделей, иначе используется базовый набор по умолчанию.

2. Создается директория для артефактов, если она отсутствует. Затем инициализируется клиент для общения с Оракулом через OpenAI с дополнительными заголовками, включая ссылку на источник и название ритуала.

3. Реализованы шесть ритуалов-подразделов, каждое из которых трактует свою задачу:

- calculate_input_hash: объединяет легенду и код квеста и вычисляет MD5-хеш — это Магическая Подпись входных данных.
- parse_codex_legends: читает CODEX.md и извлекает легенды по каждому квесту в виде пары (q_id -> legend).
- get*quest_code: ищет папку квеста по шаблону Scroll*<s*num>Quest*<q_num>, собирает код из файлов проекта (py, shell, Dockerfile и пр.), а также собирает содержимое связанных CI/CD рабочих процессов, если они существуют. Возвращает конкатенованный текст кода.
- generate_scenario: читает промпт template, заполняет его легендой и кодом, обращается к Оракулу через список моделей в MODELS, обрабатывает ошибки лимитов и отклонения, сохраняет имя последнего успешного Оракула для примарного повторного использования и возвращает текст ответа Оракула.
- clean_json: агрессивно очищает ответ Оракула, извлекая допустимый JSON даже если он обернут в markdown или другой текст.
- main: для каждого квеста из CODEX формирует путь артефакта, собирает код, вычисляет подпись, сравнивает с существующим артефактом (если файл уже существует и подпись совпадает, пропускает повторную генерацию). Если артефакт требует обновления, вызывает generate_scenario, очищает ответ, строит окончательный артефакт с полями \_meta и scenario, записывает JSON на диск и сообщает об успехе.

4. Главный поток запуска выполняет главную логику, проходя по всем квестам, создавая или обновляя артефакты.

5. Входной и выходной цикл реализован синхронно и без блокирующих зависимостей, с защитой от ошибок чтения файлов и сетевых исключений; при неудачах логи информируют пользователя и продолжают попытки следующего Оракула.

6. Точки контроля ошибок: при отсутствии файла промпта выдаётся критическая ошибка и ритуал прерывается; при любой непредвиденной ошибке чтение промптов и взаимодействие с Оракулом корректируется, чтобы не "затирать" систему.

7. В точке входа: если скрипт запускается напрямую, выполняется главный ритуал.

### 4.2. Declarative Content (Для конфигураций и данных)

- Инвентарь и артефакты, которые потребляются и создаются скриптом.
- Конфигурационные скрипты и файлы, которые скрипт читает и объединяет в единый кодекс квеста.

Инвентарь артефакта (Inventory) в виде RPG-элементов:

🏰 BASE_DIR — абсолютный путь к текущему пергаменту.
🏰 PROJECT_ROOT — корень всей Цитадели проекта.
🏰 SCENARIOS_DIR — место хранения артефиктов сгенерированных сценарием.
🏰 PROMPT_FILE — внешний свиток с инструкциями для Оракула.
🏰 CODEX_FILE — Великая Летопись CODEX.md, откуда берутся легенды.
🏰 DOTENV_PATH — путь к секретным рунам файла .env.
🛡️ OPENROUTER_API_KEY — магический ключ к порталу (системная секретность).
🛡️ OPENROUTER_BASE_URL — адрес портала Оракула.
🛡️ MODELS — список имен моделей Оракула, к которым отправляются послания.
🪄 client — Клиент для общения с Оракулом (OpenAI).
🪄 LAST_SUCCESSFUL_MODEL — имя последнего успешного Оракула, занесённое в кристалл памяти.
📜 Legends, Code — внутренние тексты Легенд и кода квеста, которые собираются и передаются в ритуал.
🧭 input_hash — Магическая Подпись входных данных для проверки кеша.
📜 prompt_template.txt — артефакт промпта, используемый для формирования запроса к Оракулу.
🗝️ CODEX.md — Великая Летопись в тексте проекта.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции:
  - calculate_input_hash
  - parse_codex_legends
  - get_quest_code
  - generate_scenario
  - clean_json
  - main
- Глобальные константы/креаторы состояния:
  - LAST_SUCCESSFUL_MODEL
  - BASE_DIR
  - PROJECT_ROOT
  - SCENARIOS_DIR
  - PROMPT_FILE
  - CODEX_FILE
  - DOTENV_PATH
  - OPENROUTER_API_KEY
  - OPENROUTER_BASE_URL
  - MODELS
  - client

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартная загрузка на CPU, ориентирована на последовательное выполнение (синхронно).
- Concurrency: синхронная обработка; внутренний цикл пробует модели последовательно.
- Dependencies: OpenAI, python-dotenv, стандартная библиотека Python (os, json, re, glob, hashlib, time).

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде; используйте .env и переменные окружения.
- DO NOT печатать чувствительные данные или полный контент ответов в продакшн-логах; логируйте безопасно.
- DO NOT выполнять сетевые вызовы синхронно в критических путях без учёта лимитов; обрабатывайте 429 и делайте паузы.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) внутрь скриптов.
- DO NOT изменяйте версии библиотек или пути к файлам во время реконструкции артефакта без явного контроля.

## 7. Verification & Testing (Верификация)

### 7.1. Gherkin scenarios

```gherkin
Feature: Scenarios Builder Script

  Scenario: Successful generation for a quest
    Given CODEX.md содержит легенды и существует код квеста 1.1 в проекте
    And промпт файл prompt_template.txt доступен
    When скрипт запускается и Оракул возвращает валидный JSON
    Then на диск записывается артефакт quest_1.1.json
    And артефакт содержит _meta.input_hash, совпадающий с вычисленной подписью
    And артефакт содержит непустой массив scenario

  Scenario: Missing prompt file
    Given промпт файл prompt_template.txt отсутствует
    When скрипт запускается
    Then выполнение прерывается с сообщением об ошибке и без сбоев
```

Это описание артефакта scenarios_builder.py следует рассматривать как рукопись архимага техномагии: он сообщает, как система следует работать, какие данные принимают и возвращают, какие ритуалы применяются для добычи смысла из легенд и кода, и как артефакты укладываются в Скрижаль для будущей трактовки Великим Советом.
