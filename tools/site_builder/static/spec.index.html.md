# index.html Specification

## 1. Meta Information

- **Domain:** Scripting
- **Complexity:** Low
- **Language:** TypeScript
- **Frameworks:** None (Vanilla JS)
- **Context:** Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот файл служит входной дверью в Великий Кодекс Техномагии — статическая страница, которая объединяет заголовок, легендарную навигацию и ссылки на внешние источники, оформляет визуальный облик через скрижаль стилей и связывает миры через скриптовый ритуал app.js. Зачем нужен этот файл: представить удобный портал для чтения и навигации поCodex с минимальной динамикой и ясной структурой, сохраняя стили, подписи и ссылки в едином артефакте.

- Context for Creator: Опишите бизнес-задачу кратко. Что делает этот модуль?
- Instruction for AI: This section provides high-level intent. Use it to understand the "WHY" of the code.

Этот HTML–артефакт задаёт основу для портального интерфейса: заголовок, кнопки навигации, ссылка на GitHub и место для внутреннего контента, который может развиваться через внешний JS-ритуал.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
- **Format:** JSON | Text | Binary | Stream
- **Schema:**
  InputData
  - Source: строка, выбираемая из перечисления
  - Format: строка, выбираемая из перечисления
  - payload: произвольный объект (данные эфира скрипта)

### 3.2. Outputs (Выходы)

- **Destination:** STDOUT | File | Database | API Response | Event Log
- **Format:** JSON | CSV | Text
- **Success Criteria:** Exit Code 0 | 200 OK | File Created
- **Schema:**
  OutputResult
  - destination: строка из перечисления
  - format: строка из перечисления
  - success: boolean
  - code: число (необязательно)
  - message: строка (необязательно)

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. В начале ритуала устанавливается DOCTYPE и кодировка UTF-8 для ясной передачи послания сквозь эфир.
2. Пролог страницы создаёт корень мира HTML: html, head и body, соединяя их с мета данными и заголовками.
3. В шкатулке head добавляются ссылки на скрижаль стилей css/style.css и задаётся адаптивная сетка через viewport.
4. В Материальном Плане тела рождается скрытая зона для Хаоса ошибки и контейнер для навигационного компаса с кнопкой Летопись и именем Кодекса.
5. Центральный Медальон навигации воспроизводит зов к внешним источникам: ссылка на Летопись index.html и GitHub репозиторий codex.
6. Алтарь app содержит магическую пипетку для динамики: место для будущего явления Домашней Летописи и первого вздоха скрипта app.js.
7. В конце ритуала подключается скрипт app.js для оживления интерфейса и связывания элементов DOM с поведением.
8. Комментарии и структура страницы сохраняются как зеркало смысла и будущих обновлений, без внедрения лишнего кода внутри текста.

### 4.2. Declarative Content (Для конфигураций и данных)

ИСТОЧНИКИ ДАННЫХ ЭЛЕМЕНТНОЙ СКИНИИ или Скрижаль Взаимодействия:

- 🏰 index.html — Главный ритуал страницы, базовая структура документа: doctype, html, head, body, meta charset, title, link stylesheet, meta viewport, div error-message, nav, div app и script js/app.js.
- 🛡️ css/style.css — Скрижаль стиля, задаёт визуальное оформление и адаптивность элементов.
- 🛡️ js/app.js — Скрижаль поведения, подключение к Магическому Алтарю и инициализация динамики DOM.
- 🛡️ GitHub ↗ (https://github.com/6607320/codex) — Внешний портал к исходным рунам проекта.
- 🏰 Летопись index.html — Главная кнопка навигации, возвращающая к корневой странице и поддерживающая единый стиль интерфейса.
- 🛡️ Метаданные страницы — Включают заголовок Великий Кодекс Техномагии, язык и параметры отображения.

## 5. Structural Decomposition (Декомпозиция структуры)

- html — корень всего артефакта
- head — инкубатор метаданных
  - meta charset UTF-8
  - title Великий Кодекс Техномагии
  - link rel stylesheet href css/style.css
  - meta name viewport content width=device-width initial-scale=1.0
- body — материальный план духа страницы
  - div id error-message
    - h1 class error-text
  - nav — магический компас навигации
    - div class nav-content
      - a href index.html class btn-magic data-text Летопись
      - div class nav-title
        - h1 Великий Кодекс Техномагии
      - a href GitHub ↗ … — портал к исходным рунам codex
  - div id app — Великий Алтарь явления
    - div class home-scroll-container fade-in
      - div id quest-list-anchor
- script src js/app.js — призыв к ритуалу поведения

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Standard CPU на стороне клиента; ритуал минимален, энергозатраты невысоки.
- **Concurrency:** Однопоточный цикл событий веб-страницы; асинхронные операции возможны внутри app.js, но основной ритуал остаётся синхронным.
- **Dependencies:** css/style.css и js/app.js являются нерушимыми скрижалями, обращающими внешний мир к внутренним концепциям.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в плоском виде внутри HTML или скриптов.
- DO NOT печатать сырые данные в консоль в боевом режиме.
- DO NOT использовать синхронные сетевые вызовы в критических путях ритуала.
- DO NOT заключать конфигурационные файлы YAML или JSON внутрь скриптов.
- DO NOT изменять версии или пути к ключевым ресурсам во время реконструкции.

## 7. Verification & Testing (Верификация)

Feature: Index html interface

Scenario: Successful page load
Given браузер запрашивает index.html
When DOM готов к взаимодействию
Then отображается навигация с Летопись и ссылкой GitHub, заголовок "Великий Кодекс Техномагии" видим, сообщение об ошибке скрыто

Scenario: Missing assets cause error overlay
Given файл css/style.css или js/app.js недоступен
When страница загружена
Then элемент error-message отображает предупреждение и контент остаётся доступным для пользователя
