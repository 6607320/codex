# debug_rag_test.py Specification

## 1. Meta Information

- **Domain:** Scripting
- **Complexity:** Low
- **Language:** Python
- **Frameworks:** Standard Library
- **Context:** ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Опись и задача артефакта: этот инструмент диагностики воспроизводит безопасную эмуляцию ритуала сканирования проекта для проекта rag_builder.py. Он не меняет боевой manifest.json и не создаёт PDF-файлы в основной директории, но воспроизводит логику сканирования и выявления путей, чтобы понять причину слепоты скрипта к файлам. Он детально логирует путь сканирования и выносит ясный вердикт о том, где возникает проблема — в путях PROJECT_DIR или в правилах фильтрации.

- Контекст для создателя: безопасная диагностика путей проекта и фильтров, чтобы подтвердить видимость целевых файлов rag_builder.py.
- Инструкция для ИИ: понять зачем нужен этот модуль и как он по шагам дублирует логику оригинального скрипта без его изменения.

Описание на русском языке: безопасный эмулятор сканирования файлов проекта, который копирует логику rag_builder.py, но работает только в режиме чтения и сообщает, какие файлы действительно видны из заданной корневой директории и какие фильтры мешают нормальной видимости.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args
- **Format:** JSON
- **Schema:**
  InputData:
  directory: string
  extensions: string[]
  filenames: string[]
  exclude_dirs: string[]

### 3.2. Outputs (Выходы)

- **Destination:** STDOUT
- **Format:** Text
- **Success Criteria:** Exit Code 0
- **Schema:**
  OutputResult:
  found_files: string[]
  total_found: number
  py_found: boolean
  md_found: boolean
  messages: string[]

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Установка корня сканирования устанавливается как текущая рабочая директория. Формируется набор правил: какие директории игнорировать и какие расширения и имена файлов считать целевыми. Затем начинается рекурсивный обход дерева каталогов. На лету из списка текущих директорий удаляются запрещённые. Для каждого встреченного файла проверяется, удовлетворяет ли он одному из условий: либо расширение файла входит в перечень целевых расширений, либо имя файла входит в перечень целевых файлов. Если файл удовлетворяет условию, его полный путь добавляется в аккумулированный список найденных файлов. В процессе обхода осуществляется отладочная подсветка для конкретной папки, где встречается файл в нужном квесте, с пометкой если путь содержит Scroll_2 и Quest_1. По завершении сканирования возвращается отсортированный список найденных путей.

После сканирования запускается тестовый режим. Создаётся безопасная папка вывода и печатаются заголовки диагностики, текущая рабочая директория и используемая PROJECT_DIR. Затем выполняется сканирование и формируются и печатаются результаты. Затем проверяются целевые файлы на предмет наличия quest_2_1.py и manifest.md. В зависимости от наличия файлов выводятся соответствующие сообщения: если ни один целевой файл не найден — вывод в духе слепоты; если найден хотя бы один — дополнительно выводится пояснение. В случае совпадения обоих файлов — выводится общее подтверждение успешности теста.

### 4.2. Declarative Content (Для конфигураций и данных)

Указаны точные настройки для безопасного сканирования и тестирования:

- PROJECT_DIR установлен как точка входа: "."
- EXCLUDE_DIRS перечисляет каталоги, которые следует полностью игнорировать
  .git, .dvc, mlruns, reports, data, build, dist, notebooklm_sources, hunt_results, counting_results, dialogue_audio, generated_palette, sampled_frames, dreams, chaos_sculptures, artist_seal, arcane_wizardry.egg-info, node_modules
- TARGET_EXTENSIONS содержит список разрешённых расширений файлов
  .py, .md, .txt, .json, .yml, .yaml, .spec, .sh, .mono, .html, .css, .js, .list
- TARGET_FILES содержит перечень конкретных имён файлов без расширения
  Dockerfile, .gitignore, .dvcignore, .flake8, LICENSE, NOTICE
- SAFE_OUTPUT_DIR = debug_output
- SAFE_MANIFEST = debug_manifest.json

## 5. Structural Decomposition (Декомпозиция структуры)

- scan_project_files — ритуал сканирования файлов проекта: обход директории, фильтрация по расширениям и именам файлов, сбор путей, вывод отладки при спецусловии Scroll_2 и Quest_1, возврат отсортированного списка путей.
- run_test — главный ритуал тестового запуска: создание безопасной папки вывода, печать заголовков диагностики, вызов сканирования, печать результатов, проверка наличия целевых файлов и вывод итогов.
- **main** — точка входа при прямом запуске скрипта, вызывает run_test.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Standard CPU
- **Concurrency:** Sync
- **Dependencies:** Python Standard Library only
- Основные механизмы ограничения и полей: обход os.walk с фильтрацией директорий, сбор путей, сортировка результатов, вывод логов в консоль.

### 6.2. Prohibited Actions (Negative Constraints)

- Не изменять основной проект и боевые артефакты вне безопасной зоны debug_output.
- Не сохранять секреты или конфиденциальные данные в явном виде.
- Не печатать сырые данные в консоль в продакшн-режиме без необходимости.
- Не выполнять сетевые вызовы в синхронном режиме внутри главного цикла.
- Не оборачивать конфигурационные файлы в скрипты.
- Не менять версии или пути во время реконструкции.
- Не изменять основной manifest.json и не создавать PDF-файлы в основной директории — это заложено в режим безопасной эмуляции.

## 7. Verification & Testing (Верификация)

Г whenua Герхин сценарии

Feature: Diagnostic tool for project file scanning

Scenario: Successful execution
Given проект содержит файлы quest_2_1.py и manifest.md в доступной структуре
When инструмент запускается
Then найдено несколько файлов и статус py_found и md_found отображаются как истинные
And выводится сообщение что скрипт видит всё и проблем не воспроизвелась в тесте

Scenario: No target files found
Given проект пуст за пределами заданной папки tools/rag_builder
When инструмент запускается
Then вывод содержит сообщение о том что скрипт не видит ничего за пределами нужной области
And итоговое заключение указывает на слепоту скрипта и причины её

## Инвентарь артефакта (Inventory) 🏰🛡️

- 🏰 debug_rag_test.py — Боевой страж диагностики, щоранно читает пространство проекта и моделирует сканирование файлов.
- 🛡️ SAFE_OUTPUT_DIR debug_output — Священная полость для безопасного вывода артефактов тестирования.
- 🏰 PROJECT_DIR "." — Единый вход в мир сканирования, корень экспедиции.
- 🏰 TARGET_EXTENSIONS список разрешённых расширений — Набор чар для фильтрации файлов.
- 🛡️ TARGET_FILES список целевых имён файлов — Карты маршрутов, по которым ведётся поиск.
- 🏰 SAFE_MANIFEST debug_manifest.json — Эмпирическая манифестационная карта безопасной версии.
- 🛡️ EXCLUDE_DIRS набор исключений — Пороги непроходимости, чтобы не блуждать по мусору проекта.
- 🏰 scan_project_files функция-риммитель — Ритуал, который собирает пути и фильтрует их по правилам.
- 🛡️ run_test функция-ключевик — Главный заклинатель теста, печатает путь диагностики и подводит итоги.

Если нужно, могу расширить разделы или привести дополнительные гипотетические сценарии тестирования.
