# rag_builder.py Specification

## 1. Meta Information

- **Domain:** Scripting
- **Complexity:** Medium
- **Language:** Python
- **Frameworks:** fpdf
- **Context:** ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Это Инструмент Мастера — Строитель «Живой Летописи» для Оракула-Архивариуса. Ритуал сканирует Великий Кодекс Техномагии, находит артефактные файлы (.py, .md, .yml и др.), и собирает их содержимое в серию PDF-томов в notebooklm_sources. Эти тома служат источником знаний для RAG-систем NotebookLM, превращая Кодекс в живую память. Ритуал работает инкрементально, отслеживая изменения через печати (хеши) в manifest.json и пересобирая только тома, затронутые хаосом изменений.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args
- **Format:** Text
- **Schema:**
  interface InputData {
  // момента использования входов в коде не обнаружено; входов нет явных
  }

### 3.2. Outputs (Выходы)

- **Destination:** File
- **Format:** JSON (для манифеста) и PDF-томы
- **Success Criteria:** ExitCode 0
- **Schema:**
  interface OutputResult {
  // явных структурированных выходов не предписано внешнему интерфейсу
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Инициализация свитков и артефактов:

- Определяются константы путей: корень Кодекса, каталог артефактов, черный список директорий и набор целевых расширений файлов.
- Задаются параметры для формирования томов: WORD_LIMIT и имя активного тома.

2. Незафиксированная печать файла (sha256):

- Для каждого файла открывается бинарный поток, читается партиями и вычисляется sha256-хеш.
- В случае ошибки чтения возврашается пустая печать.

3. Великое Обходники Кодекса (scan_project_files):

- Дерево каталогов просматривается рекурсивно.
- Папки из Черного Списка исключаются на лету.
- Файлы выбираются если имя оканчивается на одно из TARGET_EXTENSIONS или входит в TARGET_FILES.
- Путь к каждому найденному файлу добавляется в общий список, затем сам файл SCRIPT rag_builder.py исключается из списка.
- Результат возвращается в отсортированном алфавитном порядке.

4. Летопись manifest:

- load_manifest читает manifest.json (UTF-8); если файл отсутствует или некорректен, создаётся новая структура {files: {}, chunks: []}.
- save_manifest записывает обновлённую летопись в файл manifest.json внутри ARTEFACTS_DIR.

5. PDF-томы из текста (create_pdf_from_text):

- Текст очищается от запретных символов (множество эмодзи и символов удаляется через таблицу перевода).
- Создаётся пустой PDF-документ, подключается шрифт DejaVuSans (DejaVu Sans.ttf) и устанавливается размер шрифта.
- Добавляется страница, текст записывается на страницу.
- Убедиться, что директория вывода существует, затем сохраняется PDF.
- При ошибке выводится сообщение об ошибке.

6. Главный цикл (main):

- Сообщает о запуске строителя и вычисляет пути к manifest.json и директории томов.
- Загружает manifest.
- Выполняется сканирование проекта и вычисление хешей для каждого релевантного файла.
- Сравнивается текущее состояние файлов с записями в manifest; формируется список изменённых или новых файлов.
- Если изменений нет — выводится сообщение и ритуал завершается.
- Если есть изменения, определяется стратегия отката/обновления: удаляется активный том, если он есть и является живым, и связанные файлы удаляются из manifest, их пометки добавляются в очередь переработки.
- Формируется набор файлов к переработке, затем сортируется.
- Четыре фазы: подготовка к инкрементальному обновлению, наполнение новых томов, завершение активного тома и создание архивных томов при превышении WORD_LIMIT.
- Внутри цикла для каждого файла:
  - читается модификация файла, читается текст, подсчитывается количество слов.
  - если текущий том переполнен, создаётся новый архивный том (part_N.pdf), текущий контент сохраняется и добавляется запись в manifest.
  - файл вставляется в текущий том с соответствующими метаданными (путь к файлу и пр.).
  - обновляются печати файлов в manifest.
- После обработки оставшийся контент сохраняется в живой том (living_chronicle.pdf) и добавляется в manifest.
- В конце сохраняется обновлённая manifest.json и завершается ритуал.

7. Контекст исполнения:

- Главная ритуальная функция вызывается при запуске скрипта напрямую.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции:
  - calculate_sha256(filepath: str) -> str
  - scan_project_files(directory: str, extensions: tuple, filenames: tuple, exclude_dirs: set) -> List[str]
  - load_manifest(manifest_path: str) -> Dict[str, Any]
  - save_manifest(manifest_path: str, data: Dict[str, Any])
  - create_pdf_from_text(text_content: str, output_path: str)
  - main() — orchestrator
- Константы и артефакты:
  - PROJECT_DIR, ARTEFACTS_DIR, EXCLUDE_DIRS, TARGET_EXTENSIONS, TARGET_FILES
  - MANIFEST_FILE, OUTPUT_DIR, WORD_LIMIT, LIVING_CHUNK_FILENAME

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Операции в основном IO-bound. Шаги обхода файлов и чтения текстов выполняются последовательно (СИНХРОННО).
- **Concurrency:** Синхронный, без параллелизма.
- **Dependencies:**
  - Python стандартной библиотеки (os, hashlib, json, typing)
  - Библиотека fpdf (для формирования PDF)
  - В проекте ожидается наличие шрифта DejaVuSans.ttf, доступного для FPDF.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в manifest.json или в явном виде на файловой системе без защиты.
- DO NOT печатать сырые данные на консоль в продакшн-режиме (за исключением информативных сообщений ритуала).
- DO NOT выполнять сетевые запросы синхронно в критическом цикле обработки файлов.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) внутрь скриптов без явного разделения.
- DO NOT менять версии путей или имен файлов во время реконструкции без согласования архитектуры.

## 7. Verification & Testing (Верификация)

Геркин-сценарии (1-2 сценария счастья и один сценарий ошибки):

Feature: Живая летопись строится правильно
Scenario: Успешное инкрементальное обновление
Given существующая manifest.json и набор файлов в проекте
When выполняется rag_builder.py
Then создаются архивные тома (part_N.pdf) по мере достижения WORD_LIMIT
And активный том living_chronicle.pdf обновляется по мере обработки последних файлов
And manifest.json обновляется с записями о новых/измененных файлах и источниках

Scenario: Обнаружено никаких изменений
Given manifest.json отражает текущее состояние файлов
When выполняется rag_builder.py
Then выводится сообщение об отсутствии изменений
And процесс завершается без создания новых томов

Инфо: Все слова и шаги сохранены в духе ритуального повествования, без вставок кода, но с точной логикой исполнения.

Инвентарь артефактов (Inventory)

- 🏰 manifest.json — Свиток Летописи, где хранится карта изменений и стражи томов.
- 🛡️ notebooklm_sources — Хранилище томов, где появляются архивные и живой том.
- 🗺️ living_chronicle.pdf — Активный том Живой Летописи, который попадает под редактирование.
- 🗺️ part_1.pdf, 🗺️ part_2.pdf, ... — Архивные тома, каждый со своим набором источников.
- 📜 Файлы проекта (.py, .md, .yml, .json, .sh, .html, .css, .js и др.) — Свитки-источники, попадающие под печати.
- 🔮 DejaVuSans.ttf — Шрифт духа Каллиграфа, необходимый для печати в PDF.

Contextual note: Independent Artifact
