rag_builder.py Specification

1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Python
- Frameworks: fpdf
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)

Контекст для Создателя: Инструмент строителя живой летописи собирает тексты артефактов из проекта и конвертирует их в серию PDF-томов, чтобы стать источником знаний для Rag-систем NotebookLM. Ритуал работает инкрементально, отслеживая изменения по печатям (хешам) в manifest.json и пересобирая только затронутые тома.

This section provides high-level intent. Use it to understand the WHY of the code.
Этот модуль предназначен для автоматизации сканирования kodeksa, извлечения текстов из файлов .py, .md, .yml и др., консолидирования их в живую память системы и обновления тем самым RAG-архива без повторной переработки всего кода. Хозяйская идея: минимизировать переработку, сохранить порядок томов, и держать NotebookLM в курсе последних изменений Великих Кодексов.

3. Interface Contract (Интерфейсный Контракт)

Instruction for AI: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

### 3.1. Inputs (Входы)

Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
Format: JSON | Text | Binary | Stream
Schema:
interface InputData {
// ...
}

### 3.2. Outputs (Выходы)

Destination: STDOUT | File | Database | API Response | Event Log
Format: JSON | CSV | Text
Success Criteria: Exit Code 0 | 200 OK | File Created
Schema:
interface OutputResult {
// ...
}

4. Implementation Details (The Source DNA / Исходный Код)

Algorithmic Logic (step-by-step description, без копирования кода)

- Воплощение закона: устанавливаются константы свитка целостности и области деяний: PROJECT_DIR задаёт корень Великих Кодексов, ARTEFACTS_DIR место хранения артефактов, EXCLUDE_DIRS — Черный Список директорий, TARGET_EXTENSIONS и TARGET_FILES — Истинные Имена свитков, MANIFEST_FILE хранит летопись, OUTPUT_DIR — место хранения томов, WORD_LIMIT — предел слов на архивный том, и LIVING_CHUNK_FILENAME — имя активного живого тома.
- Вызов ритуала Calculate SHA256: для каждого найденного файла свитков вычисляется его отпечаток души посредством SHA256, чтобы можно было потом определить изменения.
- Великий обход Кодекса: сканируются директории в PROJECT_DIR, исключаются черные директории, собираются пути к файлам, соответствующим целям по расширениям или именам. Сам скрипт исключает собственный путь из анализа.
- Загрузка летописи: из manifest.json читаются структуры файлов и томов. При отсутствии или сломанности создаётся новая пустая летопись.
- Сохранение летописи: обновлённые данные записываются обратно в manifest.json с отступами и кодировкой UTF-8.
- Формирование PDF тома: через духа FPDF создаётся PDF-документ из текстов. В начале каждого файла добавляются заголовки с именем файла и timestamp, затем текст оборачивается в финальный футер. Перед добавлением в PDF содержимое чистится от набора запретных рун, чтобы сохранить читаемость и совместимость.
- Главный цикл main: запускается ритуал, который собирает и проверяет файлы. Сканируются файлы проекта, для каждого считается отпечаток; сравнивается с данными в manifest.json. Выявляются новые и изменённые файлы.
- Механизм отката: если последний архивный том является активным живым томом, запускается откат: удаляется текущий живой том из летописи и удаляется его PDF-файл, а записи о его исходниках удаляются. Файлы, существующие на диске, повторно попадают в круг переработки. Если последний том архивный, начинается новый активный том.
- Подготовка к обработке: формируется множество файлов к переработке. Если не осталось изменений, ритуал завершается.
- Наполнение новых томов: сортированный список файлов для обработки объединяется в новый активный том, либо добавляются части архива, если жемчужина лимита слов достигнута. В каждом новом архивном томе содержится имя файла, суммарное количество слов и список источников.
- Итоговая стадия: активный том сохраняется как живой том с именем living_chronicle.pdf и добавляется в летопись. Затем летопись сохраняется и ритуал завершается успешно.

5. Structural Decomposition (Декомпозиция структуры)

Instruction for AI:

- Для кода: перечисли функции и классы.
- Для конфигов: перечисли основные логические блоки (например: repos, services, build_stages).

Функции и ключевые элементы

- calculate_sha256: ритуал вычисления нерушимой печати SHA256 для файла.
- scan_project_files: Великий обход Кодекса с фильтрацией по расширениям и имени.
- load_manifest: загрузка manifest.json, создание структуры по умолчанию при отсутствии файла.
- save_manifest: сохранение летописи в файл manifest.json.
- create_pdf_from_text: создание PDF-тома из скомпонованного текста, очистка от запрещённых рун, запись на диск.
- main: управляет всем процессом; orchestrator руны, координация фаз сканирования, отката, наполнения и сохранения летописи.

6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Synchronous (один поток, по шагам)
- Dependencies: os, hashlib, json, FPDF, typing
- Additional: использование шрифта DejaVuSans.ttf для PDF, наличие файла в окружении

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде в коде или файлах manifest
- DO NOT выводить сырые данные в консоль в продакшн режиме без фильтрации
- DO NOT выполнять сетевые вызовы синхронно в главной петле
- DO NOT оборачивать конфигурационные файлы YAML JSON внутрь скриптов
- DO NOT менять версии библиотек или пути во время реконструкции

7. Verification & Testing (Верификация)

Instruction for AI: Generate 1-2 Gherkin scenarios that describe the happy path and one error case for this script.

Геркин сценарии

Feature: rag_builder.py живой хроникатор

Scenario: Успешное обновление инкрементом
Given manifest.json существует и содержит данные о предшествующих файлах и фрагментах
And в проекте имеются новые или изменённые файлы вне текущего архива
When скрипт rag_builder.py выполняется
Then создаётся новый архивный том или обновляется живой том living_chronicle.pdf
And manifest.json обновляется с новыми файлами и записями о томах

Scenario: Невозможно прочитать файл во время обработки
Given имеется файл в проекте который не удаётся прочитать из-за ошибки ввода-вывода
When скрипт rag_builder.py выполняется
Then выводится предупреждение и файл пропускается без падения цикла

Искусство Инвентаря (Inventory)

- 🏰 PROJECT_DIR — корень Великих Кодексов, куда ведёт путь обхода
- 🏰 ARTEFACTS_DIR — место, где рождаются свитки летописи
- 🛡️ EXCLUDE_DIRS — Черный Список запрещённых миров
- 🗝️ TARGET_EXTENSIONS — список истинных свитков по расширениям
- 🗝️ TARGET_FILES — особые безрасширочные свитки
- 🗺 MANIFEST_FILE — главный свиток летописи manifest.json
- 🗺 OUTPUT_DIR — хранилище томов notebooklm_sources
- 📜 WORD_LIMIT — магический предел слов на архивный том
- 📖 LIVING_CHUNK_FILENAME — имя активного живого тома
- 🪄 calculate_sha256 — руна вычисления нерушимой печати файла
- 🪄 scan_project_files — руна Великого Обхода Кодекса
- 🪄 load_manifest — руна чтения инициализации летописи
- 🪄 save_manifest — руна сохранения обновлённой летописи
- 🪄 create_pdf_from_text — руна формирования PDF из текста артефактов
- 🗺 main — Великая Руна-Оракул, управляющая всем процессом
- 🧭 Импортированные духи: os, hashlib, json, FPDF, typing

Contextual Note: Independent Artifact
В контексте Великой Книги Кодекса rag_builder.py описан как автономный артефакт, который оперирует с файловой системой и внешними зависимостями. В поле Context указано источник логики и связи с мировыми системами для палат NotebookLM. Independent Artifact.
