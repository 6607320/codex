# audit_index.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: Python Standard Library os, json
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот свиток диагностики служит астральным инструментом аудита Летописи. Он сверяет реальное состояние файлов Летописи, записанных в manifest.json, с тем, что отражено на диске, и выявляет пропавшие свитки, которые могли быть пропущены ритуалом rag_builder.py. Механика проста: загрузить манифест, собрать список индексированных файлов, пройтись по дереву проекта с исключениями и сопоставить оба списка, выдать вердикт по целостности и местоположению пропавших материалов.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON
- Schema: InputData
  - chunks: массив объектов
    - sources: массив строк, каждая строка есть относительный путь к файлу внутри записей Летописи

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit Code 0
- Schema: OutputResult
  - foundCount: число найденных файлов в индексе
  - missingFiles: список путей к файлам, отсутствующим в манифесте, но найденным на диске
  - indexedFilesAbsCount: общее количество абсолютных путей в manifest.json

## 4. Implementation Details (The Source DNA / Исходный Код)

Алгоритм шага за шагом:

- Заглядывает в свиток manifest.json и пытается прочитать его как JSON. Если свиток не найден, печатает сообщение об ошибке и прекращает ритуал.
- Формирует множество абсолютных путей проиндексированных файлов, собирая их из разделов chunks.sources манифеста.
- Определяет текущую рабочую директорию для разрешения относительных путей и вычисления относительных путей к месту хранения.
- Выполняет Великий обход файловой системы от корня проекта PROJECT_ROOT, постепенно исключая директории из Черного Списка.
- В каждом найденном файле проверяет, соответствует ли он целевым расширениям TARGET_EXTS (.md и .py). Игнорирует сам manifest.json.
- Для каждого релевантного файла сравнивает реальный абсолютный путь с множеством из манифеста:
  - если путь есть в манифесте, увеличивает счетчик найденных в индексе файлов
  - если пути нет, вычисляет его относительный путь к текущей директории и добавляет в список пропавших файлов
- В конце выводит вердикт: сколько файлов найдено в индексе; если есть пропавшие файлы, печатает их; иначе объявляет идеальное соответствие Летописи и реального состояния файлов.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции и классы:
  - audit: главная функция ритуала аудита
- Константы/Настройки:
  - PROJECT_ROOT: "../../"
  - MANIFEST_FILE: manifest.json
  - TARGET_EXTS: набор расширений (.md, .py)
  - exclude: множество имен директорий, исключаемых из обхода
- Логика:
  - Чтение manifest.json
  - Построение множества индекса файлов
  - Обход проекта с исключениями
  - Сравнение индекса и реального состояния
  - Вердикт и вывод

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартный CPU, обход файловой системы линейно зависит от размера дерева и числа файлов
- Concurrency: синхронный режим (однопоточность)
- Dependencies: Python Standard Library (os, json)
- Поведение при отсутствии manifest.json: печать ошибки и ранний выход
- Черный список директорий учитывается на каждом шаге обхода
- Обход не копирует и не модифицирует файлы, только читает и сравнивает

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде в конфигурациях
- НЕ выводить сырые данные в консоль в продакшн-режиме без фильтрации
- НЕ выполнять сетевые запросы синхронно в главном цикле аудита
- НЕ оборачивать конфигурационные файлы .yaml .json в скрипты
- НЕ менять версии или пути во время реконструкции артефакта

## 7. Verification & Testing (Верификация)

### Герхин-сценарии

Feature: Audit index integrity

Scenario: Successful audit where manifest and filesystem are in sync
Given manifest.json содержит chunks с sources ведущими к существующим файлам .py и .md в проекте
When запускается audit_index.py
Then выводится количество найденных файлов в индексе и отсутствие пропавших файлов
And выводится сообщение о ИДЕАЛЬНОЙ целостности

Scenario: Manifest missing
Given manifest.json отсутствует в рабочем каталоге
When запускается audit_index.py
Then выводится сообщение об ошибке о отсутствии manifest.json
And процесс завершается без критической ошибки

## 4. Declarative Content (Для конфигураций и данных) — Инвентарь

- 🏰 manifest.json — Главный свиток Летописи, содержащий перечень проиндексированных чанков и источников
- 🛡️ PROJECT_ROOT — Корень Кодекса относительно места запуска
- 🗺️ TARGET_EXTS — Шкала целевых расширений файлов: .md и .py
- 🗺️ exclude — Черный список директорий для обхода
- 🗿 audit — Главный ритуал аудита, вызывающий сверку manifest.json с реальным состоянием файлов
- 🧭 current_dir — Текущая рабочая директория для разрешения путей
- 🧭 scan_root_abs — Абсолютный путь к корню проекта для обхода
- 🧭 indexed_files_abs — Множество абсолютных путей, взятых из манифеста
- 🧰 missing_files — Список файлов, найденных на диске, но отсутствующих в манифесте
- 🧭 found_count — Счетчик файлов, найденных в индексе
- 🧰 OutputResult — Структура выходных данных (foundCount, missingFiles, indexedFilesAbsCount)

Если нужны дополнения или корректировки по стилю артефакта, скажи, и я подстрою ритуал.
