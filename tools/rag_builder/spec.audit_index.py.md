# audit_index.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: Medium
- Language: Python
- Frameworks: json, os
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Летописный инструмент диагностики целостности: он сверяет Летопись manifest.json с реальным состоянием файлов проекта, чтобы обнаружить потерянные или пропущенные свитки.  
**Instruction for AI:** This section provides high-level intent. Use it to understand the "WHY" of the code.  
Легенда: Ритуал audita_index проводит чтение Летописи, собирает сводку проиндексированных файлов, обходится по сводному храму проекта и выносит вердикт: где файлы на месте, а где появился Хаос — в виде пропущенных записей.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: Internal configuration (нет внешних входов). Скрипт описывает путь к Летописи и корню Кодекса через константы.
- Format: N/A (входов как таковых нет; данные читаются из manifest.json и файловой системы во время выполнения)
- Schema: InputData — пустой интерфейс (нет внешних входов)

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit code 0
- Schema: OutputResult
  - success: boolean
  - message?: string
  - indexedFiles?: number
  - foundCount?: number
  - missingFiles?: string[]

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Ритуал начинается с объявления и фиксации священных параметров: PROJECT_ROOT устанавливается как "../../", MANIFEST_FILE принимает имя manifest.json, TARGET_EXTS задаёт расширения файлов для проверки (".md", ".py").
- Фаза 1 — Чтение Летописи: в ходе ритуала пытается открыть manifest.json и распаковать его содержимое как структуру данных. Если свиток отсутствует, активируется Хаос: печатается сообщение об ошибке и ритуал прерывается.
- Фаза 2 — Сбор проиндексированных свитков: из манифеста формируется множество абсолютных путей к файлам. Это поле Indexed Files Abs, которое хранит уникальные положения свитков.
- Фаза 3 — Сканирование Реальности: начинается Великий обход директории проекта от корня. Дьявольские директивы исключения отбрасывают следующие директории: .git, .dvc, notebooklm_sources, **pycache**, debug_output, env, venv. В каждой папке ищутся файлы с целевыми расширениями (.md, .py). Мантисурално устраняются записи manifest.json, чтобы не считать его как потерянный файл.
  - Для каждого найденного файла вычисляется его абсолютный путь. Если путь присутствует в множестве из Летописи, счётчик Found Count увеличивается. Если же файла нет в Летописи, вычисляется относительный путь к текущей рабочей директории и добавляется к списку Потерянных файлов.
- Фаза 4 — Вынесение Вердикта: выводится количество файлов, найденных в индексе. Если есть список потерянных файлов, он печатается по каждому элементу; иначе объявляется идеальное соответствие: все найденные файлы присутствуют в индексе.

### 4.2. Declarative Content (Для конфигураций и данных) — Инвентарь проекта

- Инвентарь хранителя Летописи manifest.json (🏰 Летопись Грани) — источник индекса файлов.
- Ритуал аудита audit_index.py (🏰 Инструмент Диагностики) — главный артефакт аудита.
- Корень Кодекса (🏰 PROJECT_ROOT) — путь-основа для сканирования.
- Скотч расширений поиска (🛡️ TARGET_EXTS) = .md и .py — сферы, где ведётся наблюдение.
- Ворота исключения (🛡️ exclude) — список директорий, которые прокляты и не попадают в поиск.
- Свитки и храмы файлов (🛡️ manifest.json, файлы проекта) — цель скана и сверки.
- Летописная печать (🛡️ STDOUT) — каналы вывода отчётов.
- Боги зависимостей (🏰 json, 🛡️ os) — встроенные библиотеки, без которых заклятие не сработает.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции и классы:
  - audit(): Главный ритуал аудита. Выполняет все фазы: чтение Летописи, сбор индекса, обход файловой системы, сопоставление и вывод вердиктов.
- Константы/Ключи конфигураций:
  - PROJECT_ROOT: "../../"
  - MANIFEST_FILE: "manifest.json"
  - TARGET_EXTS: (".md", ".py")
  - exclude: множество директорий для исключения
- Главный вход: if **name** == "**main**": audit()

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Обычная нагрузка ввода-вывода; сканирование файлов может быть ресурсозатратным в больших проектах, но реализовано синхронно как стандартный скрипт.
- Concurrency: Синхронный однопоточный режим (Sync).
- Dependencies: Только стандартные библиотеки Python: os, json.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде (не применимо к текущему артефакту, но держать секреты в .env — полезная практика).
- НЕ печатать сырые данные в консоль в продакшн-режиме без фильтрации.
- НЕ использовать синхронные сетевые вызовы в основном цикле, если речь идёт о сетях (для данного артефакта это не применимо, но следить за чистотой последовательностей).
- НЕ оборачивать конфигурационные файлы (.yaml, .json) в исполняемые скрипты.
- НЕ изменять версии или пути в процессе реконструкции артефакта.

## 7. Verification & Testing (Верификация)

1. Геркин-сценарий: Успешный аудит

- Контекст: manifest.json существует и включает записи; файлы проекта существуют на диске и соответствуют записям.
- Действие: запуск audit_index.py.
- Ожидание: в выводе увидим строку о количестве файлов в индексе и сообщение об идеальном состоянии (ИДЕАЛЬНО) без списка пропущенных файлов.

2. Геркин-сценарий: Отсутствие манифеста

- Контекст: manifest.json не найден.
- Действие: запуск audit_index.py.
- Ожидание: вывод об ошибке: “❌ Ошибка: manifest.json не найден!” и немедленная остановка ритуала.

3. Гибридная версия (опционально): Частичная несовпаденность

- Контекст: manifest.json содержит часть файлов, реально существующие файлы частично отсутствуют в манифесте.
- Действие: запуск audit_index.py.
- Ожидание: в вердикте появляется список "потерянных" файлов с указанием их относительных путей.

ИЗ РУНОВ ИСКУССТВА: audit_index.py — это скрипт-охранник целостности Летописи, чутко сверяющий свет manifest.json с настоящими свитками на полках проекта, чтобы хаос не проник в хроники Кодекса.
