# **Архитектурный анализ инкрементальных систем Retrieval-Augmented Generation (RAG) в среде Gemini-Native: Исследование инструментария «Spirit-Archivist»**

## **Введение: Смена парадигмы в управлении знаниями LLM**

Современный ландшафт искусственного интеллекта переживает фундаментальный сдвиг от классических архитектур RAG (Retrieval-Augmented Generation), основанных на векторном поиске коротких фрагментов, к системам, использующим сверхдлинные контекстные окна. Появление моделей класса Gemini 1.5 Pro с контекстным окном до 1-2 миллионов токенов открыло новые горизонты для анализа целостных кодовых баз и массивных архивов документации без необходимости дробления информации на атомарные части, теряющие семантическую связность. Однако эта технологическая революция столкнулась с прагматическими ограничениями интерфейсов и инструментов загрузки данных.  
В данном отчете проводится глубокий технический и методологический анализ системы rag_builder, фигурирующей в проектной документации под кодовым именем «Мастерская Духа-Архивариуса». Эта система представляет собой специализированное middleware-решение (промежуточное ПО), разработанное для преодоления ограничений платформы Google NotebookLM при работе с динамическими репозиториями кода и документации. Мы исследуем архитектурные паттерны инкрементального обновления («Живая Летопись»), механизмы контроля целостности данных и специфику преобразования текстовых артефактов в PDF-формат для оптимизации мультимодального восприятия нейросетью.  
Анализ базируется на предоставленных исходных кодах скриптов (rag_builder.py, audit_index.py, debug_rag_test.py, find_paradox.py) и широком спектре исследовательских данных касательно ограничений NotebookLM, возможностей Gemini 1.5 Pro и лучших практик Data Version Control (DVC).

## **1\. Операционная среда и граничные условия NotebookLM**

Для понимания архитектурных решений, заложенных в rag_builder, необходимо детально рассмотреть ограничения целевой платформы — NotebookLM, функционирующей на базе модели Gemini 1.5 Pro. Именно жесткие лимиты на ингестию (загрузку) данных диктуют необходимость создания сложных агрегирующих пайплайнов.

### **1.1. Лимиты источников: проблема кардинальности файлов**

Ключевым фактором, делающим невозможным прямую загрузку репозиториев кода в NotebookLM, является ограничение на количество источников. Согласно актуальной технической документации и пользовательским отчетам, стандартная версия NotebookLM поддерживает не более 50 источников в одном блокноте. Даже премиальная версия (NotebookLM Plus), по некоторым данным расширяющая этот лимит до 300 источников , остается недостаточной для средних и крупных программных проектов, которые могут содержать тысячи файлов .py, .md, .json и конфигурационных файлов.  
Однако, платформа предоставляет колоссальный запас по объему каждого отдельного источника — до 500 000 слов или 200 МБ. Это создает уникальную инженерную задачу: необходимо преобразовать файловую систему с высокой кардинальностью (тысячи мелких файлов) в набор артефактов с низкой кардинальностью (малое количество крупных файлов), при этом сохранив структуру и читаемость кода для LLM.  
**Таблица 1.1: Сравнительный анализ ограничений NotebookLM и архитектурных ответов rag_builder**

| Параметр ограничения        | Значение (Free/Plus)       | Влияние на архитектуру репозитория                           | Решение в rag_builder                                                  |
| :-------------------------- | :------------------------- | :----------------------------------------------------------- | :--------------------------------------------------------------------- |
| **Макс. кол-во источников** | 50 / 300                   | Критическое. Невозможно загрузить проект пофайлово.          | Агрегация сотен файлов в единые PDF-тома (part_N.pdf).                 |
| **Макс. слов в источнике**  | 500,000                    | Профицитное. Позволяет создавать огромные контекстные блоки. | Установка лимита тома в \~475,000 слов для безопасности.               |
| **Поддерживаемые форматы**  | PDF, MD, TXT, Google Docs  | Требует конвертации кода.                                    | Использование fpdf2 для генерации PDF из исходного кода.               |
| **Механизм обновления**     | Ручной (удаление/загрузка) | Высокая трудоемкость синхронизации.                          | Стратегия «Живого Тома» (living_chronicle.pdf) для минимизации кликов. |

Система rag_builder решает проблему "бутылочного горлышка" количества файлов путем рекурсивного обхода директорий и слияния содержимого файлов в последовательные тома. Это превращает ограничение платформы в преимущество, так как модель получает доступ к коду в виде связных блоков, что теоретически может улучшить понимание межфайловых зависимостей, если они попадают в один том.

### **1.2. Визуально-языковая обработка: Почему PDF?**

Выбор формата PDF в качестве целевого артефакта (output_path) в скрипте rag_builder.py заслуживает отдельного обсуждения. Gemini 1.5 Pro является нативно мультимодальной моделью, обладающей способностью "видеть" документы. Исследования показывают, что модель может интерпретировать PDF не просто как текстовый слой, а как визуальный объект, сохраняя информацию о верстке, отступах и табличных данных.  
В контексте программного кода, который часто полагается на визуальную структуру (отступы в Python, вложенность блоков в YAML), использование PDF может быть как преимуществом, так и риском. С одной стороны, PDF "замораживает" форматирование, гарантируя, что отступы не поплывут при парсинге. С другой стороны, процесс конвертации текста в PDF через библиотеку fpdf2 и последующего обратного распознавания (OCR или экстракция) на стороне Gemini может вносить шум.  
Тем не менее, отчеты сообщества и документация указывают на то, что Gemini 1.5 Pro демонстрирует высокую точность в работе с длинными PDF-документами, достигая near-perfect recall (почти идеального извлечения) в тестах "иголка в стоге сена" даже на документах объемом до 1000 страниц. Это валидирует стратегию rag_builder по созданию массивных PDF-томов как жизнеспособную альтернативу текстовым файлам, особенно учитывая, что PDF воспринимается NotebookLM как "первоклассный гражданин" с лучшей поддержкой цитирования (citations).

## **2\. Философия «Живой Летописи»: Архитектура Инкрементальной Синхронизации**

Центральным элементом анализируемой системы является стратегия управления данными, названная в документации «Философией Живой Летописи». Это сложный алгоритмический подход к проблеме синхронизации статического набора данных (архив PDF) с динамически изменяющейся кодовой базой. В условиях отсутствия API для автоматической загрузки файлов в NotebookLM , минимизация ручных операций становится приоритетом DevOps-инженерии.

### **2.1. Дихотомия данных: Архив и Оперативная Память**

Архитектура разделяет знание на две категории с фундаментально разным жизненным циклом:

1. **Неизменяемые Артефакты (Immutable Artifacts):** Тома серии part_N.pdf. Это "запечатанные" блоки знаний. Как только объем накопленных файлов превышает порог WORD_LIMIT (475,000 слов), том финализируется. С точки зрения теории баз данных, это аналог SSTables (Sorted String Tables) в LSM-деревьях — данные записаны и больше не меняются. Это позволяет пользователю загрузить файл один раз и не тратить время на его повторную обработку.
2. **Мутабельный Буфер (Mutable Buffer):** Файл living_chronicle.pdf. Это динамический сегмент, аналог MemTable или журнала упреждающей записи (WAL). Он содержит:
   - Абсолютно новые файлы, добавленные в проект.
   - Старые файлы, которые претерпели изменения (изменился хеш SHA256).
   - Файлы, которые еще не накопили достаточного объема для формирования полноценного архивного тома.

Эта стратегия решает критическую проблему RAG-систем — "Stale Index" (устаревший индекс). Без четкого разделения на архив и активную часть пользователю пришлось бы либо пересобирать весь проект целиком при каждом коммите (что долго и дорого по трафику), либо вручную отслеживать, какие файлы изменились.

### **2.2. Манифест как Единый Источник Истины**

Скрипт использует файл manifest.json для отслеживания состояния системы. Это классический паттерн State File, используемый в инструментах инфраструктуры как кода (Terraform). Манифест хранит карту: Путь файла \-\> Хеш SHA256 \-\> Том (Chunk ID).  
Анализ кода функции main в rag_builder.py раскрывает алгоритм идемпотентности:  
 `# 2. Обнаружение изменений...`  
 `changed_or_new_files = [`  
 `filepath`  
 `for filepath, current_hash in current_files_hashes.items()`  
 `if manifest_data["files"].get(filepath)!= current_hash`  
 `]`

Этот блок кода гарантирует, что если файлы не менялись, система не производит никаких действий, экономя вычислительные ресурсы. Однако наиболее интересна логика отката (Rollback Strategy). Если обнаружены изменения, скрипт проверяет последний созданный том. Если это living_chronicle.pdf, он **уничтожается** (удаляется запись из манифеста и сам файл), а все входящие в него файлы возвращаются в очередь на обработку (files_to_reprocess).  
Это предотвращает фрагментацию данных. Файл не может существовать "частично" в версии 1 и "частично" в версии 2 в рамках активного тома. Активный том всегда пересобирается с нуля, гарантируя целостность "верхушки" репозитория.

### **2.3. Временные парадоксы и риск галлюцинаций**

Несмотря на продуманную архитектуру, система подвержена риску возникновения "временных парадоксов". Если пользователь вручную удалит файл living_chronicle.pdf из папки, но не обновит манифест, или если произойдет сбой записи, может возникнуть ситуация, когда старая версия файла останется в part_1.pdf, а новая попадет в новый living_chronicle.pdf (в случае, если логика исключения сработает некорректно).  
Исследования показывают, что наличие дубликатов противоречащей информации в контекстном окне (Recency Bias) существенно снижает качество ответов RAG-систем. Модель может "запутаться" между двумя версиями функции, выдав гибридный, нерабочий код. Именно для этого в инструментарий включен скрипт find_paradox.py, который выполняет операцию пересечения множеств (Set Intersection) между файлами разных томов. Это критически важный инструмент Observability (наблюдаемости), позволяющий детектировать коллизии до того, как они попадут в промт модели.

## **3\. Технический анализ реализации: Код как Ритуал**

Рассмотрим реализацию rag_builder.py с точки зрения Python-разработки и оптимизации под LLM.

### **3.1. Хеширование и обнаружение изменений**

Функция calculate_sha256 реализует поблочное чтение файлов (4096 байт). Это профессиональный подход к обработке потенциально больших файлов, предотвращающий переполнение оперативной памяти (MemoryError). Использование алгоритма SHA-256 обеспечивает криптографическую стойкость к коллизиям, что избыточно для контроля версий кода (где хватило бы и MD5 или CRC32), но гарантирует абсолютную точность детекции изменений.  
`def calculate_sha256(filepath: str) -> str:`  
 `sha256_hash = hashlib.sha256()`  
 `#... чтение блоками...`  
 `return sha256_hash.hexdigest()`

### **3.2. Генерация PDF и проблема кодировок**

Использование библиотеки fpdf2 (как указано в импортах и комментариях о pip install) сопряжено с известными проблемами поддержки Unicode. Стандартные шрифты PDF (Latin-1) не поддерживают кириллицу, эмодзи и многие математические символы.  
В коде применено два решения этой проблемы:

1. **Подключение шрифта:** pdf.add_font("DejaVu", "", "DejaVuSans.ttf"). Шрифт DejaVu Sans является стандартом де\-факто для покрытия широкого диапазона Unicode (включая кириллицу), что критично для русскоязычного контекста задачи.
2. **Санитизация текста:** В коде присутствует "Ритуал Изгнания Запретных Рун":  
   `GLYPHS_TO_EXCLUDE = "🔮📜💎..."`  
   `text_content = text_content.translate(translation_table)`  
   Это "костыль" (workaround), необходимый потому, что fpdf2 может выбрасывать исключения при попытке рендеринга 4-байтовых символов Unicode (например, цветных эмодзи), даже при наличии подходящего шрифта. Хотя это обеспечивает стабильность пайплайна, это приводит к потере семантической информации. Если код содержит комментарии с эмодзи, используемыми как маркеры (например, ⚠️ DEPRECATED), эта информация будет удалена, и LLM её не увидит.

### **3.3. Метаданные и сегментация контекста**

Скрипт оборачивает содержимое каждого файла в явные маркеры начала и конца:  
`file_header = f"\n\n--- НАЧАЛО: {filepath} (timestamp: {timestamp}) ---\n"`  
`file_footer = f"\n\n--- КОНЕЦ: {filepath} ---\n"`

Это критически важный паттерн для RAG. Поскольку PDF склеивает сотни файлов, без таких разделителей модель могла бы "слить" конец одного файла и начало другого, что привело бы к галлюцинациям при анализе потока управления. Добавление timestamp (временной метки) теоретически дает модели возможность разрешать конфликты версий (понимать, какой файл новее), хотя на практике LLM часто плохо справляются с арифметическим сравнением Unix-таймстемпов без специальных инструкций.

### **3.4. Исключение шума**

Список EXCLUDE_DIRS включает .git, .dvc, \_\_pycache\_\_. Это фундаментальное требование. Индексация папки .git привела бы к попаданию в контекст тысяч бинарных объектов и дельт, что мгновенно замусорило бы контекстное окно и сделало бы ответы модели бесполезными. Фильтрация на уровне os.walk (dirs\[:\] \=...) обеспечивает эффективность сканирования, не заходя в глубокие вложенные структуры системных директорий.

## **4\. Экосистема диагностики и наблюдаемость (Observability)**

Профессиональные системы RAG требуют инструментов для интроспекции. В комплекте с rag_builder идут три скрипта, формирующие "Аварийный набор". Их наличие свидетельствует о зрелости подхода, признающего, что ETL-процессы (Extract, Transform, Load) для LLM склонны к скрытым сбоям.

### **4.1. audit_index.py (Аудитор)**

Этот скрипт решает проблему "False Negatives" — файлов, которые существуют, но не попали в индекс. В сложных проектах файлы часто выпадают из пайплайна из\-за ошибок в .gitignore, скрытых символов в именах или проблем с правами доступа. Аудитор выполняет сверку множеств (Set Difference): Files\_{Disk} \\setminus Files\_{Manifest}. Это единственный способ гарантировать полноту знаний Оракула. Если модель утверждает, что функции не существует, запуск аудитора позволяет мгновенно проверить, "видит" ли её система индексации.

### **4.2. find_paradox.py (Детектив Времени)**

Как упоминалось выше, этот скрипт ищет пересечения между томами: Files\_{Part1} \\cap Files\_{Part2} \\neq \\emptyset. Наличие таких пересечений — сигнал о нарушении инвариантов системы. В идеальном инкрементальном пайплайне файл может находиться только в одном томе. Обнаружение дубликатов позволяет оператору вручную вмешаться (например, удалить старый том) до того, как некорректный контекст приведет к ошибкам в генерации кода.

### **4.3. debug_rag_test.py (Эмулятор)**

Скрипт позволяет безопасно тестировать изменения в логике сканирования (например, новые регулярные выражения для фильтрации файлов) без риска повредить боевой manifest.json. Это песочница для разработки самого инструментария, что соответствует лучшим практикам разработки инфраструктурного ПО.

## **5\. Data Version Control (DVC): Хранитель Артефактов**

В описании задачи упоминается "Хранитель DVC". Интеграция DVC (Data Version Control) переводит систему из разряда локальных скриптов в категорию MLOps-решений промышленного уровня.

### **5.1. Версионирование знаний как кода**

В традиционной разработке мы версионируем исходный код (Git). В ML мы версионируем данные и модели. В системе rag_builder "данными" для LLM являются сгенерированные PDF-тома. Использование DVC позволяет:

1. **Разгрузить Git:** PDF-файлы являются бинарными и тяжелыми. Хранить их в Git — плохая практика (раздувание репозитория). DVC хранит в Git только метафайлы (.dvc), а сами PDF — в удаленном хранилище (S3, Google Drive, Azure Blob Storage).
2. **Воспроизводимость:** Позволяет "путешествовать во времени". Разработчик может переключиться на коммит месячной давности, выполнить dvc checkout, и получить точное состояние базы знаний (набор PDF), которое было у Оракула в тот момент. Это критично для отладки промптов и регрессионного тестирования ответов модели.
3. **Совместная работа:** Другие члены команды могут получить готовые PDF-индексы через dvc pull, не запуская процесс генерации локально, что экономит время и гарантирует идентичность контекста у всей команды.

### **5.2. Пайплайн CI/CD**

В идеальной реализации DVC интегрируется в CI/CD. При слиянии кода в ветку main, CI-раннер может запускать rag_builder.py, фиксировать изменения в DVC и (теоретически) пушить их в облако. Хотя NotebookLM не имеет API для автоматического деплоя, наличие артефактов в DVC делает этот процесс подготовленным к автоматизации в будущем или использованию через "Deep Research" агентов.

## **6\. Визуально-текстовая дилемма: PDF против Markdown**

Один из самых дискуссионных вопросов в архитектуре RAG для кода — выбор формата. rag_builder делает ставку на PDF. Сравним этот подход с альтернативой (Markdown/Text), основываясь на исследовательских данных.  
**Таблица 6.1: Сравнительный анализ форматов для Gemini 1.5 Pro**

| Характеристика            | PDF (подход rag_builder)                                   | Markdown / Plain Text                                   |
| :------------------------ | :--------------------------------------------------------- | :------------------------------------------------------ |
| **Обработка моделью**     | Мультимодальная (Vision \+ Text). Модель "видит" страницу. | Текстовая (Tokenization). Модель читает поток символов. |
| **Сохранение структуры**  | Высокое. Отступы и верстка фиксированы.                    | Высокое, но зависит от парсера токенизатора.            |
| **Шум и ошибки**          | Риск ошибок OCR/извлечения (путаница l/1, {/().            | Отсутствует (чистый текст).                             |
| **Поддержка цитирования** | Отличная. NotebookLM подсвечивает место в PDF.             | Хорошая, но навигация менее визуальна.                  |
| **Плотность информации**  | Ниже (из-за заголовков страниц, полей).                    | Максимальная.                                           |
| **Обработка таблиц/схем** | Превосходная (Vision понимает 2D структуру).               | Затруднительная (ASCII-таблицы могут "поплыть").        |

**Анализ:** Использование fpdf2 для генерации PDF из _текста кода_ — это компромисс. Мы берем чистый текст, превращаем его в визуальное представление (PDF), чтобы модель затем снова превратила его в смысл. Исследования указывают, что для чистого текста и кода Markdown часто предпочтительнее, так как исключает слой OCR/Vision, который может быть источником ошибок (галлюцинаций) на специфических символах синтаксиса. Однако, в контексте NotebookLM, PDF имеет преимущество как **контейнер**. Он воспринимается системой как целостный документ, к которому удобно привязывать цитаты. Если проект содержит не только код, но и документацию с таблицами или диаграммами, PDF становится безальтернативным лидером благодаря мультимодальным возможностям Gemini.  
Таким образом, выбор PDF в rag_builder оправдан желанием создать универсальный контейнер, совместимый с UX NotebookLM, даже если это несет небольшие накладные расходы на точность распознавания мелкого синтаксиса.

## **7\. Рекомендации по эволюции архитектуры**

На основе проведенного анализа можно сформулировать ряд рекомендаций для улучшения системы «Spirit-Archivist»:

1. **Переход на Markdown-контейнеры для кода:** Для файлов с расширением .py, .js, .json рассмотреть возможность создания агрегированных .md файлов вместо PDF. NotebookLM поддерживает Markdown. Это исключит риски OCR-ошибок и проблем с кодировкой fpdf2, сохранив при этом структуру (заголовки \# Filename работают как разделители).
2. **Автоматизация очистки (Garbage Collection):** Внедрить логику "пересборки" архивных томов (part_N.pdf). Текущая система только добавляет данные (Append-Only). Со временем архивы могут накопить много удаленного из проекта кода ("мертвого груза"). Скрипт ре-архивации, запускаемый раз в месяц, мог бы пересобирать все тома заново, удаляя устаревший код и экономя контекстное окно модели.
3. **Углубленная интеграция метаданных:** Добавить в заголовки файлов (Header) не только путь и таймстемп, но и краткое описание (например, первые строки docstring) или теги категорий. Это поможет модели быстрее ориентироваться в содержимом при поиске ("Router Pattern").

## **Заключение**

Инструментарий rag_builder представляет собой зрелый пример инженерной адаптации к ограничениям GenAI-платформ. Вместо пассивного ожидания расширения лимитов NotebookLM, разработчики системы создали эффективный ETL-пайплайн, реализующий стратегию инкрементального обновления («Живая Летопись»).  
Архитектура демонстрирует глубокое понимание принципов работы RAG: важность свежести данных, опасность дубликатов (разрешаемую через find_paradox.py) и необходимость наблюдаемости (audit_index.py). Использование DVC и хеширования SHA256 привносит в процесс управления знаниями дисциплину DevOps.  
Хотя использование PDF для хранения кода является спорным решением с точки зрения чистой эффективности токенизации, оно оправдано спецификой мультимодального интерфейса Gemini 1.5 Pro и NotebookLM. В совокупности, «Мастерская Духа-Архивариуса» является надежным фундаментом для построения "Второго Мозга" на базе кодовой базы проекта, превращая хаос файловой системы в упорядоченную библиотеку знаний.

#### **Источники**

1\. Gemini 1.5: Unlocking multimodal understanding across millions of tokens of context \- arXiv, https://arxiv.org/pdf/2403.05530 2\. How to Upload More Files to NotebookLM? Quick Fixes and Better Alternatives (2026), https://elephas.app/blog/how-to-upload-more-files-notebooklm 3\. Frequently asked questions \- NotebookLM Help \- Google Help, https://support.google.com/notebooklm/answer/16269187?hl=en 4\. Document understanding | Gemini API \- Google AI for Developers, https://ai.google.dev/gemini-api/docs/document-processing 5\. Our next-generation model: Gemini 1.5 \- Google Blog, https://blog.google/technology/ai/google-gemini-next-generation-model-february-2024/ 6\. Is it better to upload .txt or pdf files? : r/notebooklm \- Reddit, https://www.reddit.com/r/notebooklm/comments/1m68k3n/is\_it\_better\_to\_upload\_txt\_or\_pdf\_files/ 7\. NotebookLM: A Guide With Practical Examples \- DataCamp, https://www.datacamp.com/tutorial/notebooklm 8\. Add or Discover New Sources in NotebookLM \- The New School, https://services.newschool.edu/TDClient/32/Portal/KB/PrintArticle?ID=1371 9\. Why Your RAG Is Wrong: The Ultimate Guide to Production-Ready Embedding Management, https://www.pixeltable.com/blog/embedding-management-guide 10\. Rethinking the Role of Demonstrations: What Makes In-Context Learning Work?, https://www.researchgate.net/publication/372929181\_Rethinking\_the\_Role\_of\_Demonstrations\_What\_Makes\_In-Context\_Learning\_Work 11\. 10 Systems That Duplicate Content and Cause Errors in RAG Systems \- Shelf.io, https://shelf.io/blog/10-ways-duplicate-content-can-cause-errors-in-rag-systems/ 12\. FPDF Cyrillic letters \- python \- Stack Overflow, https://stackoverflow.com/questions/66550964/fpdf-cyrillic-letters 13\. Versioning Data and Models | Data Version Control \- DVC Documentation, https://doc.dvc.org/use-cases/versioning-data-and-models 14\. Intro to MLOps: Data and model versioning \- Wandb, https://wandb.ai/site/articles/intro-to-mlops-data-and-model-versioning/ 15\. Data Version Control Best Practices | lakeFS, https://lakefs.io/data-version-control/dvc-best-practices/ 16\. Automated version control for LLMs using DVC and CI/CD \- CircleCI, https://circleci.com/blog/automated-version-control-for-llms-using-dvc-and-ci-cd/ 17\. The PDF→Markdown→LLM Pipeline : r/OpenAI \- Reddit, https://www.reddit.com/r/OpenAI/comments/1lmgnfm/the\_pdfmarkdownllm\_pipeline/ 18\. Add or discover new sources for your notebook \- Computer \- NotebookLM Help, https://support.google.com/notebooklm/answer/16215270?hl=en\&co=GENIE.Platform%3DDesktop
