# main.py Specification

## 1. Meta Information

- Domain: ML/NLP | Infrastructure
- Complexity: Medium
- Language: Python
- Frameworks: FastAPI, Pydantic, Transformers
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот файл описывает портал-ритуал, который принимает текст через API, проводит анализ эмоциональной окраски с помощью трансформерной скрижали и возвращает результат. Он демонстрирует адаптацию локального FastAPI-приложения к облачной облаке-цитадели, запускемой в Google Cloud Run, где точку входа задаёт main.py и переменная окружения PORT управляет портом службы.
Instruction for AI: Этот раздел раскрывает источник мотивации и зачем нужен код — чтобы превратить локальный прототип в управляемый serverless сервис с предсказанием тональности текста.

Описание на русском языке:
Этот модуль служит интерфейсом для облачного сервиса анализа настроения текста. Он загружает Дух анализа настроения из библиотеки трансформеров, разворачивает портал FastAPI, принимает запросы в формате JSON, возвращает результат анализа в формате JSON и иллюстрирует, как работать в окружении с портом, заданным через PORT.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: API Request
- Format: JSON
- Schema:
  interface InputData {
  text: string;
  }

### 3.2. Outputs (Выходы)

- Destination: API Response
- Format: JSON
- Success Criteria: 200 OK
- Schema:
  interface OutputResult {
  result: {
  label: string;
  score: number;
  };
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Вступление и призыв к знанию: под рукой собираются необходимые артефакты — FastAPI для портала, BaseModel для чертежей данных и pipeline из transformers как духа анализа.
2. Призыв Духа Эмоций: создаётся дух анализа через конвейер sentiment-analysis с точной настройкой модели distilbert-base-uncased-finetuned-sst-2-english.
3. Чертеж входа: формируется чертёж TextInput с полем text, который будет получать сообщение в виде строки.
4. Вратá портала: разворачивается портал через объект FastAPI с названием и описанием, задаётся версия артефакта.
5. Привязка врата: открываются врата по маршруту /analyze и клятвенно принимают POST-запросы.
6. Обработка запроса: извлекается текст из полученного эфира, передаётся духу анализа.
7. Возвращение вывода: результат анализа возвращается в виде JSON с полем result, содержающим метку и оценку.
8. Принцип работы портала: сценарий не запускает портал самостоятельно; для пробуждения используется команда запуска сервера с uvicorn main:app --reload.

Эта последовательность превращает сцену в живую систему: Эфир поступает, Хаос прогнозов разъясняется, а пользователь получает ясную сигнатуру настроения.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory of Assets (Экипировка артефактов) в виде запаса для 1-в-1 воспроизведения:

- 🏰 Главный портал: FastAPI приложение с именем Говорящий Амулет Техноманта, версия 1.0, описание — магический портальный инструмент для определения эмоциональной окраски текста.
- 🛡️ Дух анализа: конвейер sentiment-analysis из transformers, настроенный на модель distilbert-base-uncased-finetuned-sst-2-english.
- 📜 Чертеж входа: TextInput с полем text типа string, который задаёт структуру входящего сообщения.
- 🔮 Врата портала: маршрут POST /analyze, который принимает TextInput и возвращает OutputResult.
- 🗝️ Эмпирический ключ модели: модель distilbert-base-uncased-finetuned-sst-2-english, используемая в духе анализа.
- ⚙️ Запуск и окружение: команда запуска uvicorn main:app --reload; переменная окружения PORT определяет порт в облаке (Cloud Run).

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции:
  - analyze_sentiment: обработчик POST /analyze, принимает TextInput и возвращает результат анализа.
- Классы:
  - TextInput: схема входных данных с полем text.
- Главный артефакт:
  - app: экземпляр FastAPI, который служит порталом.
- Внутренние артефакты:
  - sentiment_analyzer: созданный дух анализа через pipeline("sentiment-analysis", model="distilbert-base-uncased-finetuned-sst-2-english").

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Опора на трансформерную модель требует умеренного потребления памяти и процессорной мощности; в облаке может существовать ограничение памяти и времени жизни экземпляра.
- Concurrency: Синхронная обработка запроса (Synchronous), FastAPI поддерживает асинхронность, но данный обработчик реализован синхронно.
- Dependencies: fastapi, pydantic, transformers (и связанные зависимости torch/adapter-y), необходимы в окружении.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (используйте .env или секреты окружения).
- DO NOT print raw data to console in production mode.
- DO NOT use synchronous network calls in the main loop без обоснования асинхронности.
- DO NOT wrap конфигурационные файлы (.yaml, .json) в скрипты.
- DO NOT изменять версии или пути во время реконструкции без документирования.

## 7. Verification & Testing (Верификация)

1. Гэркин-сценарий 1 — Счастливый путь
   Feature: Sentiment Analysis Portal
   Scenario: Successful execution
   Given a valid payload { "text": "I love this service" }
   When POST /analyze is called
   Then the response status is 200 OK
   And the response body contains a result object with a label (POSITIVE or NEGATIVE) and a numeric score

2. Гэркин-сценарий 2 — Ошибка ввода
   Feature: Sentiment Analysis Portal
   Scenario: Invalid payload
   Given an invalid payload (missing text)
   When POST /analyze is called
   Then the response status is 422 Unprocessable Entity

Дополнительная ремарка: этот артефакт описывает процесс адаптации локального FastAPI к облачной среде. Все взаимодействия происходят через Эфир JSON-данных, а вывод — через ответ сервера в формате JSON. Пробуждение портала осуществляется внешним управляющим командованием uvicorn main:app --reload, и порты подчиняются переменной PORT, как требует Облачная Цитадель.
