# validation_data.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: Low
- Language: Python
- Frameworks: None (Pure Python)
- Context: (нет явного глобального контекста импорта, данный артефакт предназначен для импорта другими модулями, например main.py)

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот модуль хранит «Камень Истины» — нерушимый эталон для верификации поведенческих механизмов модели. Он реализует набор испытаний, который будет использоваться главным пергаментом main.py для проведения ритуала самопроверки и демонстрации самодиагностики финального Голема.

**Instruction for AI:** Это секция концептуального замысла: зачем нужен данный модуль, какие ценности и проверки он несет в общий пайплайн.

Описание на русском языке: Это статический набор данных, представляющий собой список тест-кейсов текстов и соответствующих меток настроенности (POSITIVE/NEGATIVE). Набор служит эталоном для проверки того, что компоненты пайплайна могут корректно распознавать базовую эмоциональную окраску текста и возвращать ожидаемые метки. Он не является исполняемым ритуалом сам по себе, а предназначен для импорта и использования в процессе самопроверки внутри Docker-контейнера CI/CD.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: Module Import
- Format: Python Object
- Schema:
  interface InputData {
  // Этот артефакт не принимает внешние входные данные во время выполнения.
  // Данные закодированы внутри модуля как VALIDATION_SET.
  }

### 3.2. Outputs (Выходы)

- Destination: Module Export
- Format: JSON-подобный объект при импорте (в памяти)
- Success Criteria: Экспортируется валидный VALIDATION_SET как массив элементов с полями text и label
- Schema:
  interface ValidationEntry {
  text: string;
  label: 'POSITIVE' | 'NEGATIVE';
  }

  interface OutputResult {
  validationSet: ValidationEntry[];
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. При загрузке модуля создается нерушимая константа VALIDATION_SET, которая хранит набор испытаний.
2. Каждый элемент VALIDATION_SET — это объект с двумя полями: text и label.
3. Значение поля label должно быть строго либо 'POSITIVE', либо 'NEGATIVE'. Любые иные метки считаются нарушением структуры и не допускаются.
4. Элементы упорядочены и образуют последовательность из десяти тестов, соответствующих различным эмоциональным аккордам текста.
5. Комментарии внутри файла поясняют назначение набора и его роль в процессе самопроверки финального артефакта.
6. Модуль не выполняет побочных эффектов при импорте; он предоставляет данные для использования другими модулями (например, main.py).
7. Данные не зависят от внешних сетевых источников; модуль полностью автономен.
8. При необходимости набор может быть расширен, но структура и типы полей остаются неизменными.
9. Внешнее использование предполагает чтение VALIDATION_SET через именованный экспорт; данные читаются как обычный Python-объект.

### 4.2. Declarative Content (Для конфигураций и данных)

Инвентарь артефакта — Набор Испытаний (Validation Set). Превращение YAML/JSON-списка в РПГ-атрибуты:

- 🏰 Набор Испытаний: Эталонный блок данных, называемый VALIDATION_SET, содержащий десять записей тестов.
  - Испытание 1 — текст: "I love this product, it is absolutely amazing!"; метка: POSITIVE
  - Испытание 2 — текст: "This is the worst service I have ever received in my life."; метка: NEGATIVE
  - Испытание 3 — текст: "The movie was fantastic, a true masterpiece."; метка: POSITIVE
  - Испытание 4 — текст: "I am so disappointed with the quality, it broke after one day."; метка: NEGATIVE
  - Испытание 5 — текст: "What a wonderful experience, I would recommend it to everyone."; метка: POSITIVE
  - Испытание 6 — текст: "A complete waste of time and money, I regret buying this."; метка: NEGATIVE
  - Испытание 7 — текст: "The team was very helpful and friendly."; метка: POSITIVE
  - Испытание 8 — текст: "The food was terrible and the waiter was rude."; метка: NEGATIVE
  - Испытание 9 — текст: "An outstanding performance by the entire cast."; метка: POSITIVE
  - Испытание 10 — текст: "I will never come back to this place again."; метка: NEGATIVE

Каждый элемент носит сигнатуру: текст — текстовый фрагмент; label — одна из двух допустимых меток: POSITIVE или NEGATIVE. Элементы выступают как автономные анкеты для проверки корректности обработки текста и вывода метки.

## 5. Structural Decomposition (Декомпозиция структуры)

- Докстринг модуля (описание цели, контекста и роли набора)
- Константа VALIDATION_SET: массив объектов с полями text и label
- Экспортируемые символы: validationSet (или аналогичный экспорт структуры, соответствующий OutputResult)

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартная производительность, минимальная нагрузка на память
- Concurrency: синхронная загрузка при импорте; без асинхронности
- Dependencies: Python стандартной библиотеки; отсутствуют внешние зависимости

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ загружать данные из сети во время импорта
- НЕ хранить секреты или чувствительную информацию в открытом виде
- НЕ мутировать VALIDATION_SET во время выполнения импорта
- НЕ выводить сырые данные в консоль в продакшн-режиме
- НЕ заключать конфигурационные файлы (.yaml, .json) внутрь исполняемого кода
- НЕ менять версии или пути к данным во время реконструкции артефакта

## 7. Verification & Testing (Верификация)

1. Геркин-сценарий: Успешное выполнение
   Feature: Validation data module loads correctly
   Scenario: Successful load
   Given the module is imported
   When the code accesses VALIDATION_SET
   Then the set contains 10 entries and each entry has non-empty text and label either POSITIVE or NEGATIVE

2. Геркин-сценарий: Ошибка в наборе (некорректная метка)
   Feature: Validation data module loads correctly
   Scenario: Invalid label in set
   Given the set includes an entry with label "NEUTRAL"
   When the dataset is loaded
   Then an error is raised or the invalid structure is rejected by the loader

ИССЛЕДУЕМЫЙ АРТЕФАКТ: validation_data.py
ИСТОЧНAЯ ФРАЗА: как хранение набора для самопроверки и диагностики, не исполняющий ритуал сам по себе, но служащий основой для уверенности ИИ в процессе сборки и развёртывания.
