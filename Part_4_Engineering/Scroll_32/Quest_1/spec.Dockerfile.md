# Dockerfile Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: Docker
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот Ritual упаковывает Python-приложение в образ Docker, устанавливает зависимости из свитка libraries.list и копирует все пергаменты проекта, а затем активирует Главную Ритуальную Жизнь через скрипт start.sh. Финальный образ запускает uvicorn, слушая на 0.0.0.0 и порту, указанному в окружении, чтобы артефакт мог быть развернут в облаке или локально.  
**Instruction for AI:** Этот раздел объясняет WHY — зачем создается этот Ritual и какие бизнес-задачи он решает: быстрая развёртка сервиса на основе Python, надёжная установка зависимостей и чистая финальная среда без лишних артефактов.

Ключ к задаче: обеспечить надёжное исполнение Главного Заклинания Жизни в чистой ASCII-среде, как это принято у Орденов, и позволить миру получить доступ к сервису через uvicorn.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: JSON
- Schema:

```ts
interface InputData {
  buildArgs?: string[]; // примеры аргументов сборки
  librariesListPath?: string; // путь к libraries.list
  startScriptPath?: string; // путь к start.sh (обычно ./start.sh)
  workDir?: string; // рабочая директория, обычно /app
  hasStartScript?: boolean; // признак существования стартового скрипта
}
```

### 3.2. Outputs (Выходы)

- Destination: API Response
- Format: JSON
- Success Criteria: ImageId возвращён успешно, возврат к статусу успешной сборки
- Schema:

```ts
interface OutputResult {
  success: boolean;
  imageId?: string;
  exitCode?: number;
  logs?: string;
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Берём базовый трон образа как основу: Python 3.10. Это стартовая караула, из которой рождается новый артефакт.
2. Определяем рабочую долину проекта как /app и направляем туда все эзотерические пергаменты.
3. Из свитка зависимостей копируем на полотно в образ путь libraries.list. Это свиток, который определяет эфир зависимостей, необходимых для работы артефакта.
4. Призываем алхимика пакетного менеджера и выполняем установку зависимостей через pip install --no-cache-dir -r libraries.list, чтобы эфир зависимостей вступил в силу без остатков хаоса кэша.
5. Копируем все остальное содержимое мастерской (интерфейсов, кода, конфигураций) внутрь рабочего тронa образа, чтобы артефакт был завершённый и самодостаточный.
6. Присваиваем стартовый заклятийный пергаменту start.sh право на жизнь (chmod +x ./start.sh) — чтобы он мог исполняться в чистой среде.
7. Финальная команда запуска заклинания жизни: CMD ["./start.sh"]. Это запускает uvicorn и поднимает порт, указанный в окружении, на хосте 0.0.0.0.

### 4.2. Declarative Content (Для конфигураций и данных)

Имущество артефакта превращено в RPG-объекты:

- 🏰 Dockerfile: Сам ритуальный свиток сборки образа. Он задаёт базовый образ python:3.10, устанавливает рабочую директорию /app, копирует свиток libraries.list, выполняет установку зависимостей, копирует весь пергамент проекта, обеспечивает исполнение start.sh и задаёт главную команду CMD для запуска сервиса.
- 🛡️ libraries.list: Скрижаль зависимостей для Python-пакетов, которые должны быть установлены в финальном образе через pip install --no-cache-dir -r libraries.list.
- 🏰 start.sh: Ритуальный стражник, запускающий Голема через uvicorn и устанавливающий параметры доступа к сервису (host 0.0.0.0, port из окружения PORT).
- 🗡️ app (Эфир): Секция проекта с приложением, которое будет обслуживаться uvicorn; вытекает из копирования всего живого кода проекта в /app.

Lore notes: Скрипт внутри Использует ASCII-среду, чтобы избежать проклятий кодировок. Все пути и имена файлов остаются неизменными и читаемыми, чтобы Великий Архиватор мог воссоздать артефакт точно по инструкции.

## 5. Structural Decomposition (Декомпозиция структуры)

- BaseImageSetup: FROM python:3.10 — задаёт базовый консилиум образа.
- WorkingDirectorySetup: WORKDIR /app — устанавливает рабочий храм проекта.
- DependenciesDeclaration (Скрижаль зависимостей): COPY libraries.list . — приносит свиток зависимостей в храм.
- DependencyInstallationRitual: RUN pip install --no-cache-dir -r libraries.list — вызывает алхимию установки зависимостей.
- ArtifactCopy: COPY . . — приносит все пергаменты проекта в образ.
- ExecutionPreparation: RUN chmod +x ./start.sh — даёт стартовую силу start.sh.
- ExecutionCommand: CMD ["./start.sh"] — призывает Головного Жреца жизни к пробуждению.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU функционал; образ рассчитан на обычные серверные нагрузкИ.
- Concurrency: Асинхронность допустима в приложении (uvicorn поддерживает async), но сама сборка Docker и запуск в CMD не зависят от параллелизма здесь.
- Dependencies: Базовый образ Python 3.10; pip; uvicorn (зависимость приложения, запускаемая через start.sh); libraries.list определяет точные пакеты.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ хранить секреты в открытом виде в образе; используйте окружение (.env) или секреты оркестратора.
- НЕ выводите сырые данные в консоль в продуктивной среде без необходимости.
- НЕ используйте синхронные сетевые вызовы в критических петлях исполнения.
- НЕ оборачивайте конфигурационные файлы (.yaml, .json) в скрипты.
- НЕ изменяйте версии или пути во время реконструкции артефакта.

## 7. Verification & Testing (Верификация)

### Герхин-Сценарии

Feature: Dockerfile Ritual Functionality
Scenario: Successful build and run
Given the build context contains libraries.list, start.sh and a valid app at /app
When the image is built and a container is run with PORT environment variable set
Then uvicorn starts and the app is reachable on 0.0.0.0:PORT with a healthy response

Scenario: Missing dependencies file
Given libraries.list is absent or unreadable
When the image is built
Then the build fails with an error indicating missing libraries.list

Итог артефакта: Dockerfile, как часть Великой Сети Инфраструктуры, готов к развертыванию в любой среде, где нужна надёжная упаковка Python-приложения и пробуждение его к жизни через чистую ASCII-среду.
