# docker-compose.yml Specification

ИССЛЕДУЕМЫЙ АРТЕФАКТ: docker-compose.yml

ИНСТРУКЦИИ ПО РАЗДЕЛАМ:

- Meta Information: Определи Стихию (Язык). Context укажи только если есть явные импорты.
- Goal & Purpose: Легенду. Зачем нужен этот файл.
- Interface Contract: Входы и Выходы в формате TypeScript интерфейсов.
- Implementation Details: Опиши алгоритм словами шаг за шагом. Не используй скобки и заглушки.
- Declarative Content: ОПИСЬ ИМУЩЕСТВА (Inventory). Преврати данные в маркированный список с эмодзи. Не копируй код!

1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: Docker, Prometheus, Grafana
- Context: ../AGENTS.md

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот файл является телом ритуала ордена, связывающим три духа инфраструктуры в единый механизм: приложение, страж метрик и Провидец зеркал. Он порождает сеть, открывает порталы и разворачивает образы, чтобы мир увидел живую артерию мониторинга.
   Instruction for AI: Этот раздел передает WHY — зачем нужен этот файл, какую бизнес-задачу он решает и какова цель конфигурации.

Легенда: В тот момент, когда skryf стальной шторм докучает системе, этот скрижалль собирает вокруг него три духа в единый цикл: app как сердце, prometheus как охранник метрик и grafana как окно в мир данных. Их сотрудничество освещает путь к стабильности и наблюдаемости, а сеть monitoring-net соединяет их в единый поток.

3. Interface Contract (Интерфейсный Контракт)
   Instruction for AI: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema: interface InputData {
  // поля конфигурации внешних вызовов будут определяться точным окружением запуска
  }

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit Code 0
- Schema: interface OutputResult {
  // поля вывода зависят от контекста вызова и состояния развертывания
  }

4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Шаг за шагом, без кодов:

1. Прочитай залог версии гримуара — версия 3.8; это заклинание, которое задает правила чтения свитка docker-compose.yml.
2. Для каждого духа в артефакте подготавливай параметры ритуала: app, prometheus, grafana.
   - app: билд контекстом текущей мастерской, порты открыты как порталы 8000 к 8000, подключение к магическому каналу monitoring-net, переменная PORT установлена в 8000.
   - prometheus: образ prom/prometheus:latest, имя–контейнер prometheus_guardian, монтирование свитка ./prometheus.yml в /etc/prometheus/prometheus.yml, порт 9090, зависимости от app, сеть monitoring-net.
   - grafana: образ grafana/grafana:latest, имя–контейнер grafana_seer, порт 3000, зависимость от prometheus, сеть monitoring-net.
3. Создай или убедись в существовании магического канала monitoring-net с драйвером bridge.
4. Прогони проверку целостности: доступность образов, доступность файла prometheus.yml, корректность портальных привязок и сетевой совместимости.
5. Запусти ритуал разворачивания: все духи запускаются одновременно, но их положение синхронизируется по зависимостям (примеры: grafana после prometheus, prometheus после app).
6. Наблюдай за ходом — порты 8000, 9090 и 3000 открыты, духи живы и общаются, метрики собираются, панели Grafana доступны для визуализации.
7. Обеспечь действие защитных чар: логи пишет в безопасном месте, не выводятся секреты в поток мониторов; в случае ошибок — регистрируются и возвращают управляемый код возврата.

### 4.2. Declarative Content (Declarative Content)

Inventory данного артефакта превращен в предметы RPG-мира:

- 🏰 docker-compose.yml — Гримуар конфигурации, который управляет развертыванием трех духов и их связями.
- 🛡️ app — Артефакт приложения, который билдится из контекста текущей мастерской и запускается на порту 8000.
- 🛡️ prometheus — Страж метрик, образ prom/prometheus:latest, слушает порт 9090, зависим от app, подключен к monitoring-net, используя свиток prometheus.yml.
- 🛡️ grafana — Провидец зеркал, образ grafana/grafana:latest, слушает порт 3000, зависим от prometheus, подключен к monitoring-net.
- 🛡️ monitoring-net — Магический канал связи между духами, мост между сервисами через драйвер bridge.
- 🏰 prometheus.yml — Свиток конфигураций мониторинга, монтируется в /etc/prometheus/prometheus.yml внутри духа prometheus.
- 🏰 Dockerfile (упоминание) — контекст сборки для app, который задает чертежи для создания артефкта приложения.

5. Structural Decomposition (Декомпозиция структуры)
   Instruction for AI:

- Для кода: перечисли функции и классы.
- Для конфигов: перечисли основные логические блоки (например: repos, services, build_stages).

Структура артефакта состоит из трех духо-сервисов и их связующего канала:

- Сервис app — сердце ритуала, сервис сборки и запуска.
- Сервис prometheus — охранник данных, собирает метрики и хранит их в своей летописи.
- Сервис grafana — провидец визуализации, читает данные prometheus и рисует панели.
- Сеть monitoring-net — канал связи для всеобщего обмена.
- Свиток prometheus.yml — конфигурация монитора, монтируется внутрь prometheus.
- Контейнеры взаимодействуют по порталам: 8000, 9090, 3000.

6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Духи запускаются параллельно после разрешения зависимостей; сами контейнеры работают независимо.
- Dependencies: Docker Engine, Docker Compose, prom/prometheus:latest, grafana/grafana:latest, prometheus.yml

### 6.2. Prohibited Actions (Negative Constraints)

- Не хранить секреты в открытом виде в логе или на диске.
- Не выводить сырые данные в консоль в продакшене.
- Не использовать синхронные сетевые вызовы в главном цикле оркестра.
- Не оборачивать конфигурационные файлы .yaml/.json в скрипты.
- Не менять версии образов или пути к ресурсам во время реконструкции.

7. Verification & Testing (Верификация)
   Instruction for AI: Generate 1-2 Gherkin scenarios that describe the happy path and one error case for this script.

Геркин сценарии:

Feature: Docker Compose deployment
Scenario: Successful deployment
Given Docker daemon is running and docker-compose.yml is present in the working directory
When the command docker-compose up -d is executed
Then app should be up and listening on port 8000
And prometheus should be up and listening on port 9090
And grafana should be up and listening on port 3000
And the monitoring-net network should be established

Scenario: Port conflict during deployment
Given port 8000 is already bound by another process
When the command docker-compose up -d is executed
Then the deployment should fail with a port binding error
And logs should indicate the conflict without exposing secrets

Стиль и тон: Полный и без пропусков, с магическим флёром, где путь к файлу и связи между духами описаны как ритуал и скрижаль. Все элементы остаются в реальном контексте инфраструктуры, без фальшивых данных, соблюдая набор переменных и путей как в исходном артефакте.
