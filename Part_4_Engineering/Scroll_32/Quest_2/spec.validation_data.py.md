# validation_data.py Specification

ИССЛЕДУЕМЫЙ АРТЕФАКТ: validation_data.py

Камень Истины хранится здесь: набор тестов для проверки способности Голема к самодиагностике. Этот артефакт параллелен основному скрипту main.py и будет запечатан в Docker-контейнере, чтобы система могла пройти через Ритуал самопроверки в CI/CD. Являясь частью Скрижали, он не исполняется сам по себе, а служит эталоном для верификации мудрости артефактов.

## 1. Meta Information

- **Domain:** ML/NLP
- **Complexity:** Medium
- **Language:** Python
- **Frameworks:** None (Python standard library)
- **Context:** Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот файл хранит Рунный Набор Испытаний, предназначенный для проверки исцеляющей силы самодиагностики главного Голема. Он позволяет CI/CD подтвердить, что система может самостоятельно проверить свои алгоритмы анализа текста и корректно верно закодировать ответы валидации.
**Instruction for AI:** This section provides high-level intent. Use it to understand the "WHY" of the code.

Этот файл служит хранилищем стабильного набора примеров для валидации эмоциональной окраски текста. Он не выполняет вычислений, а предоставляет универсальные эталоны, чтобы главный процесс мог сверить корректность обработки данных.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

Source: Static Inline Data (нет внешних входов во время исполнения артефакта)
Format: N/A
Schema: interface InputData {
// Этот артефакт не принимает внешних входов
}

### 3.2. Outputs (Выходы)

Destination: Not Applicable
Format: N/A
Success Criteria: Not Applicable
Schema: interface OutputResult {
// Этот артефакт не производит выход в явном виде
}

Примечание: основной поток взаимодействия происходит через main.py, который читает и валидирует эталоны, хранящиеся в VALIDATION_SET.

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Этот артефакт не содержит исполняемого алгоритма в рамках своей роли. Он служит контейнером данных. Ритуал выглядит так: определить константный набор VALIDATION_SET и наполнить его элементами-испытаниями. Каждый элемент — это пара текст и ожидаемая метка. Главный скрипт (main.py) подхватывает этот набор, запускает проверку самодиагностики и сверяет результаты с эталонами. В ходе выполнения ритуала не выполняются вычисления внутри validation_data.py; данные просто существуют, чтобы быть прочитанными и сверенными.

### 4.2. Declarative Content (Для конфигураций и данных)

Содержимое данное в декоративной форме вInventory ниже отражает структуру и содержимое Эталона “Камня Истины”.

- 🏰 Испытание 1: текст "I love this product, it is absolutely amazing!" | метка POSITIVE
- 🏰 Испытание 2: текст "This is the worst service I have ever received in my life." | метка NEGATIVE
- 🏰 Испытание 3: текст "The movie was fantastic, a true masterpiece." | метка POSITIVE
- 🏰 Испытание 4: текст "I am so disappointed with the quality, it broke after one day." | метка NEGATIVE
- 🏰 Испытание 5: текст "What a wonderful experience, I would recommend it to everyone." | метка POSITIVE
- 🏰 Испытание 6: текст "A complete waste of time and money, I regret buying this." | метка NEGATIVE
- 🏰 Испытание 7: текст "The team was very helpful and friendly." | метка POSITIVE
- 🏰 Испытание 8: текст "The food was terrible and the waiter was rude." | метка NEGATIVE
- 🏰 Испытание 9: текст "An outstanding performance by the entire cast." | метка POSITIVE
- 🏰 Испытание 10: текст "I will never come back to this place again." | метка NEGATIVE
- 🛡️ Руна закрывает свиток: эти данные служат хранителем мудрости и питают контекст тестирования.

## 5. Structural Decomposition (Декомпозиция структуры)

- Валидируемый объект: константа VALIDATION_SET, массив из объектов { text: string, label: "POSITIVE" | "NEGATIVE" }.
- Элементы: десять записей с текстом и меткой.
- Локальные сущности: никакие функции или классы отсутствуют в этом артефакте; данные — единственный элемент структуры.
- Связь с внешним кодом: main.py обращается к VALIDATION_SET для проведения ритуала самопроверки.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- **Performance:** Standard CPU, блокировка минимальна; рассчитано на быстрый доступ к фиксированному набору данных в памяти.
- **Concurrency:** Синхронно; данные не предполагают конкурентный доступ.
- **Dependencies:** Никаких внешних зависимостей; ограничено стандартной библиотекой Python и встроенными константами.
- **Paths/Names:** Имя константы и переменные сохранены без изменений: VALIDATION_SET; упоминания main.py остаются как в описании.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (use .env).
- DO NOT print raw data to console in production mode.
- DO NOT use synchronous network calls in the main loop.
- DO NOT wrap configuration files (.yaml, .json) into scripts (like Python/Bash).
- DO NOT change versions or paths during reconstruction.

## 7. Verification & Testing (Верификация)

Геркин сценарии для счастья и ошибки:

Feature: Validation Data Integration
Scenario: Successful loading of VALIDATION_SET
Given the file validation_data.py is present and defines VALIDATION_SET with 10 items
When the main ritual is invoked to perform self-check
Then the dataset is loaded and contains 10 items with proper text and POSITIVE or NEGATIVE labels

Scenario: Invalid dataset item leads to failure
Given the file validation_data.py contains an item missing the text field
When the main ritual performs the self-check
Then the self-check fails with an appropriate validation error

Задача выполнена: артефакт validate_data.py подтверждает себя как источник информации для самопроверки и служит мостом между манускриптом и зеркалом CI/CD.
