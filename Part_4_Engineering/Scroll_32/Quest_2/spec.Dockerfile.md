# Dockerfile Specification

1. Meta Information

- Domain: Infrastructure
- Complexity: Low
- Language: Bash
- Frameworks: Docker, Python 3.10, uvicorn
- Context: Independent Artifact

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот ритуал-процесс упаковывает Python-приложение в единый образ, устанавливая зависимости из свитка libraries.list и гарантируя корректный запуск сервиса через uvicorn в чистой ASCII-среде. Он обеспечивает повторяемость развёртывания и изоляцию окружения.
   Instruction for AI: Понимайте WHY: чтобы можно было быстро построить исполняемую IMAGE для запуска ASGI-приложения.

Описание: Этот скриптовый артефакт создаёт Docker-образ на основе Python 3.10, копирует зависимости, копирует все пергаменты проекта, устанавливает исполняемость стартового скрипта и запускает приложение через uvicorn, слушая все сети и порт, указанный во внешнем контексте (PORT).

3. Interface Contract (Интерфейсный Контракт)
   Instruction for AI: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

3.1. Inputs (Входы)
Source: None (Dockerfile не принимает runtime-входов через CLI/STDIN/API)
Format: N/A
Schema:
InputData {
// Нет полей ввода в этом артефакте
}

3.2. Outputs (Выходы)
Destination: Image (Docker-образ)
Format: Binary
Success Criteria: Exit Code 0
Schema:
OutputResult {
imageName: string; // например, имя образа
tag?: string; // тег образа
digest?: string; // дайджест образа
}

4. Implementation Details (The Source DNA / Исходный Код)

4.1. Algorithmic Logic (Для исполняемого кода)
Ход ритуала:

1. Взываем к первоисточнику огня и выбираем базовый образ Python 3.10 в качестве основы ритуала.
2. Устанавливаем рабочую директорию артефакта в /app, чтобы все свитки и пергаменты оживали в едином святилище.
3. Применяем скрижаль зависимостей: копируем libraries.list и готовим её к приёму в чашах пакетов.
4. Прожигаем заклинание установки зависимостей: выполняем пакетный менеджер (pip) с параметром --no-cache-dir, чтобы все чары были чистыми и не хранились в кэше.
5. Переписываем все пергаменты проекта в храм образа: копируем содержимое текущего каталога в рабочее место /app.
6. Подготавливаем скрипт пробуждения start.sh: делаем его исполняемым, чтобы он мог привести артефакт к жизни без помех.
7. Граним Главному Заклинанию Жизни: CMD запускает start.sh, который разворачивает приложение через uvicorn, слушая мир на 0.0.0.0 и порт, заданный в окружении (PORT).
8. Выздоровление и устойчивость: образ готов к развёртыванию в любой среде, где доступен порт и развертывание через Docker.
9. Безопасность и совместимость: использованы стандартные пути и файлы, чтобы минимизировать проблемы совместимости и кодировок.

4.2. Declarative Content (Для конфигураций и данных)

- Base Image: python:3.10
- Рабочая директория: /app
- Источник зависимостей: libraries.list
- Команды установки: pip install --no-cache-dir -r libraries.list
- Копируемые артефакты: все файлы проекта в текущем каталоге на шаг COPY . .
- Исполняемый стартовый скрипт: start.sh (chmod +x)
- Команда запуска: CMD ["./start.sh"]
- Порт: установлен через переменную PORT в запуске start.sh, адрес прослушивания 0.0.0.0

5. Structural Decomposition (Декомпозиция структуры)

- Блок "FROM": выбор базового образа Python 3.10
- Блок "WORKDIR": установка рабочего каталога /app
- Блок "COPY libraries.list .": перенос скрижали зависимостей
- Блок "RUN pip install --no-cache-dir -r libraries.list": Чары зависимостей
- Блок "COPY . .": перенос всех пергаментов проекта
- Блок "RUN chmod +x ./start.sh": дарование жизни скрипту
- Блок "CMD [\"./start.sh\"]": призыв к пробуждению артефакта

6. System Context & Constraints (Системный контекст и Ограничения)

6.1. Technical Constraints

- Performance: стандартный CPU
- Concurrency: Async (uvicorn/ASGI)
- Dependencies: docker, Python 3.10, uvicorn, зависимости из libraries.list

  6.2. Prohibited Actions (Negative Constraints)

- Не хранить секреты в явном виде внутри образа; используйте окружения/.env и ARGs
- Не печатать исходные данные в консоль в продакшн-режиме
- Не применять синхронные сетевые вызовы в основном цикле приложения (в рамках образа это пожелание к приложению)
- Не оборачивать конфигурационные файлы .yaml/.json в скрипты
- Не менять версии базового образа или путей во время реконструкции

7. Verification & Testing (Верификация)

1) Happy path
   Feature: Dockerfile Build and Run
   Scenario: Successful image build and run
   Given Docker daemon is accessible
   When Docker image is built with tag app:latest from Dockerfile
   Then Running container with -p 8000:8000 should expose the app at http://localhost:8000

2) Error case
   Feature: Dockerfile Build Failure on bad dependencies
   Scenario: Build fails due to invalid libraries.list
   Given libraries.list contains invalid package names
   When Docker image build is attempted
   Then Build should fail with a non-zero exit code and log indicating pip install errors

Инвентарь артефакта (Inventory)

- 🏰 Базовый образ: Python 3.10
- 🛡️ Рабочий храм: /app
- 🏰 Скрижаль зависимостей: libraries.list
- 🛡️ Установка зависимостей: pip install --no-cache-dir -r libraries.list
- 🏰 Пергаменты проекта: копирование всех файлов
- 🛡️ Скрипт пробуждения: start.sh (исполняемость)
- 🏰 Главная руна жизни: CMD ./start.sh
- 🛡️ Порт и сеть: PORT через окружение, запуск uvicorn через start.sh
- 🏰 Итоговый артефакт: готовый Docker-образ с приложением на ASGI

Контекст к артефакту: Independent Artifact
