# main.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: Medium
- Language: Python
- Frameworks: FastAPI, Pydantic, Transformers, Prometheus FastAPI Instrumentator, Prometheus Client, pytest (упоминание в контексте тестирования), validation_data
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

- Легенда: Этот файл — Амулет с Развилкой, часть квеста 32.2, призванный служить подопытным для нашего безупречного CI/CD пайплайна: он запускает тесты, строит отчет о покрытии и захватывает артефакт через actions/upload-artifact для сохранения улик в мастерской отладки.
- Инструкция для ИИ: Этот артефакт реализует двухспиритный A/B тест двух моделей на входящем тексте, собирает метрики и обеспечивает возврат предсказания вместе с версией стратегии. Он должен быть понятен как единое API с валидацией данных и самопроверкой via эндпоинт.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: API Request
- Format: JSON
- Schema:
  interface TextInput {
  text: string;
  }

### 3.2. Outputs (Выходы)

- Destination: API Response
- Format: JSON
- Success Criteria: 200 OK
- Schema:
  interface AnalyzeOutput {
  result: any;
  model_version: string;
  }

  interface ValidateOutput {
  model_tested: string;
  accuracy: number;
  correct_predictions: number;
  total_samples: number;
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. В начале инициализируются две духоподобные сущности анализа — model_a и model_b — через пайплайн преобразований: первый дух — DistilBERT, второй — RoBERTa, оба настроены на анализ тональности.
2. Определяется чертеж входящих посланий TextInput, где поля зафиксированы как text: string.
3. Возводится портал FastAPI с именем Амулет с Развилкой и описанием: портал для A/B тестирования двух моделей, с версией конструкции 3.0.
4. Приводится Инструментатор Прометея, который обнажает метрики через портал.
5. Создаются два магических артефакта метрик: Манометр точности (model_accuracy) и Кристалл-счетчик запросов (model_requests_total), причудливо разделяющий подсчеты по измерению model_version.
6. Объявляется входная тропа POST /analyze, которая принимает текст, бросает монету и выбирает между двумя духами (A или B). Световая грань кристалла преломляется и фиксирует выбор версии. Затем текст прогоняется через выбранного духа, и результат возвращается вместе с версией модели.
7. Объявляется тропа POST /validate, которая кольцает тестовый свиток: для каждого испытания из VALIDATION_SET подбирается истинная метка и сравнивается с предсказанием духа A. По итогам вычисляется точность, обновляется метр ACCURACY_GAUGE и возвращается свиток с деталями: Tested model, accuracy, количество верных ответов и общее число испытаний.
8. В завершении залога выводится ритуал самопроверки, закрепляющий статус метрик и готовность портала к эксплуатации, без изменения основного поведения.

### 4.2. Declarative Content (Для конфигураций и данных)

- Скрипт использует VALIDATION_SET как источник тестов для ритуала проверки точности духа А; данные состоят из записей с полем text и истинной меткой label.
- Инструменты и артефакты:
  - Трое: Амулет с Развилкой (FastAPI-портал), Дух А (DistilBERT) и Дух Б (RoBERTa).
  - Манометр точности (model_accuracy) и Кристалл-счетчик запросов (model_requests_total) с разделением по model_version.
- Эфирные элементы ввода/вывода:
  - Вход: текстовые сообщения через API POST /analyze (JSON с полем text).
  - Выход: JSON-ответ с полем result и model_version.
- Свитки данных: VALIDATION_SET — список тестов, каждый элемент которого содержит текст и истинную метку.

## 5. Structural Decomposition (Декомпозиция структуры)

- 🏰 app — FastAPI приложение с титулом "Амулет с Развилкой".
- 🏰 model_a — дух DistilBERT, для анализа первой линии.
- 🛡️ model_b — дух RoBERTa, для альтернативной линии.
- 🛡️ TextInput — класс чертежа входных данных.
- 🛡️ ACCURACY_GAUGE — Манометр точности, отслеживает точность на валидационном наборе.
- 🗝️ REQUEST_COUNTER — Кристалл-счетчик запросов, измеряет количество обращений по версии модели.
- 🚪 Endpoints:
  - /analyze — постовой вход, принимает TextInput, выбирает модель и возвращает результат и версию.
  - /validate — постовой вход, выполняет валидацию по VALIDATION_SET и возвращает статистику.
- 🔮 VALIDATION_SET — набор тестовых примеров для валидации модели A.
- 🧭 VALIDATION_PROCESS — логика перебора и сравнения предсказаний с истиной.
- ⛓️ Метрики Prometheus — сбор и экспонирование данных через Instrumentator.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Оптимизирован под стандартный CPU-окружение; нагрузка может быть существенной из-за двух трансформеров в реальном времени.
- Concurrency: Синхронная обработка функций ( endpoints реализованы как синхронные def, без async).
- Dependencies: Python 3.x, FastAPI, Pydantic, transformers, prometheus_fastapi_instrumentator, prometheus_client, validation_data (модуль с VALIDATION_SET).
- External: Модели загружаются через пайплайн трансформеров и требуют сетевого доступа на этапе загрузки, после чего работают в памяти.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT сохранять секреты в открытом виде (используйте .env).
- DO NOT выводить сырые данные в консоль в продакшн-режиме.
- DO NOT выполнять синхронные сетевые вызовы в основном цикле ритуала (когда это возможно, стоит перейти к асинхронности или вынести вызовы в отдельный поток).
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) в скрипты Python/Bash.
- DO NOT менять версии или пути во время реконструкции артефакта.

## 7. Verification & Testing (Верификация)

Герхин-сценарии для квеста main.py

Feature: Script Functionality
Scenario: Successful analyze_sentiment execution
Given the API server is running
When I POST /analyze with JSON body { "text": "This product is excellent and I love it!" }
Then I receive HTTP 200 and a JSON response containing "result" and "model_version"

Scenario: Invalid input handling
Given the API server is running
When I POST /analyze with JSON body {}
Then I receive HTTP 422 (Unprocessable Entity)

Индикатор соответствия артефакта реальности: Independent Artifact
