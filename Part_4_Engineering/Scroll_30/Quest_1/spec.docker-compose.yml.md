# docker-compose.yml Specification

1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: Docker, Docker Compose, Locust
- Context: Independent Artifact

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Этот Ритуал Скрижаль docker-compose.yml призывает и синхронизирует два духа-сервиса: целевой дух приложения (target-app) и боевой стих Locust (locust-tester). Он создает каналы связи, выстраивает порядок призывов и открывает порталы для нагрузки на целевое приложение. Назначение — обеспечить управляемое тестирование прочности и мониторинг в рамках Quest’а 29.2, сохраняя каноны относительных путей и канонических имен.
   Instruction for AI: This section provides high-level intent. Use it to understand the "WHY" of the code.
   Описание на русском языке: Скрижаль orchestrates две службы: целевой сервис для теста и тестовый агент Locust, чтобы проводить нагрузку через порт 8000 и наблюдать реакции, сохраняя привязку к контексту Quest_29 и каноническим именам образов.

3. Interface Contract (Интерфейсный Контракт)
   Instruction for AI: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

3.1. Inputs (Входы)

- Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
- Format: YAML
- Schema:  
   interface BuildSpec {
  context?: string;
  dockerfile?: string;
  args?: Record<string, string>;
  }

  interface ServiceSpec {
  build?: BuildSpec;
  image?: string;
  ports?: string[];
  hostname?: string;
  depends_on?: string[];
  command?: string;
  environment?: Record<string, string>;
  }

  interface InputsSchema {
  version?: string;
  services?: Record<string, ServiceSpec>;
  networks?: Record<string, any>;
  volumes?: Record<string, any>;
  }

  3.2. Outputs (Выходы)

- Destination: File | STDOUT | API Response
- Format: YAML
- Success Criteria: Exit Code 0
- Schema:  
  interface OutputResult {
  success: boolean;
  message?: string;
  createdFilePath?: string;
  contentHash?: string;
  }

4. Implementation Details (The Source DNA / Исходный Код)

4.1. Algorithmic Logic (Для исполняемого кода)
Ритуал начинается с призыва Эфира из заданного источника: чтение docker-compose.yml либо ввод пользователя. Эфир обретает форму в объекте, который затем приводят к строгим структурами: версии и духов служб. В процессе ритуала:

- Шаг первый: собрать скрижаль, распарсить YAML в структурированное представление и очистить контуры до известных духов: target-app и locust-tester.
- Шаг второй: проверить наличие ключевых элементов: версия, раздел services, имена служб и их заготовки (build/ image, ports, hostname).
- Шаг третий: нормализовать пути сборки (build.context) относительно расположения самой Скрижали docker-compose.yml, чтобы путь ../../Scroll_29/Quest_2 корректно указывал на чертежи.
- Шаг четвертый: убедиться, что порты соответствуют заявленным порталам: 8000 для целевого духа и 8089 для Locust; проверить корректность привязок и доступность имени хоста target-app внутри сети.
- Шаг пятый: установить порядок пробуждения духов: target-app должен быть готов до запуска Locust; учесть зависимость через depends_on.
- Шаг шестой: при отсутствии Хаоса (ошибок) записать итоговую скрижаль обратно на диск или вернуть как выходной результат; если Хаос обнаружен, вернуть описательное сообщение об ошибках и не создавать финальную запись.
- Шаг седьмой: вернуть структурированное резюме вывода: путь к файлу, статус выполнения и хеш контента для детекции изменений.

  4.2. Declarative Content (Для конфигураций и данных)
  [Сюда ИИ вставит Указ Ткачу и точные данные для воссоздания 1-в-1]
  Не копировать код. Здесь описаны конкретные данные конфигурации, которые лежат в Скрижали docker-compose.yml и позволяют воссоздать окружение: два духа-сервиса, канонические образы, контексты и порталы.

5. Structural Decomposition (Декомпозиция структуры)

- version: строка версии 3.8
- services: карта имён служб
  - target-app: дух-цель
    - build.context: ../../Scroll_29/Quest_2
    - image: codex/app-29-2-telemetry
    - ports: ["8000:8000"]
    - hostname: target-app
  - locust-tester: дух-осада
    - build.context: .
    - image: codex/locust-30-1-tester
    - ports: ["8089:8089"]
    - command: locust -f /app/locustfile.py --host http://target-app:8000
    - depends_on: ["target-app"]

- networks (если присутствуют): опционально
- volumes (если присутствуют): опционально

6. System Context & Constraints (Системный контекст и Ограничения)

6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Async
- Dependencies: Docker Engine, Docker Compose, образ codex/app-29-2-telemetry, образ codex/locust-30-1-tester
- Привязки путей: build.context целевых служб, включая относительный путь ../../Scroll_29/Quest_2

  6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде внутри конфигурации; используйте .env или внешние секреты.
- DO NOT печатать сырые данные в консоль в продакшн-режиме.
- DO NOT вызывать синхронные сетевые операции в главном ритуале.
- DO NOT оборачивать конфигурационные файлы (.yaml, .json) в скрипты (Python/Bash).
- DO NOT менять версии или пути во время реконструкции.

7. Verification & Testing (Верификация)

Геркин-определения для счастья пути и одного кейса хаоса:

Feature: Docker Compose Orchestration for Load Test
Scenario: Successful orchestration
Given docker-compose.yml присутствует с определением служб
When выполняется процесс оркестрации и создается финальная Скрижаль
Then файл сохраняется на диск и не возникает ошибок

Feature: Docker Compose Orchestration - missing service
Scenario: Failure due to missing service
Given docker-compose.yml отсутствует одна из нужных служб (например target-app)
When запускается процесс валидации
Then возвращается детальное сообщение об ошибке и файл не создается

ИССЛЕДУЕМЫЙ АРТЕФАКТ: docker-compose.yml

ИСХОДНЫЙ КОД (для справки и ориентира, без копирования):

- Ритуал описывает два духа и их канонические порталы, пути контекста и правила призыва, как в комментариях к файлу.

Inventory (Declarative Content)

- 🏰 target-app
  - Тип: служба-цель
  - Образ: codex/app-29-2-telemetry
  - Контекст сборки: ../../Scroll_29/Quest_2
  - Порты: 8000:8000
    -hostname: target-app
  - Зависимости: нет явных зависимостей кроме общего контекста
- 🛡️ locust-tester
  - Тип: служба-осада
  - Образ: codex/locust-30-1-tester
  - Контекст сборки: .
  - Порты: 8089:8089
  - Команда: locust -f /app/locustfile.py --host http://target-app:8000
  - Зависимости: target-app
- Эфир: версия 3.8, сеть и (опционально) тома, связанные с контекстами сборки
- Хаос: валидации структуры и путей, в случае ошибок — возвращается сообщение и без записи итоговой Скрижали

Примечания по стилю

- Каждая часть текста пронизывается атмосферой ремесленного Веда-магуса: термины Скриптов превращаются в Ритуалы; Конфиги — в Скрижали; Данные — в Эфир; Ошибки — в Хаос.
- Имена переменных, путей и ключевых сущностей оставлены как есть: target-app, locust-tester, ../../Scroll_29/Quest_2, codex/app-29-2-telemetry, codex/locust-30-1-tester, 8000:8000, 8089:8089.
- Контекст указан как Independent Artifact, так как в рамках данного артефакта нет внешней ссылки на глобальный контекст.
