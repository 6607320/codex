# validation_data.py Specification

## 1. Meta Information

- Domain: ML/NLP
- Complexity: Low
- Language: Python
- Frameworks: None (Pure Python data structure)
- Context: ../AGENTS.md

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот артефакт — Скрижаль Испытаний: фиксированный набор текстов и эталонных меток, используемый как эталонная мертвая стойка для проверки поведения моделей анализа настроения. Он задаёт чертеж того, как система должна распознавать позитивные и негативные высказывания и служит мерилом консистентности вывода.  
Instruction for AI: Это высокоуровневый замысел — зачем нужен этот файл: задать повторяемый набор тестов для валидации модели на предсказания настроения.

Описание на русском языке: В Скрижалях записан Набор Испытаний, состоящий из десяти текстовых утверждений и соответствующих им ярлыков POSITIVE или NEGATIVE. Этот набор служит базой для проверки корректности классификации и устойчивости системы к типовым образцам речи, с заранее известной истинной меткой.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: STDIN
- Format: JSON
- Schema:
  interface InputData {
  text: string;
  label: 'POSITIVE' | 'NEGATIVE';
  }

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: JSON
- Success Criteria: Exit code 0
- Schema:
  interface OutputResult {
  status: 'SUCCESS' | 'FAILURE';
  processedCount: number;
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Ритуал начинается с чтения Зерна — Набора Испытаний — из Эфира, где каждый элемент состоит из текста и ожидаемой метки. Затем идёт последовательное прохождение через каждое испытание:

- Извлечь текст и ожидаемую метку.
- Применить внешний Мастер Предсказаний (или абстракцию модели) к тексту и получить предсказанную метку.
- Сверить предсказанную метку с ожидаемой. За каждый совпавший элемент прибавлять к счётчику удач; за несоответствия — регистрировать логи ошибок.
- Продвигаться к следующему элементу, пока не будут обработаны все десять испытаний.
  После обработки сформировать Эфирный Отчёт: общее число обработанных элементов, число совпавших и число несовпавших. Вернуть Окружение-Результат в STDOUT в формате JSON. Скрижаль завершается печатью или возвратом структуры итогов.

Если во время обработки встречаются проблемы с форматом входного элемента, такие случаи фиксируются в журнале Хаоса, но цикл продолжается с оставшимися элементами, чтобы сохранить устойчивость ритуала.

### 4.2. Declarative Content (Для конфигураций и данных)

Именно здесь без копирования кода приводится точная сущность данных — Набор Испытаний. Эти данные оформлены как Эфирные Элементы Инвентаря, которые можно воспроизвести и проверить 1-в-1.

## 5. Structural Decomposition (Декомпозиция структуры)

- Функции:
  - loadValidationSet: загрузка набора из Эфира (или импортированной константы).
  - evaluateItem: сверка текста с ожидаемой меткой через внешний предсказатель.
  - runValidation: последовательная обработка всех элементов и аггрегация результатов.
  - summarizeResults: формирование итогового вывода и подготовка выходного Эфирного Отчета.
- Классы:
  - ValidationItem (модель элемента испытания: текст, метка).
  - ValidationReport (итоговый отчёт по обработке: количество элементов, доля верных).
- Конфигурации/Данные:
  - Набор Испытаний (VALIDATION_SET) как базовая конфигурация для ритуала.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Стандартный CPU; скрипт рассчитан на низкие требования по памяти.
- Concurrency: Синхронный (последовательная обработка).
- Dependencies: Нет внешних зависимостей за пределами стандартной библиотеки Python; структура данных — простые словари со строками.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ храните секреты в открытом виде (используйте переменные окружения/.env).
- НЕ печатайте в продакшене сырые данные в консоль.
- НЕ используйте синхронные сетевые вызовы в главном цикле обработки.
- НЕ оборачивайте конфигурационные файлы (.yaml, .json) в исполняемые скрипты.
- НЕ изменяйте версии софта или путей при реконструкции артефакта.

## 7. Verification & Testing (Верификация)

Геркин-сценарии (описательные тесты поведения артефакта):

Геркин
Feature: Processing of the validation data set
Scenario: Happy path — успешная обработка набора
Given существующий набор VALIDATION_SET доступен для обработки
When система последовательно обрабатывает все элементы и сверяет предсказанные метки с ожидаемыми
Then итоговый отчет должен показать обработку всех 10 элементов и отсутствие ошибок

Геркин
Scenario: Error case — некорректная запись в наборе
Given набор содержит элемент с пропущенным поля текст или label
When система пытается валидировать набор
Then система должна зафиксировать ошибку для некорректного элемента и продолжить обработку остальных элементов

ИССЛЕДУЕМЫЙ АРТЕФАКТ: validation_data.py

ИСТОЧНЫЙ КОД ARTEFACT:
Имея ввиду свиток, известное имя файла: validation_data.py, Набор Испытаний состоит из десяти тестовых пар текста и метки: POSITIVE или NEGATIVE. Ниже перечислены элементы инвентаря, каждый из которых представляет одно испытание, с текстом и требуемой меткой.

- 🏰 Испытание 1: Текст: "I love this product, it is absolutely amazing!" Эталонная метка: POSITIVE
- 🛡️ Испытание 2: Текст: "This is the worst service I have ever received in my life." Эталонная метка: NEGATIVE
- 🏰 Испытание 3: Текст: "The movie was fantastic, a true masterpiece." Эталонная метка: POSITIVE
- 🛡️ Испытание 4: Текст: "I am so disappointed with the quality, it broke after one day." Эталонная метка: NEGATIVE
- 🏰 Испытание 5: Текст: "What a wonderful experience, I would recommend it to everyone." Эталонная метка: POSITIVE
- 🛡️ Испытание 6: Текст: "A complete waste of time and money, I regret buying this." Эталонная метка: NEGATIVE
- 🏰 Испытание 7: Текст: "The team was very helpful and friendly." Эталонная метка: POSITIVE
- 🛡️ Испытание 8: Текст: "The food was terrible and the waiter was rude." Эталонная метка: NEGATIVE
- 🏰 Испытание 9: Текст: "An outstanding performance by the entire cast." Эталонная метка: POSITIVE
- 🛡️ Испытание 10: Текст: "I will never come back to this place again." Эталонная метка: NEGATIVE

Дополнительная руна завершает свиток: ']'

Примечание: Інформационные поля и объекты оформлены в духе техномагического описания: текст и метка для каждого испытания сохранены как неизменны, чтобы обеспечить повторяемость теста и сопоставимость результатов.
