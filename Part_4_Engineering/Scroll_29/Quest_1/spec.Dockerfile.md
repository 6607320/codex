# Dockerfile Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: Docker
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот файл формирует ритуал сборки солнечного образа для запуска Python-приложения. Он берет готовую «первозданную глину» в виде образа python:3.10, возводит внутри мастерскую (/app), привносит свитки зависимостей libraries.list, призывает духа установки зависимостей через pip, помещает сердце кода main.py и свиток запуска start.sh, а затем произносит Главное Заклинание Жизни через CMD, чтобы Голем проснулся и готов к службе.
Instruction for AI: This section provides high-level intent. Use it to understand the "WHY" of the code.

Этот Dockerfile готовит среду выполнения для Python-приложения и задает путь от «заготовки» до запуска, чтобы сервис мог подняться без лишних пробелов в конфигурации.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
- Format: JSON | Text | Binary | Stream
- Schema: InputData: интерфейс, определения полей которого даны в кодовой базе и фиксируются в конфигурациях проекта. В текущем контексте это мост между шагами сборки и запуском, без внешних зависимостей по схеме в рамках артифакта.

### 3.2. Outputs (Выходы)

- Destination: STDOUT | File | Database | API Response | Event Log
- Format: JSON | CSV | Text
- Success Criteria: Exit Code 0
- Schema: OutputResult: интерфейс, определения полей которого даны в кодовой базе и фиксируются в конфигурациях проекта. В текущем артефакте это отражение статуса сборки/запуска и, при необходимости, простого сообщения об успехе.

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Шаг 1: Выбрать базовую заготовку образа — FROM python:3.10 как первозданную глину Голема.
- Шаг 2: Задать рабочее пространство — WORKDIR /app, чтобы мастерская распахнулась внутри Голема.
- Шаг 3: Переместить свиток зависимостей — COPY libraries.list ., чтобы духи зависимости могли найти свои заклинания.
- Шаг 4: Призывать зависимости — RUN pip install --no-cache-dir -r libraries.list, чтобы Все необходимые ингредиенты оказались в чаше мастерской.
- Шаг 5: Поместить сердце проекта — COPY main.py ., чтобы логика приложения была внутри головы Голема.
- Шаг 6: Привнести пергамент запуска — COPY start.sh ., чтобы запустить Главное Заклинание Жизни в чистой среде.
- Шаг 7: Включить заклинание на исполнение — RUN chmod +x ./start.sh, чтобы скрипт мог ожить и выполнять ритуал.
- Шаг 8: Повеление жизни — CMD ["./start.sh"], которое призывает Голема к действию при рождении контейнера.

### 4.2. Declarative Content (Для конфигураций и данных)

- База образа: python:3.10
- Рабочая мастерская: /app
- Свиток зависимостей: libraries.list
- Сердце кода: main.py
- Пергамент запуска: start.sh
- Руна исполнения: chmod +x ./start.sh
- Главное Заклинание Жизни: CMD ["./start.sh"]

## 5. Structural Decomposition (Декомпозиция структуры)

- FROM python:3.10 — базовый артефакт (слепок мира)
- WORKDIR /app — логическая мастерская внутри артефакта
- COPY libraries.list . — перенос свитка зависимостей
- RUN pip install --no-cache-dir -r libraries.list — призыв духа зависимостей
- COPY main.py . — перенос сердца Голема
- COPY start.sh . — перенос пергамента запуска
- RUN chmod +x ./start.sh — активация рун
- CMD ["./start.sh"] — главный акт жизни Голема

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies:
  - Базовый образ: python:3.10
  - Утилиты: pip
  - Файлы конфигурации проекта: libraries.list, main.py, start.sh

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (use .env)
- DO NOT print raw data to console in production mode
- DO NOT use synchronous network calls in the main loop
- DO NOT wrap configuration files (.yaml, .json) into scripts (like Python/Bash)
- DO NOT change versions or paths during reconstruction

## 7. Verification & Testing (Верификация)

Gherkin сценарии

Feature: Dockerfile Build and Run
Scenario: Successful build and run
Given Docker environment and Dockerfile present
When docker build -t app-image .
And docker run -p 8000:80 app-image
Then the container starts and start.sh emits the startup message and port 8000 is reachable

Scenario: Missing dependencies list leads to build failure
Given Docker environment and Dockerfile present without libraries.list
When docker build -t app-image .
Then the build fails with a message indicating missing dependencies list

ИССЛЕДУЕМЫЙ АРТЕФАКТ: Dockerfile
ИСХОДНЫЙ КОД:

- Структура: базовый образ, рабочая директория, копирование зависимостей, установка зависимостей, копирование кода, копирование скрипта запуска, настройка разрешений, главный CMD
- Логика: последовательный ритуал сборки, приводящий к готовому к запуску образу
- Инварианты: пути и версии не изменяются во время реконструкции, secrets не хранятся в открытом виде

Inventory (краткий обзор артефактов в виде RPG-элементов)

- 🏰 База образа: python:3.10
- 🏰 Рабочая мастерская: /app
- 🏰 Свиток зависимостей: libraries.list
- 🛡️ Защита зависимостей: pip install --no-cache-dir -r libraries.list
- 🏰 Сердце Голема: main.py
- 🏰 Пергамент запуска: start.sh
- 🛡️ Разрешение на исполнение: chmod +x ./start.sh
- 🏰 Главная руна жизни: CMD ["./start.sh"]
