# Dockerfile Specification

1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: Docker
- Context: Independent Artifact

2. Goal & Purpose (Цель и Назначение)
   Context for Creator: Это рецепт-rituал для создания двухступенчатого образа Python. Первый этап (Кузница) собирает и крафтит все зависимости в полном образе, второй этап (Тронный Зал) переносит только необходимые артефакты в компактный слоемый контейнер и запускает приложение.  
   Instruction for AI: Этот раздел поясняет WHY, зачем именно такой подход к сборке образа.

Легенда: Мы хотим обеспечить одноступенчатый запуск Python-приложения через uvicorn, при этом получить минимальный размер финального образа за счет копирования готовых зависимостей из BUILD stage и исключения лишнего слоя в финальном образе.

3. Interface Contract (Интерфейсный Контракт)
   Instruction: Strictly implement these data structures. Do not hallucinate fields. Use TypeScript interfaces to define shapes.

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema:
  InputData: объект со следующими полями
  - dockerfilePath: string
  - buildContext?: string
  - buildArgs?: Record<string, string>
  - tag?: string

### 3.2. Outputs (Выходы)

- Destination: Registry/Local Image
- Format: JSON
- Success Criteria: Exit Code 0
- Schema:
  OutputResult: объект со следующими полями
  - imageId: string
  - tag?: string
  - size?: number
  - success: boolean
  - message?: string

4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Ритуал этапов начинается с формирования Кузницы: базовый образ Python 3.10 выступает как источник силы. Устанавливаем рабочую директорию внутри Кузницы как /app.
- Скрижаль рецепта libraries.list копируем в Кузницу, и произносим главное заклинание: устанавливаем зависимости через pip install с флагом --no-cache-dir на этом этапе, позволяя компиляции и сборке тяжелых пакетов произойти внутри полного образа.
- Затем вступает Тронный Зал: финальный образ создается на основе легковесного base Python 3.10-slim.
- Устанавливаем рабочую директорию в /app.
- Гласим главную магию: копируем файлы не напрямую, а через портал --from=builder, чтобы перенести уже готовые гримуары site-packages в финальный образ.
- В финальном образе копируем весь код приложения и зависимости: COPY --from=builder /usr/local/lib/python3.10/site-packages в /usr/local/lib/python3.10/site-packages, затем COPY . . .
- Финальный запуск: CMD uvicorn main:app --host 0.0.0.0 --port 8000.

Эти шаги образуют двухступенчатый процесс: первый stage служит кузницей зависимостей, второй — чистым залом исполнения, где выполняется простая, но мощная служба через uvicorn.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory (построение артефактов, превращённый в RPG-элементы):

- 🏰 Stage Builder: FROM python:3.10 AS builder — рабочая кузница мира Python.
- 🛡️ Stage Final: FROM python:3.10-slim — тронный зал, где разворачиваются готовые артефакты.
- 📜 Файл рецептов: libraries.list — скрижаль зависимостей, используемая на этапе сборки.
- 🧙‍♂️ Инструмент заклинаний: pip install --no-cache-dir -r libraries.list — главный ритуал установки библиотек.
- 🗺️ Рабочие руны: WORKDIR /app — место, где складываются эльфийские файлы.
- 🪄 Портал копирования: COPY --from=builder /usr/local/lib/python3.10/site-packages в /usr/local/lib/python3.10/site-packages — перенос готовых масок зависимостей между этапами.
- 🗃️ Контент каста: COPY . . — копирование кода приложения в финальный зал.
- 🧭 Финальная магия: CMD uvicorn main:app --host 0.0.0.0 --port 8000 — запуск сервиса.

5. Structural Decomposition (Декомпозиция структуры)

- Stage 1: Кузница (builder)
  - Базовый образ: python:3.10 AS builder
  - Действия: установка рабочих рунических путей; копирование libraries.list; установка зависимостей через pip
- Stage 2: Тронный зал (final)
  - Базовый образ: python:3.10-slim
  - Действия: копирование site-packages из builder; копирование всего кода; установка точки входа (CMD) для uvicorn
- Общий поток: сборка зависимостей в полном образе → перенос необходимых артефактов в компактный финальный образ → запуск сервиса

6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Использование двухступенчатого сборочного процесса снижает размер финального образа, за счет копирования только необходимых зависимостей из builder в final.
- Concurrency: Строение образа последовательное; сборка выполняется синхронно.
- Dependencies: Docker, Python 3.10 (в двух образах: полный и slim), uvicorn, библиотеки из libraries.list.

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде; не переносить чувствительные данные напрямую через финальный образ без должной обработки.
- DO NOT печатать сырые данные в консоль в продакшн-режиме.
- DO NOT использовать синхронные сетевые вызовы в главном контуре исполнения приложения.
- DO NOT упаковывать конфигурационные файлы (yaml/json) внутрь скриптов как набор команд.
- DO NOT менять версии базовых образов или пути в процессе реконструкции.

7. Verification & Testing (Верификация)

Геркин-сценарии (1–2 варианта счастлной дороги и вариант ошибки):

Feature: Dockerfile multi-stage build
Scenario: Successful multi-stage build
Given repository содержит валидный libraries.list и рабочий код приложения
When выполняется сборка образа с помощью Dockerfile
Then финальный образ создаётся с тегом и корректным размером, и сервис может быть запущен через uvicorn

Feature: Dockerfile multi-stage build
Scenario: Missing dependencies file
Given libraries.list отсутствует
When выполняется сборка образа
Then сборка завершается с неуспехом и возвращается код ошибки

Важно: В контексте этого артефакта, все указанные шаги воспроизводимы в стандартной среде Docker: Stage builder, Stage final, копирование site-packages, копирование кода и запуск uvicorn. Разверните образ и запустите приложение, чтобы проверить соответствие ожидаемому поведению.
