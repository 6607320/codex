# --- Dockerfile для Квеста 25.1 ("Монолит") ---
# Мы берем за основу "толстый" образ, полный ненужных инструментов.
FROM python:3.10

# Устанавливаем рабочую директорию.
WORKDIR /app

# Копируем рецепт.
COPY libraries.list .

# Устанавливаем гримуары. Весь "мусор" от сборки останется в финальном образе.
RUN pip install --no-cache-dir -r libraries.list

# Копируем наш код.
# Мы копируем ТОЛЬКО скрипт API, чтобы не захламлять образ.
COPY main.py .

# Задаем команду для запуска.
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
