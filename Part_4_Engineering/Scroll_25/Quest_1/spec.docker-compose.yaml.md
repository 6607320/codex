# docker-compose.yaml Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Go
- Frameworks: Docker, Docker Compose
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот Ритуал задаёт оркестрацию двух духов в одной Скрижали. api призывает тело образа из текущей папки (build: .), связывает портал 8000 наружу и внутрь, чертит окружение REDIS_HOST=redis, и слушается Хранителя Памяти (redis) прежде чем поднять Говорящий Амулет (api). Инструкция разворачивает зависимость так, чтобы Хранитель Памяти стартовал раньше, обеспечивая стабильное сотрудничество между серверами.
Instruction for AI: Это описание высшего замысла модуля — зачем нужен этот файл и какую задачу он решает в контексте инфраструктуры и развёртывания.

ИССЛЕДУЕМЫЙ АРТЕФАКТ: docker-compose.yaml

Где в центре — две сущности: api и redis; их связь и последовательность старта; версия синтаксиса — 3.8.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: STDIN
- Format: YAML
- Schema:
  - InputData:
    - version: string
    - services: Record<string, ServiceDefinition>
    - networks?: Record<string, NetworkDefinition>
    - volumes?: Record<string, VolumeDefinition>
  - ServiceDefinition:
    - build?: string | BuildContext
    - image?: string
    - ports?: string[]
    - environment?: Record<string, string> | string[]
    - depends_on?: string[]
    - volumes?: string[]
  - BuildContext:
    - context?: string
    - dockerfile?: string
    - args?: Record<string, string>
  - NetworkDefinition:
    - driver?: string
    - external?: boolean
  - VolumeDefinition:
    - driver?: string
    - external?: boolean

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: JSON
- Success Criteria: Exit Code 0
- Schema:
  - OutputResult:
    - status: 'success' | 'failure'
    - message?: string
    - generated?: string[]
    - details?: Record<string, any>

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Ритуал начинается с призыва Эфира из входного файла. По царскому маршруту:

1. Пробудить Эфир: загрузить YAML-скрижаль из входа и распознать её версию и секции. Проверить, что версия — 3.8, что присутствуют разделы services и по меньшей мере один дух в них.
2. Вылепить Граф Духов: для каждого сервиса внутри services собрать его описание — что он строит (build) или какой образ (image) он носит, какие порты открывает, какие переменные окружения держат внутри магию соединения, какие зависимости и тома привязаны.
3. Разложить Взаимодействия: построить граф зависимостей по depends_on между духами, чтобы понять порядок старта: Redis должен быть поднят до api (как в кадре «Хранитель Памяти precedes Говорящий Амулет»).
4. Сверить Эфир с Обличиями: проверить, что для каждого духа задан образ или путь к сборке, что порты не противоречат друг другу и что переменные окружения валидны (например REDIS_HOST=redis корректно указывает на семью духов-соседей).
5. Формировать Путь Разгортания: на основе данных составить план действий — собрать/build образ api, затем запустить Redis, потом поднять api с учётом зависимостей; при необходимости обеспечить сетевые настройки и тома.
6. Вызвать Шепот Звона: вернуть результаты в Эфир (STDOUT) как структурированный JSON: статус, сообщение, список сгенерированных артефактов и дополнительные детали.
7. Обратная Связь: если где-то встречены несоответствия (отсутствие build/image, конфликт портов, нарушенный порядок зависимостей), вернуть хаос с подробным описанием проблем и guía по исправлению.

### 4.2. Declarative Content (Для конфигураций и данных)

Внутренний Инвентарь Артефакта — это не набор строк, а живые предметы в мире техники. Ниже перечислены ключевые элементы Скрижали:

- 🏰 Версия синтаксиса: 3.8 — фундаментальная мануальная карта для миров Compose.
- 🛡️ api — Говорящий Амулет:
  - Родительская Скрижаль: build: . (тело образа собирается из текущей папки)
  - Образ: codex/orchestrator-25-1-app
  - Портал наружу: 8000:8000
  - Окружение: REDIS_HOST=redis
  - Зависимости: depends_on redis
- 🛡️ redis — Хранитель Памяти:
  - Образ: redis:alpine
- 🗣️ Потоки связи — сеть и связь между духами через REDIS_HOST
- 🔌 Контактные узлы — зависимости и порядок старта

Эти образы и их магия определяют, как будет развернут пирог микросервисов и какие артефакты будут созданы в ходе ритуала.

## 5. Structural Decomposition (Декомпозиция структуры)

- Для кода/скрипта:
  - класс/модуль: ComposeParser (разбор Скрижали)
  - функции: parseYaml, validateVersion, extractServices, buildInternalGraph, resolveDependencies, generateDeploymentPlan, emitOutput
  - типы: InputData, ServiceDefinition, BuildContext, NetworkDefinition, VolumeDefinition, OutputResult
- Для конфигов:
  - главныеlogical blocks: version, services, networks, volumes
  - основные сущности: api (build, image, ports, environment, depends_on), redis (image)

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU, умеренная нагрузка; память не критична (контейнеры не требуют выделенных гигабайт в рамках обычного локального сценария).
- Concurrency: Асинхронность допускается на уровне планирования и вывода результата, но сам запуск нужно держать в последовательности согласно зависимостям.
- Dependencies: Docker, Docker Compose, Образы: codex/orchestrator-25-1-app и redis:alpine

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде в файле конфигурации или в Эфире.
- DO NOT печатать сырые данные в консоль в продакшене.
- DO NOT использовать синхронные сетевые вызовы в критическом цикле развертывания.
- DO NOT упаковывать конфигурационные файлы (.yaml, .json) в скрипты без явного разделения конфигурации и кода.
- DO NOT менять версии или пути во время реконструкции без явного уведомления.
- DO NOT копировать код в отчёт или в ленты инструкций; опираться на голос разума и логику.

## 7. Verification & Testing (Верификация)

Геркин-сценарии (1–2 сценария, счастливая дорожка и один аварийный)

Feature: Docker Compose Specification
Scenario: Успешная оркестрация api и redis
Given система содержит docker-compose.yaml с сервисами api и redis и версией 3.8
When ритуал разбора Скрижали выполняется без ошибок
Then план развертывания должен указать: сначала поднять redis, затем api, и результат — статус success

Scenario: Ошибка отсутствия образа или сборки для api
Given docker-compose.yaml определяет api без valid image или build
When ритуал разбора Скрижали выполняется
Then результат вернется с статусом failure и соотвествующим сообщением об отсутствии образа/плана сборки

Итоговый вывод по Контексту: Independent Artifact
