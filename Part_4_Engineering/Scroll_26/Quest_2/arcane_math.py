"""Квест 26.2: Пергамент с заклинанием для "Ока Истины".

Этот пергамент содержит простое, чистое заклинание, которое будет служить
"подопытным" для нашего ритуала автоматического тестирования. Его главная
цель (МАКРО-контекст) — быть объектом проверки для "Ока Истины" (`pytest`).

Сам по себе скрипт предельно прост — он содержит лишь одну функцию
`add_two_runes`. Его истинная ценность раскрывается в связке со скриптом
`test_arcane_math.py`, который будет проверять, работает ли это заклинание
так, как задумано.
"""

# Мы начинаем определение нашего "заклинания", которому даем имя
# `add_two_runes` и указываем, что оно принимает два "компонента" (`a` и
# `b`).

def add_two_runes(a, b):
    # Это "официальное описание" заклинания (docstring). Оно заносится в
    # гримуар и объясняет другим магам, в чем заключается его суть.
    """Это простое заклинание складывает силу двух рун."""
    # Главное магическое действие: заклинание `return` "материализует"
    # результат, который равен сумме сил двух рун (`a + b`).
    return a + b

# trigger 3
