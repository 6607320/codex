# spell_with_flaw.py Specification

## 1. Meta Information

- Domain: Scripting
- Complexity: Low
- Language: Python
- Frameworks: flake8
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот артефакт — нерабочий инструмент. Его предназначение как жертвенного скрипта призвано проверить работу иммунной системы CI/CD: как она обнаруживает хаос в коде и отсеивает его до попадания в мастер-борозды.

**Instruction for AI:** This section provides the high-level intent. Use it to understand the "WHY" of the code.

Этот файл задуман как демонстрационный тестовый объект: он содержит намеренные изъяны и стилистические ошибки для обнаружения инструментами статического анализа. Успешный провал CI/CD после коммита подтверждает, что механизмы защиты активны и блюдут чистоту ритуалов сборки.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: None
- Format: None
- Schema:

```typescript
interface InputData {}
```

### 3.2. Outputs (Выходы)

- Destination: None
- Format: None
- Success Criteria: None
- Schema:

```typescript
interface OutputResult {}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

Step-by-step алгоритм трактует этот артефакт как учебный ритуал проверки хаоса в коде:

- Шаг 1. В начале документа появляется докстринг, который deklarирует «квест» и контекст: это не рабочий инструмент, а инструмент-обман для активации стражей CI.
- Шаг 2. Импортируется гримуар os, но нигде не применяется в ритуале. Это — первый изъя(object) хаоса: неиспользованный импорт, за который должен увидеть F401.
- Шаг 3. Объявляется функция bad_spell(a, b). В теле функции выполняется операция сложения a + b, что в контексте стиля и форматирования поднимает вопросы об E201, E202 и E225 (пробелы внутри скобок и вокруг оператора).
- Шаг 4. В коде присутствуют многочисленные комментарии, указывающие на крайне явные стилистические дефекты и пробелы, которые CICD-скрипты линтинга должны зафиксировать.
- Шаг 5. В конце кода встречается маркерный комментарий trigger 3, который символизирует «призыв» к включению теста на CI/CD и пробуждает охрану хаоса GitHub Actions.
- Итог: запуск линтинга (например, через flake8) обнаруживает хаос и приводит к неуспеху прогона, доказывая, что иммунная система проекта бодрствует.

### 4.2. Declarative Content (Для конфигураций и данных)

[Секция заполнена в виде Инвентаря — предметов RPG, чтобы увидеть артефакт как набор компонентов.]

- 🏰 spell_with_flaw.py — Главный артефакт-обман, цель которого — продемонстрировать работу хаоса и тестирования.
- 🛡️ Unused Import os — Неприменяемый гримуар в эпосе, за который должен зацепиться страж F401.
- 🗡️ Функция bad_spell(a, b) — Простая магия сложения, которая демонстрирует нарушение стилистических норм вокруг оператора и скобок (E201, E202, E225).
- 🧭 Комментарии с указаниями на хаос — Очерчивают наличие хаоса в коде и намекают на то, что линтеру предстоит увидеть «лишние пробелы» и прочие дефекты.
- 🔥 Триггер 3 — маркер вызова CI/CD к выполнению проверки и тестирования.
- 🧰 Докстринг модуля — макро-контекст квеста, объясняющий логику и цель артефакта.
- 🎯 Цель линтинга — доказать, что система защит активна и безошибочно ловит хаос.

## 5. Structural Decomposition (Декомпозиция структуры)

- Модуль: spell_with_flaw.py
  - Докстринг модуля (описание квеста и макро-контекста)
  - Импорт: import os
  - Функция: def bad_spell(a, b)
  - Комментарии кода (описание изъянов и ошибок стиля)
  - Маркер: trigger 3

- В качестве компонентов кода можно перечислить:
  - Раздел документации на верхнем уровне
  - Неиспользованный импорт os
  - Функция bad_spell(a, b) с базовой логикой a + b
  - Комментарии, указывающие на стиль и хаос
  - Контекстный призыв к CI/CD (trigger 3)

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies: flake8, Python 3.x (для локального линтинга и проверки хаоса)

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (use .env)
- DO NOT print raw data to console in production mode
- DO NOT use synchronous network calls in the main loop
- DO NOT wrap конфигурационные файлы (.yaml, .json) в скрипты
- DO NOT change версии или пути при реконструкции

## 7. Verification & Testing (Верификация)

Герхин-сценарии для описания пути счастья и одного случая ошибки.

```gherkin
Feature: Script Verification for spell_with_flaw.py
  Scenario: Happy path — линтинг находит хаос и приводит к падению CI
    Given артефакт spell_with_flaw.py содержит неиспользуемый импорт и нарушения стиля
    When выполняется ритуал линтинга с использованием flake8
    Then CI/CD пайплайн завершится неудачей (exit code не 0) и будут перечислены ошибки F401, E201, E202, E225

  Scenario: Error case — инструмент линтинга недоступен
    Given окружение не имеет установленного flake8
    When запускается ритуал линтинга
    Then процесс завершается с сообщением об отсутствующем инструменте линтинга
```

Итог: спецификация выполнена в духе прикладной техномагии, где артефакт spell_with_flaw.py предстает как учебный ритуал, демонстрирующий принципы обнаружения хаоса и силы стилистической чистоты в мире CI/CD.
