# quest_26_1_ci_flake8.yaml Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Low
- Language: Bash
- Frameworks: GitHub Actions; Flake8
- Context: ../AGENTS.md

Стихия этот артефакт держит в узде как стража скрижали: он рождает ритуал непрерывной чистоты кода в духе CI, чтобы хаос ошибок не проник в Грани репозитория.

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Цель этого файла — выразить ритуал автоматической проверки чистоты кода на Python‑территории проекта Quest 1. Он обеспечивает, чтобы перед слиянием в ветку мастер код проходил сквозь стража Flake8 и не позволял хаосу кода проникнуть в летопись проекта.

Instruction for AI: Этот раздел передает WHY: зачем нужен файл — поддерживать стиль и качество кода, предотвращать распространение хаоса и сужать поверхность синтаксических ошибок.

Цель: автоматизировать статическую проверку кода Python в Part_4_Engineering/Scroll_26/Quest_1 с помощью Flake8 через GitHub Actions, возвращая понятный отчёт и корректный статус выполнения.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema:
  - InputData:
    - source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
    - path?: string (опционально указывает путь к целевым файлам, например Part_4_Engineering/Scroll_26/Quest_1/)
    - configPath?: string (опционально путь к конфигурации Flake8)
      Принципиально входы описывают, откуда инициируется работа и куда целится анализируемый эфир кода.

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit Code 0
- Schema:
  - OutputResult:
    - success: boolean
    - exitCode?: number
    - reportPath?: string
    - lintSummary?: {
      errors: number
      warnings: number
      ignored?: number
      }
    - details?: string

  Выходы отражают состояние ритуала: тихий успех с нулевым кодом или хаос ошибок с описанием и местом отчёта.

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Ритуал поднимается в мир, чтобы закрепить окружение: удостовериться, что используется Python версии 3.10 и доступна чистая колдовская чашка pip, чтобы вызвать духа Flake8.
2. Вызвать Install Dependencies: установить инструмент Flake8 через pip, чтобы стать в охране кода.
3. Призывать Flake8 на целевую зону Part_4_Engineering/Scroll_26/Quest_1/ либо на указанный путь из входов. Ритуал читает вывод Flake8 как эфир, собирает числа ошибок и предупреждений.
4. Анализировать эфир: если ошибок и предупреждений нет — ритуал завершается с кодом 0 и выводит чистый свод.
5. Если хаос ошибок обнаружен — собрать детальный свод, указать количество ошибок и предупреждений, указать местоположение проблем в файлах. Ритуал возвращает не‑нулевой код и сохраняет отчет в указанном месте (если указан путь к отчёту).
6. Оформить результаты в OutputResult: заполнить поля success, exitCode, lintSummary и (при наличии) reportPath и details.
7. Вывести итог в эфире STDOUT, чтобы хранители кода увидели счастливый финал или хаос ошибок.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory данных и конфигураций, превращённых в RPG-предметы:

- 🏰 Target Directory (Локация очистки): Part_4_Engineering/Scroll_26/Quest_1/
- 🛡️ Linter (Страж чистоты): flake8
- 🪄 Python Interpreter (Чертог времени): Python 3.10
- 🔮 CI Enchanter (Заклинатель CI): GitHub Actions
- 📜 Workflow Name (Имя свитка): quest_26_1_ci_flake8.yaml
- 📜 Report Destination (Где хранится отчёт): по желанию, указанный путь к отчёту (reportPath)
- 🗺️ Output Schema (Эхо эфира): структура OutputResult с полями success, exitCode, reportPath, lintSummary

Эти артефакты состоят в едином заклятии, которое повторяется для каждого повторения ритуала: проверять чистоту кода и сохранять следы результативности.

## 5. Structural Decomposition (Декомпозиция структуры)

- Код/Конфигурация: YAML‑письмо, определяющее одну задачу (linting)
- Функциональные блоки (для кода): нет функций и классов в самом артефакте, но внутри ритуала можно увидеть последовательность шагов:
  - Шаг 1: Checkout Code
  - Шаг 2: Set up Python
  - Шаг 3: Install Dependencies
  - Шаг 4: Lint with flake8

- Для конфигураций: основные логические блоки
  - on: триггеры события
  - permissions: read
  - jobs: linting
    - steps: Checkout Code, Set up Python, Install Dependencies, Lint with flake8

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync (последовательное выполнение шагов; один поток выполнения в каждом запуске)
- Dependencies: flake8; Python 3.10; actions/checkout@v4; actions/setup-python@v5

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text (используйте .env или секреты репозитория)
- DO NOT print raw data to console в продакшн режиме
- DO NOT use synchronous сетевые вызовы в главном цикле ритуала
- DO NOT wrap конфигурационные файлы (.yaml, .json) в скрипты
- DO NOT менять версии или пути во время реконструкции артефакта

## 7. Verification & Testing (Верификация)

Геркин-сценарии (1–2 варианта, описывающих счастливый путь и одну ошибку):

Функционал: quest_26_1_ci_flake8.yaml

Сценарий 1: Успешное выполнение
Дано целевые файлы в Part_4_Engineering/Scroll_26/Quest_1/ не содержат нарушений Flake8
Когда запускается ритуал CI
Тогда выходной код равен 0 и выдается чистый отчёт об очистке кода

Сценарий 2: Обнаружены нарушения
Дано в целевых Python‑файлах есть нарушения Flake8
Когда запускается ритуал CI
Тогда выходной код не равен 0 и формируется отчёт с перечнем ошибок и мест их обнаружения

Истины артефакта в чистых словах: он хранит в себе барьер против хаоса, превращая каждый прогон в охранную запись, чтобы код не рассыпался в процессе эволюции проекта.
