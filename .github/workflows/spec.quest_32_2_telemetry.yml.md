# quest_32_2_telemetry.yml Specification

## 1. Meta Information

- **Domain:** Infrastructure
- **Complexity:** Medium
- **Language:** Bash
- **Frameworks:** actions/checkout@v4, google-github-actions/auth@v2, docker/login-action@v3, docker/build-push-action@v5, pytest, pip
- Context: Independent Artifact

Комментary: Стихии CI переплетаются с кузницей артефактов — Bash-палитра ведёт ритуал, где каждый шаг подбрасывает искру к телеметрии и выпуску множества контейнеров.

## 2. Goal & Purpose (Цель и Назначение)

**Context for Creator:** Этот файл задаёт ориентированную наTelemetry CI/CD цепочку для квеста 32.2: проверка кода, сборка тестового покрытия и публикация артефактов, а затем развёртывание контейнерного образа в реестр.  
**Instruction for AI:** Это экономит время, обеспечивая повторяемость и прозрачность процесса от push до публикации образа и сбора телеметрических данных.

Этот рабочий процесс обеспечивает автоматическую очистку дискового пространства, установку зависимостей, выполнение тестов с покрытием, загрузку отчётов, аутентификацию в облаке и сборку с публикацией Docker-образа в реестр. Все шаги направлены на надёжную повторяемость и сбор телеметрии по итогам каждого коммита.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- **Source:** CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
- **Format:** JSON | Text | Binary | Stream
- **Schema:**
  interface InputData {
  event?: any
  payload?: any
  secrets?: Record<string, string>
  repo?: string
  ref?: string
  path?: string
  }

### 3.2. Outputs (Выходы)

- **Destination:** STDOUT | File | Database | API Response | Event Log
- **Format:** JSON | CSV | Text
- **Success Criteria:** Exit Code 0 | 200 OK | File Created
- **Schema:**
  interface OutputResult {
  success?: boolean
  artifacts?: string[]
  coverageReportPath?: string
  imageTag?: string
  }

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

- Целью является запуск CI/CD ритуала, привязанного к изменению в директории Part_4_Engineering/Scroll_32/Quest_2 на ветке main.
- Сценарий начинается с подготовки окружения: контроль свободного места и очистка указанных системных артефактов через безопасный деструктивный шаг в рамках тестовой кузницы, чтобы освободить место под сборку артефактов.
- Далее выполняется установка зависимостей из двух списков (libraries.list и libraries_dev.list) с помощью pip.
- Запускаются тесты по коду квеста 32.2 с подсчётом покрытия и формируется файл отчёта coverage.xml.
- Отчёт по покрытию загружается как артефакт для последующего анализа.
- Происходит аутентификация к облачному провайдеру через Workload Identity Federation и получение токена доступа.
- Выполняется вход в реестр Artifact Registry с использованием OAuth2 access token.
- В финале строится Docker-образ по указанному контексту и образ отправляется в реестр, используя тег, зависящий от идентификатора проекта и текущего коммита.

## 4.2. Declarative Content (Для конфигураций и данных)

- 🏰 Название процесса: Capstone Pipeline with Telemetry Quest 32.2
- 🛡️ Триггер: запуск на push в ветку main с изменениями внутри Part_4_Engineering/Scroll_32/Quest_2
- 🧭 Разрешения: contents read; id-token write
- 🗺️ Задание: build-test-deploy с именем Build, Test, Deploy, and Collect Artifacts, выполняемое на ubuntu-latest
- 🧭 Шаги ритуала:
  - Checkout Repository
  - Free Up Disk Space: вывод дискового пространства, очистка /usr/share/dotnet, /opt/ghc, /usr/local/share/boost и AGENT_TOOLSDIRECTORY
  - Install Dependencies: pip install -r libraries.list и pip install -r libraries_dev.list
  - Run Tests and Generate Coverage Report: pytest с покрытием по каталогу Quest_2 и создание coverage.xml
  - Upload Coverage Report Artifact: загрузка coverage.xml как артефакт
  - Authenticate to Google Cloud: использование google-github-actions/auth с workload_identity_provider и service_account из секретов
  - Login to Artifact Registry: docker/login-action к europe-west3-docker.pkg.dev с oauth2accesstoken
  - Build and Push Docker Image: docker/build-push-action с контекстом ./Part_4_Engineering/Scroll_32/Quest_2 и тегом image: europe-west3-docker.pkg.dev/${project_id}/codex-golems/amulet-telemetry:${github.sha}

## 5. Structural Decomposition (Декомпозиция структуры)

- Триггеры и контекст
  - on push
  - paths: Part_4_Engineering/Scroll_32/Quest_2
- Безопасности и разрешения
  - permissions contents: read
  - permissions id-token: write
- Рабочие элементы (Job)
  - build-test-deploy
  - name: Build, Test, Deploy, and Collect Artifacts
  - runs-on: ubuntu-latest
- Шаги внутри задания
  - Checkout Repository
  - Free Up Disk Space
  - Install Dependencies
  - Run Tests and Generate Coverage Report
  - Upload Coverage Report Artifact
  - Authenticate to Google Cloud
  - Login to Artifact Registry
  - Build and Push Docker Image

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: стандартный CPU, обычная сборка CI; не выделяется высокопроизводительная графика или GPU
- Concurrency: синхронное выполнение шагов внутри задания; отдельные шаги могут выполняться последовательно
- Dependencies: Docker, pytest, pip, Google Cloud actions, Docker реестр, GitHub Actions артефакты
- reproduce-ability: использование конкретных путей в репозитории Part_4_Engineering/Scroll_32/Quest_2
- Безопасность: использование секретов из GitHub Secrets для Workload Identity Provider и Service Account

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT хранить секреты в открытом виде в коде; используйте секреты CI
- DO NOT выводить чувствительные данные в логи в продакшн-режиме
- DO NOT выполнять необратимые операции без явной необходимости в рабочем окружении
- DO NOT укладывать конфигурационные файлы (.yaml, .json) в скрипты как код
- DO NOT менять версии или пути во время реконструкции артефактов без явной необходимости

## 7. Verification & Testing (Верификация)

1. Гиркейн сценарий 1: Успешное выполнение

- Предусловия: репозиторий содержит код квеста 32.2; ветка main обновлена соответствующими файлами
- Действие: запуск CI при push в main на путь Part_4_Engineering/Scroll_32/Quest_2
- Ожидаемый результат: сборка и тесты проходят успешно; отчёт о покрытии загружен как артефакт; аутентификация в Google Cloud выполнена; артефакт Docker образа успешно построен и отправлен в реестр

2. Гиркейн сценарий 2: Ошибка тестов

- Предусловия: тесты Quest_2 завершаются неудачей
- Действие: запуск CI
- Ожидаемый результат: шага Run Tests and Generate Coverage Report завершается с неуспехом; журнал содержит ошибка тестов; последующие шаги могут зависнуть или пропустить публикацию артефактов
