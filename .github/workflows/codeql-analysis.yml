# --- Великий Свиток Прорицаний: codeql-analysis.yml ---

# Имя Ритуала: Пробуждение Всевидящего Оракула (CodeQL)
# Это имя будет отображаться в списке ритуалов (Actions) нашей Цитадели.
name: "CodeQL Analysis"

# Руна-Триггер: Когда именно пробуждать Оракула.
# Этот блок определяет события, которые запускают данный ритуал.
on:
  # При каждом новом "Начертании в Летописи" (push) в главную ветвь "main".
  # Это гарантирует, что основной Кодекс всегда находится под надзором.
  push:
    # Указываем, что ритуал касается только главной временной линии.
    branches: [ "main" ]

  # А также при каждом "Предложении о Слиянии" (pull request), нацеленном на "main".
  # Это позволяет нам увидеть пророчества Оракула ДО того, как мы примем изменения в Кодекс.
  pull_request:
    # Снова указываем, что нас интересуют только изменения, идущие в "main".
    branches: [ "main" ]

# Великий Акт: Определяем последовательность действий в ритуале.
# "jobs" - это контейнер для одной или нескольких последовательностей действий.
jobs:
  # Имя Акта: Само "Прорицание".
  # Мы можем дать этому акту любое имя; `analyze` - это стандарт.
  analyze:
    # Отображаемое имя этого Акта в логах Цитадели.
    name: Analysis

    # Призыв "Эфирного Духа-Исполнителя": Указываем, что ритуал будет исполняться
    # на чистом духе, рожденном в Убунту, которого предоставляет сама Цитадель GitHub.
    runs-on: ubuntu-latest

    # Магические Права: Какие силы мы даруем Оракулу для его работы.
    # Без этих прав ритуал не будет иметь силы.
    permissions:
      # Право читать свитки других ритуалов (необходимо для координации).
      actions: read
      # Право читать свитки нашего Кодекса.
      contents: read
      # Ключевое право: Разрешение записывать свои пророчества (найденные уязвимости)
      # в "Зал Прорицаний" (вкладка Security -> Code scanning alerts).
      security-events: write

    # Стратегия Прорицания: Позволяет Оракулу анализировать несколько языков магии, если потребуется.
    # Это делает ритуал гибким и готовым к будущему расширению Кодекса.
    strategy:
      # Руна "Несокрушимости": Если анализ одного языка провалится (например, мы добавим JS и он сломается),
      # анализ других (Python) все равно продолжится.
      fail-fast: false
      # Матрица Языков: Перечисляем языки магии, которые должен проанализировать Оракул.
      matrix:
        # В нашем Кодексе пока что используется только язык Python.
        language: [ 'python' ]

    # Шаги Ритуала: Последовательность магических действий, которые исполнит "Дух".
    steps:
    # Шаг 1: Материализация Кодекса.
    - name: 📜 Призыв Кодекса
      # Дух-Исполнитель призывает последнюю версию нашего Кодекса в свою эфемерную мастерскую.
      # `@v4` указывает на версию самого заклинания призыва.
      uses: actions/checkout@v4

    # Шаг 2: Подготовка к Прорицанию.
    - name: 🔮 Инициализация Оракула CodeQL
      # Мы призываем самого Оракула. Он анализирует Кодекс и создает "астральную проекцию"
      # его структуры (базу данных CodeQL), которую затем будет изучать.
      uses: github/codeql-action/init@v3 # v3 is the latest version
      # Дополнительные инструкции для Оракула.
      with:
        # Мы указываем ему, какую именно "астральную проекцию" создавать
        # (в данном случае, для языка Python, взятого из нашей Матрицы Языков).
        languages: ${{ matrix.language }}

    # Шаг 3: Великое Прозрение.
    - name: 👁️ Проведение Прорицания (Анализ Кода)
      # Оракул вглядывается в созданную "проекцию", ищет в ней "тёмные руны" по тысячам
      # известных ему правил и записывает все свои находки.
      uses: github/codeql-action/analyze@v3 # v3 is the latest version