# quest_26_2_ci_full.yaml Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Low
- Language: Python
- Frameworks: flake8, pytest
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Легенда: Этот ритуал автоматизирует проверку чистоты кода и истинности поведения в пределах части Летописи, посвящённой Quest_2. Он призван обеспечивать качество кода и стабильность зависимостей на каждом пуше и в ветке запросов на слияние, фокусируясь на каталоге Part_4_Engineering/Scroll_26/Quest_2/\*\*. Это Скрижаль для защиты репозитория от хаоса и для ускорения выпуска надежного ПО.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

Source: CLI Args | STDIN | API Request | Kafka Topic | Smart Contract Call
Format: JSON
Schema:

```typescript
interface InputData {
  source: "CLI" | "STDIN" | "API" | "Kafka" | "SmartContract";
  payload?: any;
  metadata?: {
    [key: string]: string;
  };
}
```

### 3.2. Outputs (Выходы)

Destination: STDOUT | File | Database | API Response | Event Log
Format: JSON | CSV | Text
Success Criteria: Exit Code 0 | 200 OK | File Created
Schema:

```typescript
interface OutputResult {
  success: boolean;
  status?: number;
  destination?: "STDOUT" | "File" | "Database" | "API" | "EventLog";
  dataSummary?: string;
}
```

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Ритуал активируется при каждом push или pull_request, направленных к пути Part_4_Engineering/Scroll_26/Quest_2/\*\*.
2. Призыв Летописи: извлекаем точную копию репозитория через репозиторию-Алтарь (checkout).
3. Создание Магического Круга: устанавливаем окружение Python версии 3.10 для исполнимой магии.
4. Призыв Инструментов: устанавливаем зависимости, в частности flake8 и pytest, чтобы очистить руну кода и проверить истинность поведения.
5. Проверка Чистоты Рун: запускаем lint с помощью flake8 по каталогу Part_4_Engineering/Scroll_26/Quest_2/ и фиксируем стиль кода.
6. Испытание Истинности: запускаем тесты pytest внутри Part_4_Engineering/Scroll_26/Quest_2/ для проверки корректности логики.
7. Проклятие Хаоса: если лент или тесты терпят крах, задача завершается с ошибкой; в противном случае задача завершается успешно и возвращает статус 0.

### 4.2. Declarative Content (Для конфигураций и данных)

ИЗДАНИЕ ИНВЕНТАРЯ (Inventory) для этого артефакта:

- quest_26_2_ci_full.yaml: основная Скрижаль Ритуала Проверки Чистоты и Истинности, что управляет триггерами и шагами.
- 🏰 Ла́тоскоп Ритуала: путь срабатывания триггера — Part_4_Engineering/Scroll_26/Quest_2/\*\* на push и pull_request.
- 🛡️ Скрижаль Разрешений: permissions contents: read, ограничивающая доступ к содержимому репозитория.
- 🗺️ Карта Пути: структура инструментария включает директории Ubuntu окружения и модуля Quest_2.
- 🧭 Круг Призыва: Python 3.10 через actions/setup-python@v5.
- 🧰 Инструменты Тьмы: flake8 и pytest, устанавливаемые через pip.
- 🗡️ Посох Чистоты: шаг проверки чистоты кода (lint) через flake8 по пути Part_4_Engineering/Scroll_26/Quest_2/.
- 🗝️ Камень Истинности: шаг испытания (pytest) по той же директории.
- 🛡️ Камеление Безопасности: использование GitHub Secrets и избегание хранения секретов в открытом виде (не копируй и не вставляй реальные секреты в конфигурацию).

## 5. Structural Decomposition (Декомпозиция структуры)

- Trigger Block: on структура, определяющая события запуска (push, pull_request) и целевые пути.
- Permissions Block: скрижаль прав доступа к содержимому репозитория.
- Jobs Block: единое задание lint-and-test.
  - Runner: ubuntu-latest
  - Steps:
    - Checkout Code: uses actions/checkout@v4
    - Set up Python: uses actions/setup-python@v5 с python-version 3.10
    - Install Dependencies: pip install flake8; pip install pytest
    - Lint: команда flake8 по Part_4_Engineering/Scroll_26/Quest_2/
    - Test: команда pytest по Part_4_Engineering/Scroll_26/Quest_2/

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: По шагам внутри задания выполняются последовательно; несколько заданий могут выполняться параллельно, если настроено.
- Dependencies: actions/checkout@v4, actions/setup-python@v5, Python 3.10, flake8, pytest

### 6.2. Prohibited Actions (Negative Constraints)

- DO NOT store secrets in plain text; используйте секреты GitHub.
- DO NOT print raw data to console в продакшн-режиме.
- DO NOT использовать синхронные сетевые вызовы в главной петле выполнения.
- DO NOT оборачивать конфигурационные файлы в скрипты (как часть скриптов).
- DO NOT изменять версии или пути во время реконструкции.

## 7. Verification & Testing (Верификация)

1-2 Gherkin сценария, описывающие счастливо-путь и один кейс ошибки:

```gherkin
Feature: Quest_2 CI Ritual

  Scenario: Successful execution
    Given a push or pull request targets Quest_2 path and dependencies are available
    When the workflow runs and all steps complete successfully
    Then the job finishes with exit code 0 and the checks pass

  Scenario: Lint or test failure
    Given there are style violations or failing tests in Quest_2
    When the workflow runs
    Then the job fails and reports the error
```
