# .pre-commit-config.yaml Specification

## 1. Meta Information

- Domain: Infrastructure
- Complexity: Medium
- Language: Bash
- Frameworks: pre-commit, black, flake8, docformatter, prettier
- Context: Independent Artifact

## 2. Goal & Purpose (Цель и Назначение)

Context for Creator: Этот артефакт — Скрижаль Хранилищ, свод правил для предкоммитного ритуала. Она перечисляет кузницы (репозитории) и стражей (хуки), которым мы повелеваем в службе качества кода, чтобы каждый коммит проходил через очищение, проверку и корректировку стиля.

Instruction for AI: Высокий уровень намерения — обеспечить единообразие стиля кода, исправлять хаос в файлах до того, как он попадет в хроники репозитория.

Описание на русском языке: .pre-commit-config.yaml задает набор внешних репозиториев и хуков, которые выполняются перед каждым коммиту. Он автоматически удаляет trailing whitespace, добавляет отсутствующую пустую строку в конце файлов, проверяет YAML на синтаксис, ограничивает размер добавляемых файлов, форматирует код и документы согласно принятым правилам, а также корректирует стиль Markdown и YAML в рабочих артефактах.

## 3. Interface Contract (Интерфейсный Контракт)

### 3.1. Inputs (Входы)

- Source: CLI Args
- Format: Text
- Schema: InputData — интерфейс без конкретных полей (пустой интерфейс). Описание: входной контракт для конфигурационного артефакта, не задающий фиксированный набор входов в рамках этого шаблона.

### 3.2. Outputs (Выходы)

- Destination: STDOUT
- Format: Text
- Success Criteria: Exit Code 0
- Schema: OutputResult — интерфейс без конкретных полей (пустой интерфейс). Описание: выходные данные не привязаны к конкретному формату в рамках этого шаблона, возвращают статус выполнения.

## 4. Implementation Details (The Source DNA / Исходный Код)

### 4.1. Algorithmic Logic (Для исполняемого кода)

1. Ритуал начинается с открытия Скрижали Хранилищ и признавания списка кузниц (repos) и их духов-покровителей (hooks).
2. Для каждой кузницы считываются данные о её идентификаторе (repo) и версии (rev), затем активируются указанные хуки.
3. В цепочке действий каждого хука устанавливаются параметры:
   - trailing-whitespace очищает призрачные пробелы в конце строк, если путь не подпадает под исключение .md
   - end-of-file-fixer гарантирует наличие ровно одной пустой строки в конце артефактов
   - check-yaml проверяет синтаксис всех YAML-файлов, чтобы не возник хаос в хрониках
   - check-added-large-files останавливает добавление файлов больше заданного порога (maxkb)
   - black приводит к единообразному стилю Python кода и поддерживает длину строки 88 символов
   - flake8 осуществляет аудит кода на стиль и потенциальные дефекты, с учётом исключений по файлу
   - docformatter корректирует докстринги и комментарии в документах
   - prettier обеспечивает стиль Markdown и YAML в соответствии с настройками
4. Каждый шаг выполняется последовательно по каждому хуку; если какой-либо хук возвращает неуспех, ритуал прерывается и выводится хаос ошибок.
5. По завершении всех хуков, если путь чист и ошибки отсутствуют, ritual завершается с успехом (выход 0).
6. Любая зафиксированная эманация ошибок приводит к немедленному сообщению об ошибке и выводу хаоса в консоль/лог, чтобы хранители могли исправить и повторить ритуал.

### 4.2. Declarative Content (Для конфигураций и данных)

Inventory данных ритуала можно перечислить как набор артефактных предметов, каждый из которых имеет имя, призвание и эффект:

- 🏰 Репозиторий: pre-commit/pre-commit-hooks — версия v6.0.0 — источник стражей, откуда берутся базовые проверки и очистка хвостов хвостов.
- 🛡️ Страж: trailing-whitespace — удаление призрачных пробелов в конце строк; исключение: файлы с расширением .md, чтобы не конфликтовать с форматированием Markdown.
- 🛡️ Страж: end-of-file-fixer — гарантирует наличие ровно одной пустой строки в конце каждого файла.
- 🛡️ Страж: check-yaml — проверяет корректность YAML файлов на синтаксис.
- 🛡️ Страж: check-added-large-files — ограничение размера добавляемых файлов (maxkb=800).
- 🏰 Репозиторий: psf/black — версия 25.9.0 — андреализация единого стиля Python; hook: black с параметром line-length=88.
- 🏰 Репозиторий: pycqa/flake8 — версия 7.3.0 — аудит кода на стиль и возможные дефекты; исключения по файлу: Part_4_Engineering/Scroll_26/Quest_1/spell_with_flaw.py:F401.
- 🏰 Репозиторий: PyCQA/docformatter — версия v1.7.7 — форматирование докстрингов и комментариев; параметры: --in-place, --wrap-summaries=88, --wrap-descriptions=88.
- 🏰 Репозиторий: pre-commit/mirrors-prettier — версия v4.0.0-alpha.8 — хранитель эстетики; хуки: prettier, следят за стилем Markdown и YAML в соответствии с .prettierrc.json.

Истинное намерение Скрижали — вернуть порядок в эфир и хранить древний канон стилистической гармонии в каждом коммите.

## 5. Structural Decomposition (Декомпозиция структуры)

- Репозитории (repos) — главный узел, перечисляющий внешние кузницы и их архивы.
  - Поля узла: repo (URL), rev (версия), hooks (массив хуков).
- Хуки (hooks) — служители, каждая стихия с идентификатором id и локальными параметрами.
  - Примеры параметров: exclude (поправляет исключения для файлов), args (дополнительные параметры).
- Вложенные блоки хуков — конкретные правила и настройки внутри каждого репозитория.
- Конфигурационные параметры — столпы управления размером файлов, форматированием и стилем.

## 6. System Context & Constraints (Системный контекст и Ограничения)

### 6.1. Technical Constraints

- Performance: Standard CPU
- Concurrency: Sync
- Dependencies: pre-commit, black, flake8, docformatter, prettier
- Привязка к конкретным версиям (rev) в каждом репозитории обеспечивает воспроизводимость ритуала.

### 6.2. Prohibited Actions (Negative Constraints)

- НЕ устанавливайте секреты в виде plaintext в конфигурацию (.env хранение за пределами ритуала).
- НЕ выводите сырые данные в консоль в продакшн-режиме без фильтрации.
- НЕ используйте синхронные сетевые вызовы в критическом циклe выполнения хуков.
- НЕ оборачивайте конфиги (yaml/json) в скрипты — держите их как независимые артефакты.
- НЕ изменяйте версии или пути во время реконструкции артефактов без согласования.

## 7. Verification & Testing (Верификация)

### Геркин сценарии

Feature: Script Functionality
Scenario: Successful execution
Given Репозитории и хуки сконфигурированы согласно Скрижали
When выполняется предкоммитный ритуал
Then все хуки проходят успешно и возвращается Exit Code 0

Scenario: Failure due to trailing whitespace
Given файл содержит хвостовые пробелы в конце строк
When выполняется предкоммитный ритуал
Then хуки обнаруживают хаос и возвращают ненулевой код выхода с сообщениями об ошибке

ИССЛЕДУЕМЫЙ АРТЕФАКТ: .pre-commit-config.yaml

ИСТОРИЯ ЭТИХ РИТУАЛОВ НИКОГО НЕ ОТОЖДАТ: данный артефакт — независимая сцена без внешних ссылок, чьё предназначение — поддерживать чистоту и единообразие в хрониках кода.
